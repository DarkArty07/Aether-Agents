# M1.3 tool qualification checkpoint

Status: partial. Every tool has been invoked against the installed boundary, but typed denial is not a functional worker-path pass.

## Passed behavior

- project admission/inspection;
- manifest validation and non-dispatching Run start;
- status, cancellation, semantic close and zero-survivor checks;
- decision/evidence append and trace query;
- provider catalog search/describe and plan-only read-only call;
- fresh-process preservation of JSON-shaped `swarm_message` string payloads.

## Still unaccepted

A successful model-backed Dispatch, real participant message, verified artifact, bounded retry evidence and final cleanup have not passed together on the current installed boundary. That case requires explicit provider/model/budget authority and remains the M1.3 gate.

The earlier fixture qualification identities and correction artifacts remain in Git history/private runtime backups; they are not active repository paths or reusable authority.
