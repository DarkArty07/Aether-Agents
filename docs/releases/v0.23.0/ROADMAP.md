# v0.23 runtime roadmap

## Complete

- local package installation, registration, status and rollback boundary;
- exact 15-tool v1alpha2 surface;
- fixture-first invocation of every endpoint and correction of JSON-shaped string coercion;
- canonical cold-start descriptions for all tools;
- owner-directed active Hermes prompt `0.4.0`, with `3.0.0-hot.3` retained for rollback;
- source/runtime documentation consolidation on one local `main` checkout;
- physical removal of retired runtime/profile/documentation trees.

## Current gate

M1.3 remains partial until one low-risk model-backed case, covered by identifiable current or standing authority, proves dispatch, participant messaging, artifact verification, semantic disposition, close and zero survivors on the current installed boundary. Authority does not require a redundant confirmation, but provider, account, model, budget, timeout, retry and effects must be representable, frozen and inspectable before execution. The current manifest/dispatch path does not yet satisfy the model-selection and cost-evidence portion of that contract.

## Later, separately authorized

- accept or reject model-backed production entry;
- qualify the three allowed profiles;
- compare the 15-tool baseline against any proposed surface change;
- evaluate, retain or roll back prompt `0.4.0` from fresh-session behavioral evidence;
- publish a release.

No later item is implied by local implementation.
