# v0.23 runtime roadmap

## Complete

- local package installation, registration, status and rollback boundary;
- exact 15-tool v1alpha2 surface;
- fixture-first invocation of every endpoint and correction of JSON-shaped string coercion;
- canonical cold-start descriptions for all tools;
- active Hermes prompt `3.0.0-hot.3`;
- source/runtime documentation consolidation on one local `main` checkout;
- physical removal of retired runtime/profile/documentation trees.

## Current gate

M1.3 remains partial until one explicitly authorized, low-risk model-backed case proves dispatch, participant messaging, artifact verification, semantic disposition, close and zero survivors on the current installed boundary. Provider, account, model, budget, timeout, retry and effect authority must be frozen before execution.

## Later, separately authorized

- accept or reject model-backed production entry;
- qualify the three allowed profiles;
- compare the 15-tool baseline against any proposed surface change;
- promote or roll back the hot prompt from comparative evidence;
- publish a release.

No later item is implied by local implementation.
