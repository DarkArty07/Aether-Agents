# v0.19.5 Gate C Evidence — Real Bounded Harmonia Selection

**Verdict:** PASS — bounded topology operationally replaces routine Hermes hub-and-spoke relay

**Architecture verdict:** `VIABLE — BOUNDED`

**Date:** 2026-07-27

**Branch:** `feature/v0.19.0-autonomous-coordination-design`

**Candidate head:** `e8b287b`

**Preserved evidence root:** `/tmp/harmonia-v0195-gatec-K6fJCz/project`

**Gate A contract:** `V0.19.5_GATE_A_PROPOSAL.md`

## 1. Scope and authority

The disposable Gate C exercised one immutable three-task fixture:

```text
task-a -> Hefesto
             |
             v
after task-a closes, eligible candidates = [task-c, task-b]
Harmonia deterministic policy -> selected_task_id = task-b
contract binding -> task-b worker = Daedalus
```

Harmonia selected only `selected_task_id`. The kernel derived eligibility, recomputed the proposal, committed one winner under durable CAS, resolved the worker from the immutable contract and dispatched through `KernelDispatcher -> OlympusRuntimeAdapter -> ACPManager`. Harmonia made no direct ACP call and Hermes performed no routine result relay or successor selection after admission.

The run did not activate Harmonia globally, mutate production configuration, merge PR #113, tag, release, deploy, publish or modify the preserved R8 work.

## 2. Real semantic trace

The source task produced:

```json
{"answer":"SOURCE_OK","task_id":"task-a"}
```

The selected successor read the source evidence identified by the kernel-owned handoff and produced:

```json
{"answer":"SELECTED_OK","source_answer":"SOURCE_OK","task_id":"task-b"}
```

The final harness report asserted every invariant:

- selection committed exactly once;
- canonical selected task was `task-b`;
- resolved worker was `daedalus`;
- exactly one `task-b` dispatch and attempt;
- zero `task-c` dispatches and attempts;
- source and selected task both reached `CLOSED`;
- source semantic result matched exactly;
- selected result proved consumption of `SOURCE_OK`;
- two cleanup completions;
- zero manager sessions and zero attributable survivors.

The real process exited with code `0`.

## 3. Independent durable verification

After the harness completed, Hermes independently opened the evidence read-only and verified:

- 37 durable coordination events;
- non-empty event hashes, writer proofs and authentication tags;
- every `previous_hash` linked to the preceding `event_hash`;
- exactly one `task.selection.committed` for `task-b` and worker `daedalus`;
- one selected dispatch/attempt and no unselected dispatch/attempt;
- two `evidence.receipt.recorded`, two `task.closed` and two `cleanup.completed` events;
- both evidence artifact SHA-256 digests matched their bytes;
- the exact `SOURCE_OK -> SELECTED_OK` semantic results above;
- two completed Olympus sessions: Hefesto and Daedalus;
- two persisted assistant turns;
- neither isolated session ID appeared in the canonical shared observability database;
- no process whose command or environment referenced the disposable root remained in `/proc`.

Independent verifier result:

```json
{
  "cleanup_completed": 2,
  "events": 37,
  "resolved_worker": "daedalus",
  "selected_task_id": "task-b",
  "survivors": [],
  "turns": 2,
  "verdict": "PASS"
}
```

## 4. Live-discovered corrections

The live gate remained fail-closed while two defects absent from the earlier fake path were corrected:

| Commit | Correction | Tracking |
|---|---|---|
| `aa6d6b8` | Bind Daimon hook observability to the database injected into `ACPManager`, preserving disposable-root isolation. | GitHub #117 |
| `0e95a07` | Size the dispatch fence from the immutable task time budget, capped by writer authority, instead of a fixed ten-second lease. | GitHub #118 |
| `e8b287b` | Require exact semantic evidence that the selected successor consumed the source result. | Gate hardening |

A previous attempt that outlived the ten-second fence stopped without a second selection or fallback. The corrected fresh-root run passed.

## 5. Deterministic candidate evidence

After the final corrections and semantic hardening:

- focused Harmonia demo/Gate B tests from a clean committed checkout: `25 passed`;
- coordination suite from a clean committed checkout: `943 passed`;
- full repository suite from a clean committed checkout: `1137 passed`;
- Ruff: PASS;
- `compileall`: PASS;
- wheel build: PASS (`olympus_mcp-0.18.2-py3-none-any.whl`);
- `git diff --check` on the affected scope: PASS;
- fake full-lifecycle demo: PASS with all semantic and lifecycle invariants true.

The earlier editable-worktree counts of 953 coordination and 1147 repository tests included ten preserved, uncommitted R8 pilot tests. They passed, but they are not used as committed-candidate evidence.

The full suite includes deterministic rejection, replay, CAS, stale/divergent projection, exact-winner contention, no-fallback, fixed-worker binding, recovery, lifecycle, evidence and cleanup coverage.

## 6. Residual observation

After the successful result and zero-survivor verification, ACP stdio finalization emitted:

```text
RuntimeError: athrow(): asynchronous generator is already running
```

This warning did not change the exit code, durable lifecycle, logical cleanup or OS survivor result. It is tracked as GitHub #119 and is not silently relabeled fixed.

## 7. Gate C and architecture verdict

**PASS.** v0.19.5 proves that the deterministic kernel design can replace routine Hermes hub-and-spoke coordination for the demonstrated bounded topology: one source task, two contract-bound candidates, one canonical task-only selection, one selected real successor, trusted semantic handoff, durable closure and zero fallback.

**`VIABLE — BOUNDED`.** This is an operational architecture verdict, not merely local implementation evidence.

It does not claim arbitrary DAG support, open-ended planning, model-driven selection, dynamic worker substitution, multi-project load, production migration or global replacement. Harmonia remains default-off. Merge, tag, release, deployment, publication and production activation remain separate user decisions.
