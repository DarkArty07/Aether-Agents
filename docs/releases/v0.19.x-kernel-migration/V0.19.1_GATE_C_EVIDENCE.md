# v0.19.1 Gate C — Live Disposable Validation Evidence

**Execution date:** 2026-07-26
**Candidate:** `3942550608005c56c8086e04558d70d569fb0af2`
**Strict verdict:** **BLOCKED**
**Runtime composition result:** Live ACP acceptance, exact replay deduplication, durable status, and bounded stop all worked once.
**Blocking defect:** [GitHub #106](https://github.com/DarkArty07/Aether-Agents/issues/106)

## Authorization and limits

The user explicitly authorized this Gate C only:

- project root `/home/arty/Escritorio/agentes/aether`;
- one read-only Hefesto task;
- at most one Harmonia ACP session;
- temporary configuration and a bounded fresh-process handler when required;
- 15-minute outer limit;
- evidence capture and immediate rollback;
- no merge, tag, release, deployment, or v0.19.2 work.

No second live session was launched after the gate revealed a storage-root mismatch.

## Frozen request

| Field | Value |
|---|---|
| Request ID | `gate-c-v0191-readonly-20260726` |
| Worker | `hefesto` |
| Plan revision | `1` |
| Snapshot | `sha256:b0ca7091fc392c2c47861368ec6db732cf8fc0d16ee0022f3ddf6fc010ccb0bb` |
| Expected task effect | Read one versioned evidence file; no writes or external effects |
| Model budget | `3` |
| QA reserve | `1` |
| Recovery reserve | `1` |
| Task timeout | `180` seconds |
| Gate outer timeout | `540` seconds within the authorized 15-minute bound |

The objective asked Hefesto to read only `V0.19.1_IMPLEMENTATION_EVIDENCE.md` and report its result and implementation candidate hash.

## Environment preflight

- Static config parse: PASS.
- Contract parse: PASS.
- Six Daimon profiles discovered; Hefesto present.
- No live Hefesto ACP process before start.
- Existing stale Hefesto DB row from issue #46 had no corresponding process and was not reused.
- Five pre-existing Olympus server processes belonged to separate gateway/TUI owners. None was terminated.
- The interactive MCP schema predated Harmonia, so the published handler was loaded in one bounded fresh Python process with an isolated Olympus session DB.
- Signing keys were generated inside that process, were never printed or persisted, and were removed before process exit.

## Live sequence

The fresh process called the published `server.call_tool("harmonia", arguments)` handler.

| Step | Result | Elapsed |
|---|---|---:|
| `start` | `ok=true`, `state=session_bound`, `outbox_status=SENT` | 2.588 s |
| Exact `start` replay | Same run, task, contract, attempt and ACP session | 0.021 s |
| `status` | Same durable identities; no ACP poll by Harmonia | 0.002 s |
| `stop` | `ok=true`, `state=cancel_requested` | 0.021 s |
| Status after stop | `cancel_requested`, `cleanup_unverified` | 0.002 s |

Total handler-process runtime: **3.206 seconds**.

### Durable identities

- Project: `4b3faab76a0756eae52ca847aa4213615e707ac26111c715f219c364e430038d`
- Run: `run-91c39c0610d258b0dccb65d5eba5e2f9`
- Task: `task-660c51dfd29dbcebdf013486a6ad33a9`
- Contract: `contract-af81930f0070187297cf22a44f577bb7`
- Harmonia ACP session: `19126b3f-25fe-5d50-870d-f18535db361b`
- Raw ACP session recorded by Olympus: `1eb51f43-a147-4ecc-acec-08ed4b257647`

The isolated Olympus DB contains exactly one Hefesto session. It was active before stop and `cancelled` after shutdown. Exact replay did not create a second session.

## What was demonstrated

1. The public server handler loaded the temporary Harmonia configuration.
2. Admission accepted exactly one project, one task and one worker.
3. The kernel durably created the run, task, attempt and dispatch before ACP effect.
4. ACP accepted one deterministic session identity and acknowledged prompt delivery.
5. Exact request replay returned the same durable and ACP identities without another session.
6. `status` reconstructed state from the durable store.
7. `stop` wrote `cancel.intent` before the close operation.
8. The final public state remained honest: `cancel_requested` with `cleanup_unverified`.
9. The repository HEAD, index and dirty-baseline fingerprints were unchanged.

## What was not demonstrated

The isolated Olympus DB recorded zero assistant turns and zero tool calls. Stop followed replay/status immediately, before Hefesto produced a semantic response. Therefore this gate proves ACP session acceptance and delivery acknowledgement, **not** successful completion or correctness of the read-only task.

No response, completion, worker quality, multi-agent scheduling, recovery, or release readiness is claimed.

## Blocking defect

The frozen control root was:

```text
/home/arty/Escritorio/agentes/aether/.olympus/gate-c-v0.19.1-20260726
```

However, `server.init_server()` derives Harmonia's Aether home from:

```python
config.profiles_dir.expanduser().resolve().parent
```

Because `profiles_dir` was a symlink to the real profiles, the ledger was created under the canonical `home/.olympus/projects/...` path instead of the frozen control root. This ignored `OlympusV3Config.aether_home` and violated the disposable isolation packet.

The live composition itself worked, but **VALIDATED requires every frozen limit to hold**. Issue #106 remains unresolved, so the strict Gate C verdict is `BLOCKED`, not `VALIDATED`.

## Durable ledger evidence

Before rollback, the newly created store had:

- SQLite `PRAGMA integrity_check`: `ok`;
- 9 events;
- one each of `run.created`, `task.created`, `task.admitted`, `task.ready`, `task.dispatched`, `attempt.started`, `dispatch.staged`, `session.bound`, and `cancel.intent`;
- one dispatch outbox entry with `status=SENT`, `attempts=1`, `reconciliation_required=0`;
- exactly one session binding;
- SHA-256 `255be16bc0ebfb5449307f651c204c06689a922a3bb37bfddb1bbb83708e2bb2`.

## Rollback and cleanup

- Temporary config now has `enabled=false`, only `legacy`, empty allowlist, and `max_active_runs=0`.
- Active `home/olympus_v3.yaml` remains absent.
- Ephemeral signing keys were removed from the process environment.
- The temporary profiles symlink was removed.
- No exact `--acp-server` process remained after cleanup.
- The canonical Gate C project-store path is absent.
- The ephemeral-key ledger was moved intact into the evidence package so it cannot poison a future canonical activation.
- The five pre-existing Olympus servers remained untouched.
- Git HEAD remained `3942550608005c56c8086e04558d70d569fb0af2`.
- Staged index remained empty.
- Dirty-baseline and tracked-diff SHA-256 fingerprints were unchanged.

## Captured package

Unversioned runtime package retained at:

```text
.olympus/gate-c-v0.19.1-20260726/
```

| Artifact | SHA-256 |
|---|---|
| `transcript.json` | `7f7772447ff74196b9b26e4be1e6c663a223375584ec01f55526706ed012a25b` |
| `olympus_v3.db` | `e16450c9fd94d09a3761d05df2635c53c2fdbfcea262b6ec23c36c06b1ec2423` |
| `captured-project-store/coordination-v0.19.1.sqlite` | `255be16bc0ebfb5449307f651c204c06689a922a3bb37bfddb1bbb83708e2bb2` |
| `stdout.log` | empty-file SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `stderr.log` | empty-file SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |

## Correction follow-up

Issue #106 was later corrected deterministically at `5fb7510` using `config.aether_home` as Harmonia's explicit store authority. The external-directory and symlink regressions passed, followed by Gate B with 181 focused, 764 coordination, and 956 repository tests. Complete evidence is in [`V0.19.1_ISSUE_106_CORRECTION_EVIDENCE.md`](V0.19.1_ISSUE_106_CORRECTION_EVIDENCE.md).

No configuration was activated and no second ACP session was executed. The historical Gate C verdict above remains `BLOCKED`; a later live rerun requires new explicit authorization and must preserve one unambiguous storage root. If semantic delivery is part of the claim, its bounded matrix must also allow enough observation time to collect a persisted Hefesto turn.
