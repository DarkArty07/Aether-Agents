# v0.19.4 Gate C Evidence — Live Fixed Hefesto→Ictinus Handoff

**Verdict:** PASS — one bounded disposable live chain completed with no routine Hermes relay

**Date:** 2026-07-27

**Branch:** `feature/v0.19.0-autonomous-coordination-design`

**Final live run:** `run-5a1e8bcdededab44d50694086e9f4dd1`

**Evidence root:** `/tmp/aether-v0194-gatec-live-8`

**Gate B evidence:** `V0.19.4_GATE_B_EVIDENCE.md`

## 1. Authorized scope

Gate C exercised exactly one caller-fixed two-task contract in a disposable project and isolated control root:

```text
gate-a -> Hefesto
gate-b -> Ictinus
gate-b prerequisite -> gate-a
```

Harmonia remained default-off outside the explicit test process. The run did not modify production configuration, deploy, publish, select workers dynamically, or use Hermes to relay A's result to B.

## 2. Real ACP trace

The supported public `harmonia start` surface admitted the complete fixed contract once. The kernel then executed:

```text
Hefesto A running
  -> A terminal observation
  -> verifier-owned A evidence receipt
  -> digest-addressed immutable handoff snapshot
  -> ACPManager cleanup with zero-survivor proof
  -> A CLOSED
  -> kernel stages Ictinus B from persisted authority
  -> B re-verifies and consumes A snapshot
  -> kernel-owned materialization of read-only B ACP JSON result
  -> verifier-owned B evidence receipt
  -> ACPManager cleanup with zero-survivor proof
  -> B CLOSED
```

The durable event sequence proves cleanup-before-handoff:

| Event | Ledger sequence |
|---|---:|
| A `cleanup.completed` | 17 |
| A `task.closed` | 19 |
| B `dispatch.staged` | 24 |
| B `task.closed` | 34 |

There were exactly two `dispatch.staged`, two `session.bound`, two `runtime.terminal.observed`, two `evidence.receipt.recorded`, two `cleanup.completed`, two `cleanup.receipt.recorded`, and two `task.closed` events. There were zero `cleanup.failed` and zero `cleanup.unknown` events.

## 3. Exact artifacts and B consumption

- Source SHA-256: `sha256:f79bb285b557b4de34bf5164629cfcbe1dc7943798e95e7518a2a57eb21ad15d`
- A result SHA-256: `sha256:323f757b9e9792f19abfdf345ba6ff06a337968d14add90b393dcd3de58eb598`
- A handoff snapshot SHA-256: `sha256:5c97c637329a1a22a146453be923d8bcea86c473513e9d1ad26cd6dc79ae841a`
- B result SHA-256: `sha256:17261fd77253ab87df74ce491837cfd2af3ce52bcfe5717727af5cd4ce4f403e`

The snapshot JSON equals the verifier-accepted A result document. Ictinus returned:

```json
{
  "answer": "B_CONSUMED_VERIFIED_HANDOFF",
  "handoff_digest": "sha256:5c97c637329a1a22a146453be923d8bcea86c473513e9d1ad26cd6dc79ae841a",
  "source_sha256": "sha256:f79bb285b557b4de34bf5164629cfcbe1dc7943798e95e7518a2a57eb21ad15d"
}
```

The reported handoff digest exactly matches both the dispatch metadata and the bytes Ictinus consumed.

## 4. Cleanup and isolation evidence

Both cleanup proofs contain the exact survivor map with every value `false`:

```json
{
  "acp_mapping": false,
  "logical_manager_session": false,
  "pid_session_mapping": false,
  "prompt_task": false
}
```

At acceptance time:

- active dispatch/cleanup/finalization leases: `[]`;
- logical manager sessions after shutdown: `[]`;
- ACP processes for the disposable root after shutdown: `[]`;
- persisted Olympus sessions: one completed Hefesto and one completed Ictinus;
- canonical servers untouched: `true`.

The disposable coordination store is below the isolated control root, not the canonical runtime home.

## 5. Live-discovered corrections

Gate C remained fail-closed while live execution exposed and corrected gaps absent from the deterministic Gate B matrix:

| Commit | Correction |
|---|---|
| `3b8b69b` | Align public cleanup identity with ACPManager. |
| `6e3b323` | Close against the renewed dispatch lease fence. |
| `11ff5a8` | Capture a read-only worker's exact structured ACP result and materialize verifier-owned evidence. |
| `122958c` | Unwrap the real nested ACP progress shape. |
| `7ccd000` | Release cleanup and close-finalization operation leases immediately. |

The final correction state passed:

- coordination plus ACP lifecycle from a clean committed checkout: `932 passed`;
- full repository suite from a clean committed checkout: `1097 passed`;
- Ruff: PASS;
- `compileall`: PASS.

The earlier local counts of 942/1107 included ten preserved, uncommitted R8 pilot tests and are not used as committed-candidate evidence.

## 6. Independent verification

After the harness returned PASS, Hermes independently re-read the SQLite ledger, evidence artifacts, handoff bytes, lease table, Olympus session table, and `/proc` process state. The independent verifier reproduced the same event ordering and digest and found zero active operation leases and zero process survivors.

Harness report:

```text
/tmp/aether-v0194-gatec-live-8/control/gatec-report.json
```

## 7. Gate C verdict and boundaries

**PASS.** v0.19.4 proves one real, fixed, digest-bound, cleanup-gated Hefesto→Ictinus ACP handoff through the supported default-off Harmonia surface, with no routine Hermes relay and no dynamic worker selection.

This verdict does not authorize merge, tag, release, deployment, production activation, arbitrary DAGs, repair loops, or v0.19.5 dynamic selection. Those remain separate decisions and gates.
