# v0.19.1 Gate B — Deterministic Implementation Evidence

**Date:** 2026-07-26
**Result:** PASS
**Implementation baseline:** `33aea3d4085bc4158de726279996334d44712c2c`
**Implementation candidate:** `39ca82dc4ab34ba487d4371d24bf689f4e8f4bba`

> Deterministic implementation green; ACP live execution not authorized and operational hypothesis not yet validated.

## Authorized scope

Gate B covered local source implementation and deterministic tests for the default-off public tool:

```text
harmonia(action="start" | "status" | "stop")
```

It did not authorize or perform runtime configuration activation, live ACP execution, merge, tag, release, deployment, or Gate C.

## Atomic implementation history

```text
c7c0a30 test(coordination): define Harmonia admission contract
d3a43c8 feat(coordination): add default-off Harmonia contract
e880624 test(coordination): require isolated read-only Harmonia stores
3620469 feat(coordination): add project store resolver and inspector
2bcaeea test(coordination): specify server-owned Harmonia runtime lifecycle
c82b403 feat(coordination): compose project-scoped Harmonia runtimes
60fcc8d test(coordination): specify Harmonia durable admission identity
c7adbec feat(coordination): enforce durable Harmonia run identity
d0a9b5a test(coordination): require contractual Harmonia dispatch prompts
4805cf2 feat(coordination): bind Harmonia dispatch to canonical prompts
e9c1911 test(coordination): specify Harmonia start status and stop
b5f90b1 feat(coordination): orchestrate Harmonia single-task actions
68c0f34 test(olympus): specify dedicated Harmonia MCP seam
67890bb feat(olympus): expose default-off Harmonia lifecycle
1f7aa60 chore(tests): normalize setup CLI import order
076b40b test(coordination): avoid credential-shaped sanitization fixture
39ca82d test(coordination): separate Harmonia configuration gate
```

## Executed verification

### Focused Gate B commands

```text
python -m pytest tests/coordination/test_harmonia_contract.py -q
54 passed

python -m pytest tests/coordination/test_harmonia_config.py -q
13 passed

python -m pytest tests/coordination/test_harmonia_store.py -q
6 passed

python -m pytest tests/coordination/test_harmonia_runtime.py -q
10 passed

python -m pytest tests/coordination/test_harmonia_admission.py -q
8 passed

python -m pytest tests/coordination/test_harmonia_service.py -q
12 passed

python -m pytest tests/coordination/test_harmonia_server.py -q
7 passed

python -m pytest tests/coordination/test_kernel_dispatcher.py tests/coordination/test_olympus_adapter.py -q
45 passed

python -m pytest tests/test_server_schema.py tests/coordination/test_shadow_compatibility.py -q
24 passed
```

Focused total: **179 passed** across nine commands.

### Subsystem and repository gates

```text
python -m pytest tests/coordination -q
762 passed

python -m pytest -q
954 passed

python -m compileall -q src tests
PASS

python -m ruff check src tests
All checks passed!

git diff --check
git diff --cached --check
PASS
```

The full suite was rerun after every Gate B correction that changed the effective candidate. The final recorded run returned `954 passed in 10.11s` before the test-file-only artifact correction; the post-correction run also returned `954 passed in 10.11s` with identical semantics.

## Scope and negative assertions

Implementation diff through `39ca82d`:

```text
23 files changed, 3093 insertions(+), 49 deletions(-)
```

Deterministic assertions passed:

- no change to `src/olympus_v3/db.py`;
- no Pilot/PilotStore file in the implementation range or reference from the Harmonia runtime path;
- `talk_to` schema and `_handle_talk_to` AST are unchanged from baseline;
- no `kernel_run`, `harmonia_start`, `harmonia_status`, or `harmonia_stop` surface;
- no `complete_task`, trusted-evidence, closure-receipt, or handoff implementation;
- no raw `ACPManager.agents` or `.sessions` access outside `ACPManager`;
- Harmonia tests construct no subprocess or real `ACPManager`;
- configuration remains default-off and legacy-compatible;
- redacted baseline-to-candidate scan found no private key, access key, token, bearer value, password, or credential-shaped assignment;
- the pre-existing unrelated dirty worktree remained unstaged and unmodified by Gate B commits.

## Defects found and closed

- GitHub #102 — full-width derived project/session identities violated inherited grammar/length limits; fixed in `b5f90b1`.
- GitHub #103 — pre-existing Ruff import grouping blocked the canonical repository gate; isolated fix in `1f7aa60`.
- GitHub #104 — sanitization fixture was credential-shaped; fixed in `076b40b`.
- GitHub #105 — required `test_harmonia_config.py` artifact was omitted although its tests existed; original TDD-first tests moved in `39ca82d`.

## Gate C follow-up

At Gate B closeout, Gate C remained separately blocked and required explicit authorization before configuration activation or any live ACP session.

The user later authorized one disposable Gate C run on 2026-07-26. Its strict verdict is **BLOCKED**: live ACP acceptance, exact replay deduplication, durable status and bounded stop worked once, but issue #106 caused the ledger root to escape the frozen control root, and stop occurred before any Hefesto turn was persisted.

Complete evidence and rollback details are versioned in [`V0.19.1_GATE_C_EVIDENCE.md`](V0.19.1_GATE_C_EVIDENCE.md). No second live session, merge, tag, release or deployment was performed.
