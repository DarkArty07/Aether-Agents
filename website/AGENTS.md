# Website workstream

Read the root AGENTS.md and website/IMPLEMENTATION_PLAN.md. Work only in this isolated
website worktree; do not push, publish, deploy, activate an Aether runtime, or merge to
main. A contract-authorized unit may consume its reviewed prerequisite with an ordinary
non-rewriting Git merge and may make local commits that keep inherited and owned changes
inspectable.

The owner's first review authorized a Spanish copy refinement and removal of visible
sketch numbering; the current owner review additionally authorizes a broader clarity
pass, Mexican Spanish and cleanup of ambiguous visual symbols. See
DETAIL_REFINEMENT_PLAN.md and COPY_REFINEMENT_MX.md. Preserve main headlines and
meaning; HOMEPAGE_PROPOSAL.md and COPY_REFINEMENT.md retain earlier wording. Section 07 is
authorship, not the superseded release-readiness panel. The owner's final direction
additionally selects the new Morfeo image for the hero and the abstract ether image for
the finale; see FINAL_POLISH_PLAN.md. Keep original PNG files in assets/images.
The graph remains vector-led with masked classical margins from the approved artwork.

Documentation is rendered directly from the tracked repository docs/ corpus, not
duplicated. Its explicit manifest owns the five ordered groups, with docs/index.md
represented only at /docs/; every other tracked Markdown page has one article route.
Canonical pages remain English, while the index may provide Mexican-Spanish search
orientation. Keep the capability registry as the current-status authority and label
this site as a stabilization build rather than a release or installation path. Preserve
tracked-only ingestion, source-revision links, safe sanitization, no-JavaScript reading,
responsive navigation, local code/table scrolling and landing identity/artwork/motion.

Run `npm run build`, `npm run check`, `npm test`, and `npm run test:e2e` from website.
Build assets are generated/ignored. Dependencies and lock stay inside this directory.
Never present decorative animations as live product data or infer release qualification.
