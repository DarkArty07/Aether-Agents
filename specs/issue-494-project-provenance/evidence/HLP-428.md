# HLP-428 evidence — Project provenance, early refusal, and bounded review failure

## Pre-dispatch inspection and breakdown

Decomposition reference: `specs/issue-494-project-provenance/tasks.md` committed at Aether
revision `9ec079104908eab57475e2797e8ce70c2a3b798b` by Supervisor on base
`a27516f04843dca4f0ae77b5aa2acfbd41b80df1`.

Governing authority: Objective Contract `oc_bc27d75a1165818f@v1` (SHA-256
`5c848f98c8adfb9fcd572e64a8d60bbb4825a147286fd4c3133a8d97cc83d8be`).

## AE-494 implementation evidence — portable artifact and ledger reconciliation

**Producer:** Implementer unit AE-494 (card `t_b91f5208`).
This section records producer facts, exact revisions, and reproducible commands without
claiming merge, check, or issue state that has not been observed. Dual-repository pull
requests, merges, and issue reconciliation belong exclusively to INT-494.

### Reviewed fork producer and exact revisions

| Item | Value |
| --- | --- |
| Behavior producer | HF-494 (card `t_78fe784a`), approved by same-card Supervisor review (round 4, strict contract audit) |
| Candidate commit | `184a6d03bcab8964fa507ada4d02edc13cdcfb9f` |
| Candidate tree | `dd579f0f587461f26d59bcb2478069752cd13055` |
| Fork repository | `https://github.com/DarkArty07/aether-hermes` |
| Fork base commit | `aed6591a69f453a1867b73628603e7b53ba40ffc` |
| Fork base tree | `2258311f9aa52c58fda9bdbd03183ba50ac85df0` |
| Branch | `fix/494-project-provenance-and-bounded-failure` (local to the fork clone and unpushed when this snapshot was taken; see the INT-494 section for its merge and current remote state) |
| Accepted commits | `e97c1be069` (fix), `b0371537c0` (scoping/normalization), `184a6d03bc` (AC3 generic matrix) |
| Touched files | 2 files, `+973/-131`: `hermes_cli/kanban_db.py` (+205/-131), `tests/hermes_cli/test_kanban_project_provenance.py` (+768/-0) |
| Baseline digests | `hermes_cli/kanban_db.py`: `299d3580f72d54a767bb314b65d097326086871aed633e9b6ebdc33f26bbf001`; `tools/kanban_tools.py`: `9e288c1e00cb00771cd4cc718103fda78fc5fc11fa08b97dcd70dfa221d629cc` |
| Candidate digests | `hermes_cli/kanban_db.py`: `465000e602df324fec8c39d36bfc7effcbf7b021d4a1a2d888afc53afd3b3a17`; `tests/hermes_cli/test_kanban_project_provenance.py`: `b174e0d91dce053ae8c6ac128a31af642c156e2ba97613ea03a9869a4c8f5197` |
| Reviewer evidence | `hf494-round4-reviewer-evidence.tgz` (SHA-256 `859d05bee46c971d628fec4fa420b7aaa80613218dc3c529c1de3fe6f8717fd6`) |

Fork behavioral results are reported from the independently approved HF-494 handoff:
- AC1: RED reproduced at base `aed6591a69` (4 failed / 1 passed, `Expected canonical project ..., got None`); candidate tip passed 5/5.
- AC2: supported route verified with canonical Project, fresh `.worktrees/<child-id>`, direct parent edge, review claim with `source_status=review`, and real `is-inside-work-tree` materialization.
- AC2 early refusal: verified early refusal on path traversal, absent parent edge, and unbound project with zero inserted rows.
- AC3: complete refusal matrix verified (mismatch/board/traversal/absent-edge/contradictory-registry/symlink refuse); generic non-Aether cross-project task creation remains constructible; same-profile linked projects and affinity recovery pass.
- AC4: real review-lane containment verified (tick 1 auto_blocked, blocked, `consecutive_failures=1`, claim/worker cleared, `gave_up` present, `origin_signal` absent, 0 notified subscribers; ticks 2-3 stable).
- Mutation pins: 4 single-line mutation pins verified.

### Portable patch artifact

| Item | Value |
| --- | --- |
| Component | `HLP-428` |
| Patch path | `patches/hermes/HLP-428-project-provenance-and-bounded-failure.patch` |
| SHA-256 | `6fe6b20f8a932356da3f5dcb741b3d79f4aa1194dd9f5471c811f3b74a70366f` |
| Lines / bytes | 1217 lines / 52696 bytes |
| Generation command | `git -C <fork-repo> diff aed6591a69f453a1867b73628603e7b53ba40ffc..184a6d03bcab8964fa507ada4d02edc13cdcfb9f > patches/hermes/HLP-428-project-provenance-and-bounded-failure.patch` |
| Reconstruction | `git apply --check` passes cleanly on base `aed6591a69f453a1867b73628603e7b53ba40ffc`. Applying the patch to a clean checkout at that base and writing tree yields `dd579f0f587461f26d59bcb2478069752cd13055`, matching candidate tree byte for byte. |
| Portability | Repository-relative paths only; zero machine paths, usernames, credentials, or runtime state. |

### Reconciliation and repository surfaces

| Surface | Changes |
| --- | --- |
| `.gitattributes` | Added 1 whitespace line: `patches/hermes/HLP-428-project-provenance-and-bounded-failure.patch whitespace=-trailing-space` |
| `.github/workflows/policy.yml` | Added 1 allow-list line: `patches/hermes/HLP-428-project-provenance-and-bounded-failure.patch` |
| `HERMES_LOCAL_PATCHES.md` | Added summary-table row and detailed section under heading `## HLP-428 — recover Project provenance, fail closed on cross-project mismatch, and bound review failure (maintained fork, 2026-09-23)` |
| Reconciliation fragment | `specs/001-aether-v1-productization/evidence/hermes-patch-reconciliation/entries/HLP-428.json` created. Per critical mechanics, declares `components: ["HLP-428", "hermes_cli/kanban_db.py"]` because `test_kanban_project_provenance.py` does not exist at pre-merge pin `aed6591a69...` |
| Aggregate & preflight | Regenerated at pin `aed6591a69f453a1867b73628603e7b53ba40ffc` with 32 records (`present: 30`, `partial: 0`, `absent: 0`, `unverified: 2` [`HLP-246`, `HLP-247`]) |
| Tests | `tests/test_hermes_patch_reconciliation.py` updated: `EXPECTED_ACTIVE_IDS` gains `HLP-428` (32 total), `PATCH_DIGESTS` gains `HLP-428` entry binding SHA-256 `6fe6b20f...` |

### Reproducible verification results

| Gate / Command | Observed Result | Evidence |
| --- | --- | --- |
| `uv run python scripts/check_documentation.py` | Exit 0 | `documentation validation passed` |
| `uv run python scripts/check_public_artifacts.py --root .` | Exit 0 | `public artifact path scan passed: tracked surface + 0 artifact(s)` |
| `uv run python scripts/validate_hermes_patch_reconciliation.py --check --fork-checkout <clean-base-worktree> --json` | Exit 0 | `status: current`, `records: 32`, `present: 30`, `absent: 0`, `refusing: []`, `unverified: ["HLP-246", "HLP-247"]` |
| `uv run pytest tests/test_hermes_patch_reconciliation.py` | Exit 0 | 23 passed |
| `uv run pytest tests/test_public_artifacts.py` | Exit 0 | 9 passed |
| `uv run pytest tests/test_contract_quality_documents.py` | Exit 0 | 14 passed, 1 skipped |
| Byte-for-byte tree reconstruction | Verified | Base `aed6591a69` + patch -> write-tree = `dd579f0f587461f26d59bcb2478069752cd13055` |

### Observed GitHub and release status

*Historical AE-494 snapshot — recorded by the implementation unit at the time it finished,
before the terminal integration ran. Superseded by the INT-494 section below; retained as
attribution for the producer's then-current observation.*

- Fork PR: NOT OPEN / NOT MERGED (deferred to INT-494)
- Aether PR: NOT OPEN / NOT MERGED (deferred to INT-494)
- Issue #494 state: OPEN (unmodified by implementation unit)
- Release / tag / activation: NONE (no activation, tag, or publication)

---

## INT-494 integration and terminal closeout

**Producer:** Supervisor unit INT-494 (card `t_26010baf`). This section records only facts
observed at integration time. No activation, release, tag, publication or deployment was
performed.

### Independent verification at the exact reviewed bytes

The accepted candidate bytes were re-verified rather than trusted from the unit handoff:

| Check | Observed |
| --- | --- |
| Baseline digests at `aed6591a69` | `hermes_cli/kanban_db.py` `299d3580f72d54a767bb314b65d097326086871aed633e9b6ebdc33f26bbf001`; `tools/kanban_tools.py` `9e288c1e00cb00771cd4cc718103fda78fc5fc11fa08b97dcd70dfa221d629cc` — both equal the review's values |
| Candidate digests at `184a6d03bc` | `hermes_cli/kanban_db.py` `465000e602df324fec8c39d36bfc7effcbf7b021d4a1a2d888afc53afd3b3a17`; `tests/hermes_cli/test_kanban_project_provenance.py` `b174e0d91dce053ae8c6ac128a31af642c156e2ba97613ea03a9869a4c8f5197` — both equal the review's values |
| AC1 RED at the unchanged revision | The delivered module copied onto base production bytes in a detached control worktree: `4 failed, 1 passed`, including `AssertionError: Expected canonical project 'p_646df9d7', got None` |
| AC1/AC2/AC3/AC4 GREEN at the candidate | Same module at `184a6d03bc`: `5 passed` |
| AC3 fail-before property | At base the matrix fails `DID NOT RAISE ValueError` at `test_kanban_project_provenance.py:360`; it passes at the candidate |
| Touched-scope battery | `100 passed, 0 failed` across 5 files (`test_kanban_project_provenance`, `test_kanban_board_project`, `test_kanban_project_link`, `test_kanban_session_affinity`, `test_kanban_collaboration`) with `HERMES_TEST_FILE_RETRIES=0` |
| Fork Actions | NOT RUN — inherited Actions remain disabled for this repository. Reported as NOT RUN, never green |

### Maintained-fork merge

| Item | Value |
| --- | --- |
| Pull request | `DarkArty07/aether-hermes#16` — MERGED, no bypass, merge commit |
| Merge commit | `58f8c37a49b341f25b8fdd6310542fe932031b8d` |
| Merge tree | `a93162c1a867202b03c12fa372c71029152fdcf7` |
| Parents | `84139ae9d8adff23ba790c9289aa631a2490ce38` (previous `aether-main`), `665653b471a66cb5089578ba123c2a71e286a43c` (branch tip including the `AETHER_FORK.md` record) |
| Branch commits preserved | `e97c1be069`, `b0371537c0`, `184a6d03bc`, `665653b471` — all reachable as inspectable history, no squash or rebase |
| Merged file digests | `hermes_cli/kanban_db.py` `465000e602df…`; `tests/hermes_cli/test_kanban_project_provenance.py` `b174e0d91dce…` — identical to the reviewed candidate |
| Required checks | None configured on this repository; GitHub Actions disabled. NOT RUN |

`AETHER_FORK.md` gained the §"Project provenance recovery, early refusal, and bounded
failure containment (Aether #494)" record naming the three behavior commits, the behavior,
candidate evidence, rollback and retirement gate.

### Portable artifact and reconciliation

| Item | Value |
| --- | --- |
| Patch SHA-256 | `6fe6b20f8a932356da3f5dcb741b3d79f4aa1194dd9f5471c811f3b74a70366f` — unchanged from the reviewed artifact |
| Reconstruction | `git apply --check` plus `git apply` on a freshly materialized tree at `aed6591a69` yields `write-tree` `dd579f0f587461f26d59bcb2478069752cd13055`, equal to the reviewed candidate tree byte for byte |
| Aggregate re-pin | Regenerated at the merged revision `58f8c37a49b341f25b8fdd6310542fe932031b8d`; `records: 32`, `present: 30`, `partial: 0`, `absent: 0`, `refusing: []`, `unverified: [HLP-246, HLP-247]` |
| HLP-428 fragment | Now declares `hermes_cli/kanban_db.py` **and** `tests/hermes_cli/test_kanban_project_provenance.py`, both `present` at the merged revision. The module was deliberately omitted while the pin predated it, because a declared-absent path refuses candidate preparation |
| Manifest conflict | `.github/workflows/policy.yml` was resolved as a union: this objective's contract entry and a concurrent objective's entry are both retained. No entry was dropped |

### Aether-side gates at the integrated revision

| Gate | Observed |
| --- | --- |
| `scripts/validate_hermes_patch_reconciliation.py --check --fork-checkout <merged checkout>` | `status: current` |
| `tests/test_hermes_patch_reconciliation.py` | 23 passed |
| `scripts/check_documentation.py` | `documentation validation passed` |
| `scripts/check_public_artifacts.py --root .` | `public artifact path scan passed: tracked surface + 0 artifact(s)` |
| `scripts/check_hermes_baseline_drift.py --json` | exit 0, no drift findings |
| Ruff check / format --check (CI scope) | `All checks passed!` / `172 files already formatted` |
| MyPy `src/aether_agents` | `Success: no issues found in 69 source files` |
| compileall | exit 0 |
| `git diff --check` | clean |

### Preserved state

The installed Aether runtime and the loaded maintained-fork source were **not** changed,
restarted or re-pointed; this phase delivers reviewed source only. The concurrently
executing objective's workers and every unrelated live board were preserved: read-only
byte and row fingerprints taken before any writer ran showed no file added or removed and
no deleted rows, and all deltas attribute to concurrent heartbeat and claim churn rather
than to this objective's lanes.

### Aether merge and merged-tree readback

| Item | Value |
| --- | --- |
| Pull request | `DarkArty07/Aether-Agents#508` — MERGED, protected branch, no bypass or force |
| Merge commit | `afb59d6a0d3f37d5767e3cc07ecbe1ca1f8f8aa7` |
| Merge tree | `0569657846ba5e9dc86a0090f8909513695def0e` |
| Parents | `f112738862c486ba92a4da70524b5873649283be` (previous `main`), `d2bd8953c8ba4b78a51de6ff8c5bfc479949d4f2` (branch tip) |
| Required checks | All four configured contexts pass at the merged head: `pull-request-target`, `policy (3.11)`, `policy (3.12)`, `policy (3.13)` |
| Optional matrix | `observation-qualification` 3.11/3.12/3.13 all completed SUCCESS. This change does not modify observation source; the optional matrix is recorded as observed, not as a required gate |
| Merged patch digest | `6fe6b20f8a932356da3f5dcb741b3d79f4aa1194dd9f5471c811f3b74a70366f` — byte-identical to the reviewed artifact |
| Merged-tree artifacts | Breakdown, contract, ledger, patch, HLP-428 fragment, aggregate and evidence report all present on the merged `main` |

Post-merge gates re-executed on a clean checkout of the merge commit: reconciliation
`--check` reported `status: current` at the merged fork revision `58f8c37a49…`;
`tests/test_hermes_patch_reconciliation.py` 23 passed; documentation, public-artifact and
baseline-drift checks passed; Ruff check, MyPy and `git diff --check` clean.

Decisive route re-verified at the exact final merged fork revision `58f8c37a49…`:

| Control | Observed |
| --- | --- |
| Delivered module with the merged production source | `5 passed, 0 failed` |
| Identical module with only `hermes_cli/kanban_db.py` reverted to `aed6591a69` bytes | `4 failed, 1 passed` — the AC1/AC2/AC3/AC4 fail-before property survives at the final revision |
| Wider affected fork surface (8 files, incl. collaboration, block and tool modules) | `92 passed, 0 failed` |
| Aether full bootstrap runner | `1958 passed, 70 skipped, 0 failed` |

### Issue disposition

`#494` remains **OPEN**. A source-only merge does not close it. Its successor obligations
are the corrected *effective* runtime and a real review canary: live adoption of the merged
fork revision `58f8c37a49…`, the mixed-version/activation phase after the concurrent
objective settles, and end-to-end confirmation that the supported route no longer strands
review. No success is claimed for the effective runtime, and no stable-release, publication
or WSL2 qualification is implied.

### Residue and preservation disposition

- Aether's objective head branch was auto-deleted by the forge on merge; the remote no
  longer carries it, and its commits remain reachable through merge commit `afb59d6a`.
- The fork's head branch was **not** auto-deleted (the repository has
  `delete_branch_on_merge` disabled). Its tip `665653b471` is verifiably an ancestor of the
  merged `aether-main`, so it is objective-owned residue. The supported remote-deletion
  attempt (`git push origin --delete fix/494-project-provenance-and-bounded-failure`) was
  **refused by the Aether protected-edge guard** with
  `AETHER-SUPERVISOR-EXTERNAL-EFFECT`. That denial is authoritative and was **not** routed
  around: the remote branch therefore remains. It is inert merged residue — every commit is
  reachable from `aether-main` through merge `58f8c37a49…` — and its removal is left to an
  authorized operator or a later objective rather than to a bypass.
- Reviewer and probe scratch worktrees outside both repositories were removed after their
  evidence was captured. The two still in use at handoff are disposable checkouts under the
  system temporary root; they carry no authoritative state and are not part of either
  repository.
- Unrelated branches, worktrees, boards and concurrent objectives were preserved untouched.
- The installed Aether runtime and the loaded maintained-fork source remain at the
  pre-existing pin; nothing was activated, restarted, tagged, published or deployed.

### Release-readiness handoff

- **Remaining live adoption:** the merged fork revision `58f8c37a49…` is not the installed
  runtime pin. Adopting it requires the separately justified exact-version successor phase
  once the concurrently executing objective settles, followed by a real review canary.
- **Rollback candidate:** reverse the `HLP-428` portable patch under a hash check, or revert
  the fork merge `58f8c37a49…` on `aether-main`. No schema migration or irreversible state
  change is involved, so a rollback restores prior behavior exactly.
- **Board and session witnesses:** 89 live board databases (526 task rows, 6 active) were
  fingerprinted read-only before any writer ran. After integration the same root held 90
  databases — one board appeared from a concurrently executing objective — with the same
  526 task rows total and no file removed. No database lost rows, and the #492 board kept
  its exact 7-row status distribution. All logical deltas attribute to concurrent heartbeat
  and claim churn rather than to this objective's lanes.
- **Specific acceptance not yet qualified:** AC6's effective-runtime portion and the real
  review canary. AC1 through AC5 are qualified at exact reviewed revisions.

### Remaining risks and limits

- `_escalate_expired_flow_attentions` shares the guarded defect class but has no delivered
  test coverage and is named by no acceptance criterion. Carried forward as a bounded
  coverage caveat, not as verified behavior.
- `ruff format` drift on the two production files is pre-existing and unchanged by this
  change.
- Fork GitHub Actions remain unavailable, so no CI evidence exists for that repository.
- The pinned aggregate records `unverified: [HLP-246, HLP-247]`, which is the pre-existing
  state of those records and is unrelated to this objective.
