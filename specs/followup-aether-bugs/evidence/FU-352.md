# FU-352 — concurrent lifecycle result collection

## Disposition

**Current integrated lifecycle behavior requalified; no new product or fixture defect reproduced.**
The existing source deterministically admits both spawned contenders, collects both child
outcomes, serializes the real lifecycle mutation lock, and preserves the expected-active CAS
and transition-journal invariants across the bounded repeated runs below. This unit made no
product or test change: the already-integrated ready-event/exception-result fixture is treated
as an input to this qualification, not as new proof from this unit. Issue #352 remains open
for the terminal Supervisor disposition because no causal RED was observed.

Unit compatibility impact: `none` (evidence-only qualification; no lifecycle, journal, or
fixture correction).

## Contract and source receipt

- Objective Contract: `oc_7a35eca6393f18a7@v1`
- Contract SHA-256: `1478ba06eedd98ccea156f320c432dcc9c1ee9ad9f0149b3883b56d98b6ea6c3`
- Contract digest was rechecked from `.aether/objective-contracts/oc_7a35eca6393f18a7/v1.md`.
- Contract base ancestor: `efa698ea8f4c5b62aa79670ac57e90f12dece7ab`.
- Actual qualification revision: `ba100d1470ee79e7f7de60e53a5f4f4c8e8098e7`.
- The unrelated primary-lineage commit `da7bcef5854763bf5b70a5540f707722c158dfbb` is not
  an ancestor of the qualification revision.
- Current `tests/test_observation_lifecycle.py` SHA-256:
  `8a1fa15733cbe496d2d76a327fce35d92f0eb54e05d52ce368bb2807e854783d`.
- Current `src/aether_agents/lifecycle.py` SHA-256:
  `cbea7f1ffd3efcbb8c98904a0541314944370827a4c0187108b33053bb32ad87`.
- Changed files in this unit: this evidence file only. Lifecycle source, lifecycle tests,
  PluginContext/qualification files, journal/storage, policy, contract, fork sources and
  ledgers were preserved.

Relevant source anchors:

- `tests/test_observation_lifecycle.py:85-123` — spawn-safe contender admission, child
  exception-to-result conversion, and result reporting.
- `tests/test_observation_lifecycle.py:2690-2743` — two spawned contenders, per-child ready
  events, bounded result/join waits, child exit-code checks, one-commit/one-stale outcome,
  active-pointer and transition-journal assertions.
- `src/aether_agents/lifecycle.py:1557-1584` — thread/process mutation lock.
- `src/aether_agents/lifecycle.py:1786-1801` — expected-active CAS validation and atomic
  active-pointer write.
- `src/aether_agents/lifecycle.py:1841-1858` — existing-release activation path.
- `src/aether_agents/lifecycle.py:1860-1994` — transition journal creation, validation and
  terminal update.

## Qualification and diagnosis

The named regression was executed unchanged through the repository's exact-Hermes wrapper
with retries disabled:

- Named test: `1 passed`.
- Twenty sequential fresh wrapper processes: `20/20` passed, `0` failed.
- Sixteen concurrent fresh wrapper processes, launched in bounded batches of four: `16/16`
  passed, `0` failed.

Each named-test process exercised the actual `multiprocessing` `spawn` children, temporary
filesystem state, lifecycle mutation lock, active-pointer CAS and transition journals. The
parent waits for both per-child ready events before releasing the common interleaving gate;
it then collects exactly two queue results and asserts both child exit codes are zero. The
outcomes are exactly one `committed` and one `stale`; the final active pointer names one
candidate; journals contain one `committed` and one `failed` entry; and the failed entry has
exactly `ACTIVE_RELEASE_CAS_MISMATCH`. No sleep or timestamp ordering is used, and the
existing 10-second result/join deadlines and exact assertions are unchanged.

No causal RED occurred on the current source. Consequently there was no candidate product or
fixture repair to rerun; a retry-only or deadline/assertion change would not satisfy FU-352.
The historical admission-gate correction is recorded by the existing source and is not
claimed as work performed by this unit.

## Verification matrix

| Obligation | Check actually run | Observed result |
| --- | --- | --- |
| Recheck unchanged named boundary | `HERMES_TEST_FILE_RETRIES=0 uv run --frozen python scripts/run_tests.py -- -q --tb=short tests/test_observation_lifecycle.py::test_two_process_transitions_with_one_expected_active_have_one_commit` | `1 passed in 0.37s` |
| Bounded sequential repetition | Same exact wrapper/node in 20 fresh subprocesses, `HERMES_TEST_FILE_RETRIES=0` | `20/20` exit code 0; every subprocess reported `1 passed` |
| Bounded concurrent repetition | Same exact wrapper/node in 16 fresh subprocesses, four concurrent workers, `HERMES_TEST_FILE_RETRIES=0` | `16/16` exit code 0; every subprocess reported `1 passed` |
| Affected lifecycle module | `HERMES_TEST_FILE_RETRIES=0 uv run --frozen python scripts/run_tests.py -- -q --tb=short tests/test_observation_lifecycle.py` | `90 passed in 133.14s` |
| Touched lifecycle/test lint | `uv run --frozen ruff check tests/test_observation_lifecycle.py src/aether_agents/lifecycle.py` | Passed: `All checks passed!` |
| Touched lifecycle/test format | `uv run --frozen ruff format --check tests/test_observation_lifecycle.py src/aether_agents/lifecycle.py` | Passed: `2 files already formatted` |
| Touched lifecycle/test syntax | `uv run --frozen python -m compileall -q src/aether_agents/lifecycle.py tests/test_observation_lifecycle.py` | Passed |
| Aether source type check | `uv run --frozen mypy src/aether_agents` | Passed: no issues in 53 source files |
| Documentation gate | `uv run --frozen python scripts/check_documentation.py` | Passed |
| Patch whitespace | `git diff --check` | Passed |
| Canonical full runner | `HERMES_TEST_FILE_RETRIES=0 uv run --frozen python scripts/run_tests.py` | `1127 passed, 64 skipped, 2 failed`; both failures are unchanged unrelated baseline findings: `tests/test_public_artifacts.py::test_tracked_public_surface_contains_no_operator_paths` reports the historical `oc_0084270d940c98d9` path findings, and `tests/test_same_card_phase_predicates.py::test_initial_review_requires_an_independent_reviewer` retains the known review-lifecycle failure. |
| Full Ruff gate | `uv run --frozen ruff check src/aether_agents tests scripts` | Fails on 7 pre-existing findings in `src/aether_agents/knowledge/semantic.py`, `tests/test_knowledge_regressions.py`, and `tests/test_objective_contracts.py`; no lifecycle/test-file finding. |
| Full Ruff format gate | `uv run --frozen ruff format --check src/aether_agents tests scripts` | Reports 4 unrelated files requiring formatting: `src/aether_agents/knowledge/semantic.py`, `src/aether_agents/objective_contracts/hermes_plugin.py`, `tests/test_knowledge_regressions.py`, and `tests/test_objective_contracts.py`. |
| Public-artifact scan | `uv run --frozen python scripts/check_public_artifacts.py` | Reports only the two historical `oc_0084270d940c98d9` findings named above; no FU-352 path was implicated. |

The full-runner and whole-repository static failures are outside this unit's writable
boundary and were not altered or masked.

## Remaining risk and next boundary

The historical intermittent queue timeout was not reproduced in the current integrated
source under the named test, 20 sequential fresh-process runs, or 16 bounded concurrent
fresh-process runs. This evidence therefore does not close #352 as a product fix; it gives
Supervisor the exact retained-open boundary and confirms that the required invariants hold in
this qualification environment. If the failure recurs, the next investigation should capture
which child admission/result stage and lock/CAS boundary failed in the exact full-suite
context before considering any product or fixture change. No live profile, board, database,
provider, credential, activation, publication, push, PR, merge, or issue mutation was
performed.
