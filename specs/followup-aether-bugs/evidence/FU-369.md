# FU-369 — goal-mode review and recovery

## Unit identity

- Objective Contract: `oc_7a35eca6393f18a7@v1`, SHA-256
  `1478ba06eedd98ccea156f320c432dcc9c1ee9ad9f0149b3883b56d98b6ea6c3`.
- Aether unit base: `ba100d1470ee79e7f7de60e53a5f4f4c8e8098e7`.
- Maintained-fork base: `DarkArty07/aether-hermes@8a6b33ae480373015178b80e87c88fe0abda3919`.
- Candidate fork commit: `8ddb28c8eb747345254970339f0c1a67d05b454e`.
- Exact upstream source re-resolved before editing: `NousResearch/hermes-agent@6e07eb48387044dbcaf12490931c2b8ca7ec8653`; it retains both defects.
- No live/imported runtime, profile, board, database, service, credential, or external issue state was changed.

## Baseline RED

On unchanged maintained-fork base `8a6b33a`, the added acceptance regressions failed as required:

```text
uv run --frozen pytest -q --tb=short \
  tests/hermes_cli/test_kanban_review_surfaces.py \
  tests/tools/test_kanban_tools.py \
  tests/hermes_cli/test_kanban_session_affinity.py \
  tests/hermes_cli/test_kanban_cli.py
```

Observed: `3 failed, 93 passed` after adding the regressions. The failures were: goal-mode `request-review` was rejected by the completion judge; the exact goal-mode controller recovery signal was rejected; and the CLI capability block bypassed the goal gate.

## Bounded implementation

- `tools/kanban_tools.py` and `hermes_cli/kanban.py` now apply the auxiliary whole-objective judge only to `kanban_complete` / `complete`. `request_review` is a separate readiness transition carrying the implementer's summary/metadata to review, so it no longer requires the independent verdict it is asking the reviewer to produce.
- The existing independent-review ownership and durable reviewer provenance remain in `kanban_db.request_review`; the tests cover a different reviewer, requested changes, and re-review.
- `hermes_cli/kanban_db.py` exposes a native goal-mode block predicate. Dependency/input blocks remain allowed; the only additional route is `kind="capability", origin_signal="recovery"` for a terminal goal-mode flow controller with unresolved durable `flow_attention`. An unrelated capability/transient block remains rejected.
- The controller context names the exact recovery route and states that arbitrary capability/transient blocks are not an escape. Existing pending attention and origin-subscription behavior remains intact; the controller remains blocked/attention-pending and emits one `origin_signal=recovery` to the origin.
- `goals.py`, the Objective Contract, both ledgers, and #354 files were not changed.

## Requirement-to-evidence coverage

| Obligation | Check and observed result | Evidence |
| --- | --- | --- |
| Complete implementation can enter review without circular judge | Goal-mode tool and CLI request-review tests; candidate focused suite passed | `tests/hermes_cli/test_kanban_review_surfaces.py::test_goal_mode_review_handoff_uses_readiness_not_completion_judge` |
| Whole-objective completion gate remains | Tool and CLI completion tests continue to reject a `continue` verdict | `tests/hermes_cli/test_kanban_review_surfaces.py::test_goal_mode_completion_handoff_still_requires_judge` |
| Incomplete work remains rejected | Baseline judge-negative completion and existing block-gate regressions remain green | `tests/tools/test_kanban_tools.py`, `tests/hermes_cli/test_kanban_review_surfaces.py` |
| Reviewer independence and re-review | Existing review lifecycle plus new goal-mode changes/re-review path passed | `tests/hermes_cli/test_kanban_review_surfaces.py` |
| Exact recovery reaches origin once | Pending controller attention accepts only the exact pair, preserves blocked unit/controller attention, and emits one origin signal | `tests/hermes_cli/test_kanban_session_affinity.py::test_goal_mode_controller_accepts_exact_recovery_signal` |
| Arbitrary goal-mode block cannot escape | Tool rejects capability/transient and forged capability/recovery without pending attention; CLI rejects capability block | `tests/tools/test_kanban_tools.py`, `tests/hermes_cli/test_kanban_cli.py` |
| Portable patch/reconstruction | `git apply --check`, `git diff --check`, byte-for-byte reconstructed diff comparison | `patches/hermes/HLP-369-goal-mode-review-recovery.patch` |

## Verification recorded

- Maintained-fork focused suite: `96 passed`.
- Reconstructed exact-base focused suite: `96 passed`.
- Focused suite plus Windows-footgun checks: `105 passed`.
- Fork `ruff check` on changed source/tests: passed.
- Fork `compileall` on changed source/tests: passed.
- Fork `git diff --check`: passed.
- Aether reconciliation suite with the pending-fragment fixture: `19 passed`.
- Aether canonical focused runner for reconciliation, documentation, contract-quality, and objective-contract tests: `77 passed, 1 skipped`.
- Aether documentation check: passed.
- Aether public-artifact scan retains two unrelated historical findings in `oc_0084270d940c98d9/v1.md` (`absolute-user-home`, `operator-desktop-layout`); no FU-369 path is implicated.
- Fork `ruff format --check` reports pre-existing formatting drift in the maintained source files; no formatter was run because it would reformat unrelated hunks.
- Fork `ty check` reports pre-existing diagnostics across the maintained fork; no type-only changes were made.
- Aether canonical full runner: exit `1`, `1128 passed, 64 skipped, 445 subtests`; two failures were the known historical public-artifact path scan and the exact-upstream same-card reviewer predicate. Neither failure touches FU-369 files or behavior.
- Fork canonical full runner (`HERMES_TEST_FILE_RETRIES=0`): exit `1`; the runner reported `21 files with test failures (81 tests failed)` and `1 file where no tests ran` after the optional-provider/runtime matrix. The failure list contains no FU-369 focused file (`test_kanban_review_surfaces.py`, `test_kanban_session_affinity.py`, `test_kanban_cli.py`, or `test_kanban_tools.py`); focused candidate and reconstruction suites remain green. The reported failures are environment/optional-dependency surfaces such as Anthropic, Bedrock, Hindsight, Fal/Daytona/Modal, and Parallel Web.
- Aether implementation/reconciliation commit: `9deaa77` (`feat(reconciliation): add HLP-369 goal review recovery`).
- Maintained-fork implementation commit: `8ddb28c8eb747345254970339f0c1a67d05b454e` (`fix(kanban): separate goal review from completion`).

## Reconciliation boundary

`HLP-369.json` is a pending reconciliation fragment. The active aggregate fixture intentionally copies only IDs present in the preserved `HERMES_LOCAL_PATCHES.md`; the fragment has its own schema, patch-digest, and parser check. Terminal integration owns adding the durable HLP-369 ledger paragraph, after which it can join the active aggregate without changing the validator.

Patch SHA-256: `cc3316f5def4673822cc149a95c4edfef9eef5196bedae2216b4df9a003ea8fd`.

Unit compatibility: `patch` — the correction is backward-compatible and retains the existing completion, review ownership, recovery, and block-loop interfaces.
