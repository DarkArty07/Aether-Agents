# Implementation Evidence — FU-354

**Status:** GREEN qualification canary; no Aether product correction was demonstrated or
made. Ready for same-card Supervisor review.

**Unit:** FU-354 / `t_f324bdac`

**Objective Contract:** `oc_7a35eca6393f18a7@v1`, contract SHA-256
`1478ba06eedd98ccea156f320c432dcc9c1ee9ad9f0149b3883b56d98b6ea6c3`

**Source requirement:** AC-2 / deliverable 4 / Aether issue #354.

## Source and boundary receipt

- Aether execution started from decomposition commit
  `ba100d1470ee79e7f7de60e53a5f4f4c8e8098e7`, rooted at contract base
  `efa698ea8f4c5b62aa79670ac57e90f12dece7ab`.
- Before mutation, the assigned Aether target hashes matched the handoff:
  - `src/aether_agents/objective_contracts/store.py` —
    `7d4473547c35ff142afd613d6ed4c98c9c785c22341d56a005d4ba97cf8e9d30`
  - `src/aether_agents/objective_contracts/hermes_plugin.py` —
    `6e803f59f8b2e0fb2b74b85e51a8ec8776b6e44c18821f820e16dc1dbc53e130`
  - `tests/test_objective_contracts.py` —
    `8d38c15600ab5c44de5fe423b471da43decc4d39fb99623faed9227568386ff8`
- `src/aether_agents/objective_contracts/execution_boards.py` was preserved; its
  pre-mutation hash was `bb9c51cfe2812ae63fde5f6c1d79543d9bb23fb5d7d58b8b2855a8949dbb6bdc`.
- The maintained source was resolved read-only from
  `DarkArty07/aether-hermes@8a6b33ae480373015178b80e87c88fe0abda3919`.
  The disposable clone resolves that exact commit object independently of the mutable
  `aether-main` branch tip; the canary does not require the branch to remain at the
  recorded SHA. It materializes separate worktrees for the unchanged HLP-354 base
  `6243e40ea6b06e85061bf3fedffaad43eed51dec`, the existing HLP-354 patch reconstruction,
  and the exact maintained revision. The reconstruction's
  `hermes_cli/kanban_db.py` bytes were compared with commit
  `7d3173e1f3dba107f9a389d4e35c95f215775ee1`.
- The only Aether code change is the existing test file extended with the canary;
  this evidence artifact is the corresponding durable qualification record. No
  maintained-fork checkout, product source, Objective Contract, policy, ledger,
  runtime, profile, live board, or settings were changed.

## Canary behavior

`tests/test_objective_contracts.py:1862-2341` adds one deterministic disposable canary.
It:

1. creates an isolated Git project with a tracked Aether marker and two linked session
   worktrees;
2. advances the primary checkout with an unrelated commit;
3. concurrently drives two real Aether `hermes_plugin._handle` flows through
   `begin`, every required `set_section`, `finalize`, and `prepare_handoff`;
4. verifies each handoff's recorded `base_commit`, final artifact bytes, SHA-256, exact
   committed artifact bytes, board project identity, primary `default_workdir`, and
   `worktree_base_ref`;
5. reconstructs the existing HLP-354 patch and compares its module bytes with the
   recorded HLP commit, then runs the actual maintained `hermes_cli.kanban_db` resolver
   in isolated subprocesses once from the unchanged HLP-354 base and once from the exact
   maintained-fork revision;
6. creates an actual root task and a directly linked descendant in each disposable board,
   then resolves both task worktrees; and
7. compares primary tracked bytes/status/HEAD, pre-existing refs, and pre-existing
   worktree identities before and after materialization. Newly created refs/worktrees are
   restricted to the four expected disposable root/descendant tasks.

### Baseline RED observed

The unchanged maintained HLP-354 base (`6243e40...`) ignores the board's recorded
`worktree_base_ref` and materializes both the root and descendant at the moving primary
HEAD. The canary observes both heads equal the unrelated primary commit, both differ from
the handoff base, and the finalized contract artifact is absent from both contaminated
worktrees.

### Candidate GREEN observed

The reconstructed HLP-354 source remains a byte-level provenance check. The exact
maintained-fork revision `8a6b33ae...` is the GREEN resolver source: the subprocess
asserts both its imported module path and Git revision before creating the root and
child worktrees. Both worktrees contain byte-identical copies of the finalized contract
artifact whose SHA-256 matches the handoff. The unrelated primary commit is not an
ancestor of either task worktree. The child has the root task as its parent. Primary
tracked bytes, clean status, primary HEAD, all pre-existing refs, and all pre-existing
worktree identities remain unchanged; only the four expected disposable task refs and
worktrees are added.

## Acceptance mapping

| Obligation | Actual check | Observed result | Evidence |
| --- | --- | --- | --- |
| Real/disposable concurrent flows | Two barrier-synchronized Aether plugin flows author/finalize separate linked session worktrees | PASS | `tests/test_objective_contracts.py:2097-2176` |
| Handoff does not depend on moving primary | Primary is advanced before authoring; each plugin handoff reads its session worktree and records that worktree HEAD | PASS | `tests/test_objective_contracts.py:2074-2085,2178-2192` |
| Exact contract artifact and digest | `git show <base>:<relative_path>` equals final bytes; SHA-256 equals handoff digest | PASS | `tests/test_objective_contracts.py:2178-2188` |
| Root and descendant lineage | Exact maintained resolver creates a root and a directly linked child; both are resolved from the same board | PASS on exact maintained source; RED on unchanged base; HLP reconstruction separately byte-matched | `tests/test_objective_contracts.py:2200-2261,2291-2304` |
| Unrelated primary lineage absent | `git merge-base --is-ancestor` is false for primary HEAD → each exact-maintained task HEAD | PASS on exact maintained source; baseline heads equal primary | `tests/test_objective_contracts.py:2188,2311-2312` |
| Primary/ref/worktree preservation | Tracked bytes/status/HEAD and existing ref/worktree identities are compared; new entries are allowlisted to expected tasks | PASS | `tests/test_objective_contracts.py:2194-2198,2322-2341` |
| Maintained behavior is real, not mocked | Child subprocess imports `hermes_cli.kanban_db` from the exact maintained disposable source and asserts both module path and Git revision | PASS | `tests/test_objective_contracts.py:2200-2288` |
| Scope and exclusion | No product/fork edit; #275, live runtime, credentials, settings, raw DB mutation and publication untouched | PASS by diff/status inspection | This evidence and local Git state |

## Verification record

All commands were run from the assigned Aether worktree.

| Command | Result |
| --- | --- |
| `env -u AETHER_MAINTAINED_HERMES_CHECKOUT uv run --frozen python scripts/run_tests.py -- -q tests/test_objective_contracts.py -k reconstructed_maintained_resolver --tb=short` | **1 passed, 39 deselected** (37.20s); public remote source clone and exact maintained-revision resolver exercised |
| `AETHER_MAINTAINED_HERMES_CHECKOUT=<disposable checkout with a later aether-main tip> uv run --frozen python scripts/run_tests.py -- -q tests/test_objective_contracts.py -k reconstructed_maintained_resolver --tb=short` | **1 passed, 39 deselected** (17.01s); exact pinned commit resolved and exercised despite the later mutable branch tip |
| `AETHER_MAINTAINED_HERMES_CHECKOUT=<disposable checkout with a later aether-main tip> uv run --frozen python scripts/run_tests.py -- -q tests/test_objective_contracts.py tests/test_project_init.py tests/test_hermes_patch_reconciliation.py --tb=short` | **89 passed** (19.88s) |
| `uv run --frozen python -m compileall -q src tests` | **PASS** |
| `uv run --frozen mypy src/aether_agents` | **PASS** — no issues in 53 source files |
| `uv run --frozen python scripts/check_documentation.py` | **PASS** |
| `git diff --check` | **PASS** |
| `uv run --frozen python scripts/run_tests.py` | **1128 passed, 64 skipped, 2 failed** in 244.53s. Failures are pre-existing and reproduced independently: the public-artifact scan reports historical `oc_0084270d940c98d9` operator-path findings, and `test_same_card_phase_predicates.py::test_initial_review_requires_an_independent_reviewer` fails because current baseline returns `True` instead of `False`. Neither failure touches FU-354 files or behavior. |
| `uv run --frozen ruff check tests/test_objective_contracts.py` | **4 pre-existing findings** (two import-order and two unused-import findings in the earlier session-worktree/execution-board tests); the clean-base file produces the same four findings, and no finding is reported in the added canary. |
| `uv run --frozen ruff format --check tests/test_objective_contracts.py` | **Pre-existing formatting failure** in earlier session-worktree tests (`test_session_worktree_authoring...`); the added canary is formatted and introduces no additional formatter finding. |
| `uv run --frozen python scripts/check_public_artifacts.py` | **Known baseline failure** for historical `.aether/objective-contracts/oc_0084270d940c98d9/v1.md` (`absolute-user-home`, `operator-desktop-layout`); no FU-354 artifact is implicated. |
| Focused isolated runs of the two full-suite failures | Each failed independently with the same baseline result; no FU-354 failure was observed. |

## Compatibility and remaining risk

- **Unit compatibility impact:** `none` — tests/evidence-only qualification; no product
  behavior or public interface was changed in Aether.
- The canary qualifies the current Aether handoff together with the exact maintained-fork
  resolver revision in disposable reconstructions; the HLP-354 patch remains a separate
  byte-level provenance check. It does not itself close GitHub #354 or make an aggregate
  release decision; Supervisor owns review, integration, issue disposition, and release
  conclusions.
- External source availability is a test prerequisite when the optional local checkout
  hint is absent. The run above succeeded both with the pinned public clone and with the
  disposable exact local checkout.
- No live provider, profile activation, service, board monitor, credential, remote branch,
  PR, issue, release, package, or deployment effect occurred.
