# Unit Evidence: ABR-315 — Owner-Authorized Tracked SOUL Source Edit

- **Unit:** ABR-315
- **Card ID:** `t_174fa815`
- **Issue:** [#315](https://github.com/DarkArty07/Aether-Agents/issues/315)
- **Objective Contract:** `oc_ddebf175a40251f7@v1` (SHA-256 `722100569399ac7b61e15bccdcba28fd5fafe1585e4a1f7fbbe3bf5dcf0aa4e8`)
- **Review Lane:** Same-card Supervisor review (`kanban_request_review`, `reviewer="supervisor"`)
- **Unit Compatibility Impact:** `patch`

---

## 1. Repositories and Source Identity

| Tree | Revision / Identity | Evidence |
| --- | --- | --- |
| Aether starting tree | `b52afd17a93d25e31a48e5c86bd569f45f2f04d4` + breakdown `5834212cfe8d6cb96325ab9d97671a637eef71c0` | Worktree `t_174fa815` on branch `aether-agents-2/t_174fa815-abr-315-owner-authorized-tracked-soul-so` |
| Maintained fork baseline | `DarkArty07/aether-hermes` `aether-main` `28b593efa86bbc674b32f488c35932a4e7e85a51` | Verified before mutation; isolated nested worktree on branch `wt/t_174fa815` |
| Maintained fork candidate | local unit commit `3bc559b6d29f5f5cb99f34c60e5163de38ae91ad` | Local commit only; no push, PR, merge, or remote mutation |

Pre-mutation target-file hashes at fork baseline `28b593e`:
- `tools/file_tools.py`: `b6053e05a826551cb6de8c40f712aecd9e518122fde6aef6ecdc4090a707c198`
- `tests/tools/test_file_write_safety.py`: `469b652fa7032e03f54dda96635c3d51223b1facfe66f4e7c31dd31acc433826`

---

## 2. Scope and Root Cause

### Root Cause
In upstream Hermes, `_PROTECTED_INSTRUCTION_BASENAMES` unconditionally matched `soul.md` across all directory trees, while `_protected_instruction_reason` simultaneously exempted the active `real_home` (`~/.hermes`) where live profile `SOUL.md` files actually reside.

This caused two complementary defects:
1. **False Deny / Timeout on Tracked Source:** An ordinary, reversible edit or patch to a tracked repository or package source file (such as `src/aether_agents/resources/profiles/supervisor/SOUL.md` or any worktree file named `SOUL.md`) was falsely categorized as a protected agent-instruction write. When executed in automated, headless, or TUI sessions, this resulted in unanswerable approval timeouts: `BLOCKED: write to protected agent-instruction file(s) (SOUL.md) approval prompt timed out without a user response. Silence is not consent.`
2. **False Bypass on Live Profile SOUL:** A write targeting the active profile's actual instruction file (`<HERMES_HOME>/SOUL.md`) bypassed `_protected_instruction_reason` via `resolved.startswith(real_home + os.sep)`, completely unmonitored by the protected-instruction gate.

### Bounded Repair
1. Defined `_is_installed_profile_soul(resolved, normalized)` to check whether a path corresponds to an installed Hermes profile's `SOUL.md` (`<HERMES_HOME>/SOUL.md`, `<HERMES_ROOT>/SOUL.md`, or `<HERMES_ROOT>/profiles/<name>/SOUL.md`).
2. Gated installed profile `SOUL.md` writes before the `real_home` exemption, ensuring live profile identity files always require explicit human approval and fail closed when unapproved.
3. Removed `soul.md` from `_PROTECTED_INSTRUCTION_BASENAMES`. Project-local instruction files (`AGENTS.md`, `CLAUDE.md`, `.cursorrules`, and project-local `.hermes/` config directories) remain protected and require approval across all directories. Ordinary tracked package source or project files named `SOUL.md` proceed without false prompts or timeouts.

Preserved without edits: Aether policy hooks, ledgers, live `home/` profiles, credential guards, and repository operating guidance.

---

## 3. Regression-First / Fail-First Verification (RED)

New focused tests in `tests/tools/test_soul_instruction_gating.py` were run against the unchanged fork baseline (`28b593efa8`):

**Command:**
```bash
HERMES_TEST_FILE_RETRIES=0 scripts/run_tests.sh tests/tools/test_soul_instruction_gating.py
```

**Observed Result on Unchanged Baseline:**
```text
tests/tools/test_soul_instruction_gating.py FFFF..                       [100%]

=================================== FAILURES ===================================
FAILED test_tracked_package_soul_write_proceeds_without_approval
    AssertionError: {'error': 'BLOCKED: write to protected agent-instruction file(s) (SOUL.md) was denied by the user...'}
FAILED test_tracked_package_soul_patch_proceeds_without_approval
    AssertionError: {'error': 'BLOCKED: write to protected agent-instruction file(s) (SOUL.md) was denied by the user...'}
FAILED test_live_active_profile_soul_write_requires_approval_and_deny_blocks
    AssertionError: assert (None) where None = {'bytes_written': 16, 'dirs_created': True, 'verified': True}.get('error')
FAILED test_live_active_profile_soul_no_human_fails_closed
    AssertionError: assert (None) where None = {'bytes_written': 16, 'dirs_created': True, 'verified': True}.get('error')
PASSED test_live_named_profile_soul_requires_approval
PASSED test_project_agents_md_still_requires_approval

Summary: 4 failed, 2 passed in 0.77s
```

The baseline failed on both sides of the contract:
- Tracked package `SOUL.md` writes and patches falsely triggered approval and were blocked.
- Live active profile `SOUL.md` writes bypassed approval and were written unmonitored.

---

## 4. Candidate Verification (GREEN)

Following the candidate fix in fork commit `3bc559b6d29f5f5cb99f34c60e5163de38ae91ad`:

**Focused Soul Instruction Suite:**
```bash
HERMES_TEST_FILE_RETRIES=0 scripts/run_tests.sh tests/tools/test_soul_instruction_gating.py
```
**Observed Result:**
```text
tests/tools/test_soul_instruction_gating.py (6 passed, 2.2s)
Summary: 1 files, 6 tests passed, 0 failed in 2.2s
```

**Full File Write Safety & Gating Suite:**
```bash
HERMES_TEST_FILE_RETRIES=0 scripts/run_tests.sh tests/tools/test_file_write_safety.py tests/tools/test_soul_instruction_gating.py tests/tools/test_write_approval.py tests/tools/test_write_deny.py
```
**Observed Result:**
```text
tests/tools/test_write_deny.py (10 passed, 1.3s)
tests/tools/test_write_approval.py (15 passed, 1.4s)
tests/tools/test_soul_instruction_gating.py (6 passed, 2.3s)
tests/tools/test_file_write_safety.py (58 passed, 4.2s)
Summary: 4 files, 89 tests passed, 0 failed in 4.2s
```

**Linters & Diff Check:**
```bash
python -m ruff check tools/file_tools.py tests/tools/test_file_write_safety.py tests/tools/test_soul_instruction_gating.py
python -m compileall -q tools/file_tools.py tests/tools/test_file_write_safety.py tests/tools/test_soul_instruction_gating.py
git diff --check
```
**Observed Result:** Clean exit 0 across all checks (`All checks passed!`).

**Aether Worktree Integration Probe:**
```bash
PYTHONPATH=<candidate-fork-worktree> uv run --frozen python -c '
import tools.file_tools as ft
tracked = "src/aether_agents/resources/profiles/supervisor/SOUL.md"
assert ft._protected_instruction_reason(tracked, enabled=True, extra_patterns=[]) is None
live = "home/profiles/implementer/SOUL.md"
assert ft._protected_instruction_reason(live, enabled=True, extra_patterns=[]) == "SOUL.md"
agents = "AGENTS.md"
assert ft._protected_instruction_reason(agents, enabled=True, extra_patterns=[]) == "AGENTS.md"
'
```
**Observed Result:** Clean exit 0; candidate fork imports resolve as expected.

---

## 5. Acceptance Coverage Matrix

| Obligation | Check and Observed Result | Evidence Location |
| --- | --- | --- |
| Tracked package SOUL write proceeds without approval | `test_tracked_package_soul_write_proceeds_without_approval` passes; `approvals["calls"] == []`; file updated | `tests/tools/test_soul_instruction_gating.py:50-63` |
| Tracked package SOUL patch proceeds without approval | `test_tracked_package_soul_patch_proceeds_without_approval` passes; replacement applied | `tests/tools/test_soul_instruction_gating.py:66-83` |
| Live active profile SOUL write requires approval | `test_live_active_profile_soul_write_requires_approval_and_deny_blocks` blocks with `BLOCKED: write to protected agent-instruction file(s) (SOUL.md)` | `tests/tools/test_soul_instruction_gating.py:86-104` |
| Live active profile SOUL without human fails closed | `test_live_active_profile_soul_no_human_fails_closed` fails closed; file unwritten | `tests/tools/test_soul_instruction_gating.py:107-120` |
| Live named profile SOUL requires approval | `test_live_named_profile_soul_requires_approval` blocks with `BLOCKED` on deny | `tests/tools/test_soul_instruction_gating.py:123-138` |
| Project-local AGENTS.md remains protected | `test_project_agents_md_still_requires_approval` blocks with `BLOCKED` | `tests/tools/test_soul_instruction_gating.py:141-147` |
| Preservation / boundary rules | No edits to ledgers, canonical contracts, live `home/` profiles, credentials, or public API | Clean git diff and status across both trees |

---

## 6. Downstream Ledger Paragraph (not applied by this unit)

The fork product changed, so the terminal integration owner should add the following paragraph to the maintained-fork/Aether ledgers without editing it in this unit:

```markdown
### HLP-315 / #315 — distinguish tracked package SOUL from installed profile SOUL

- **Issue:** [#315](https://github.com/DarkArty07/Aether-Agents/issues/315)
- **Commit:** `3bc559b6d29f5f5cb99f34c60e5163de38ae91ad`
- **Evidence:** `specs/autonomous-bug-remediation/evidence/ABR-315.md`
- **Scope:** `tools/file_tools.py` protected agent-instruction gate and focused file write tests.
- **Behavior:** Distinguishes installed Hermes profile `SOUL.md` files (`HERMES_HOME/SOUL.md`, `HERMES_ROOT/profiles/<name>/SOUL.md`, `HERMES_ROOT/SOUL.md`) from ordinary tracked package sources or project files. Tracked source edits proceed without false approval prompts or unanswerable timeouts; installed profile instruction files and project-local `AGENTS.md` / `CLAUDE.md` / `.cursorrules` remain protected and fail closed when unapproved.
- **Rollback:** Revert fork commit `3bc559b6d29f5f5cb99f34c60e5163de38ae91ad`.
- **Retirement gate:** Retires when the upstream Hermes release distinguishes installed profile `SOUL.md` from ordinary project/package source files and the ABR-315 regression suite passes without the downstream patch.
```

---

## 7. Limits and Remaining Risk

- Inherited fork Actions were not triggered; verification is based on local canonical test runners with file retries disabled (`HERMES_TEST_FILE_RETRIES=0`).
- No modifications were made to live installed profiles under `home/` or live runtime state.
- Issue #315 remains open pending integrated PR closeout by Supervisor (ABR-INT).
- No push, PR, merge, publication, or deployment was executed.
