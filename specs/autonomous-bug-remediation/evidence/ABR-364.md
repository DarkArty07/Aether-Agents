# Unit Evidence: ABR-364 — Objective Contract Finalization Path Grammar

- **Unit:** ABR-364
- **Card ID:** `t_e4dac1b0`
- **Issue:** [#364](https://github.com/DarkArty07/Aether-Agents/issues/364)
- **Objective Contract:** `oc_ddebf175a40251f7@v1` (SHA-256 `722100569399ac7b61e15bccdcba28fd5fafe1585e4a1f7fbbe3bf5dcf0aa4e8`)
- **Aether Base:** `b52afd17a93d25e31a48e5c86bd569f45f2f04d4` + supervisor breakdown `5834212cfe8d6cb96325ab9d97671a637eef71c0`
- **Branch:** `aether-agents-2/t_e4dac1b0-abr-364-contract-finalization-rejects-op`
- **Review Model:** Same-card Supervisor review (`kanban_request_review`, `reviewer="supervisor"`)
- **Unit Compatibility Impact:** `patch`

---

## 1. Scope and Outcome

Objective Contract `validate` and `finalize` reject operator-local paths (`absolute-user-home`, `windows-user-home`, `operator-desktop-layout`) using the bounded public-artifact policy grammar before immutable finalization. Valid portable relative paths and http(s) references remain accepted. Historical finalized contract `oc_0084270d940c98d9@v1` is not rewritten, deleted, or exempted.

Exclusive writable boundaries respected:
- `src/aether_agents/objective_contracts/store.py`
- `scripts/check_public_artifacts.py`
- `tests/test_objective_contracts.py`
- `tests/test_public_artifacts.py`
- `specs/autonomous-bug-remediation/evidence/ABR-364.md`

No new `src/` helper file was added, avoiding modification to `.github/workflows/policy.yml` and leaving that hotspot intact for ABR-323.

---

## 2. Regression-First / Fail-First Verification (RED)

New tests `test_validate_and_finalize_reject_operator_local_paths` and `test_validate_and_finalize_accept_portable_paths_and_urls` were overlaid on the unmodified baseline:

**Command:**
```bash
uv run --frozen python scripts/run_tests.py -- -q --tb=short tests/test_objective_contracts.py -k "test_validate_and_finalize"
```

**Observed Result on Unchanged Baseline:**
```
F.                                                                       [100%]
=================================== FAILURES ===================================
____________ test_validate_and_finalize_reject_operator_local_paths ____________
tests/test_objective_contracts.py:258: in test_validate_and_finalize_reject_operator_local_paths
    with pytest.raises(ContractError, match="AETHER-OBJECTIVE-CONTRACT-OPERATOR-PATH"):
E   Failed: DID NOT RAISE ContractError
=========================== short test summary info ============================
FAILED tests/test_objective_contracts.py::test_validate_and_finalize_reject_operator_local_paths
1 failed, 1 passed, 37 deselected in 0.40s
```

On unchanged baseline, `store.validate` and `store.finalize` did not detect or reject operator-local paths, allowing non-portable machine paths to be validated and finalized.

---

## 3. Candidate Verification (GREEN)

Following implementation of `find_operator_path_violations` in `src/aether_agents/objective_contracts/store.py`, integration into `_draft_validation`, and grammar reuse in `scripts/check_public_artifacts.py`:

**Command:**
```bash
uv run --frozen python scripts/run_tests.py -- -q --tb=short tests/test_objective_contracts.py -k "test_validate_and_finalize"
```

**Observed Result:**
```
..                                                                       [100%]
2 passed, 37 deselected in 0.24s
```

**Full Objective Contracts Test Suite:**
```bash
uv run --frozen python scripts/run_tests.py -- -q --tb=short tests/test_objective_contracts.py
```
**Observed Result:**
```
.......................................                                  [100%]
39 passed in 4.40s
```

**Public Artifacts Tests:**
```bash
uv run --frozen python scripts/run_tests.py -- -q --tb=short tests/test_public_artifacts.py -k "not test_tracked_public_surface_contains_no_operator_paths"
```
**Observed Result:**
```
.....                                                                    [100%]
5 passed, 1 deselected in 0.15s
```
*(Note: `test_tracked_public_surface_contains_no_operator_paths` fails solely on historical contract `oc_0084270d940c98d9@v1`, which is explicitly documented in the breakdown and contract as the only known allowed scanner hit and must not be edited or exempted.)*

**Wheel & Sdist Scanner Qualification:**
```bash
uv build && uv run --frozen python scripts/check_public_artifacts.py --root <empty-git-dir> --artifact dist/aether_agents-0.24.0.tar.gz --artifact dist/aether_agents-0.24.0-py3-none-any.whl
```
**Observed Result:**
```
public artifact path scan passed: tracked surface + 2 artifact(s)
exit: 0
```

**Linters & Diff Check:**
```bash
git diff --check
uv run --frozen ruff check src/aether_agents/objective_contracts/store.py scripts/check_public_artifacts.py tests/test_public_artifacts.py tests/test_objective_contracts.py
uv run --frozen ruff format --check src/aether_agents/objective_contracts/store.py scripts/check_public_artifacts.py tests/test_public_artifacts.py
```
**Observed Result:** Clean exit 0 across all checks.

---

## 4. Acceptance Obligations Coverage Matrix

| Obligation | Check | Observed Result | Evidence Location |
|---|---|---|---|
| Reject Unix home path before finalize | `test_validate_and_finalize_reject_operator_local_paths` case 1 | `ContractError("AETHER-OBJECTIVE-CONTRACT-OPERATOR-PATH")` raised on `validate` & `finalize` | `tests/test_objective_contracts.py:243-267` |
| Reject Windows user home path | `test_validate_and_finalize_reject_operator_local_paths` case 2 | `ContractError("AETHER-OBJECTIVE-CONTRACT-OPERATOR-PATH")` raised on `validate` & `finalize` | `tests/test_objective_contracts.py:269-289` |
| Reject desktop layout path | `test_validate_and_finalize_reject_operator_local_paths` case 3 | `ContractError("AETHER-OBJECTIVE-CONTRACT-OPERATOR-PATH")` raised on `validate` & `finalize` | `tests/test_objective_contracts.py:291-311` |
| Accept portable relative paths and URLs | `test_validate_and_finalize_accept_portable_paths_and_urls` | Draft validates (`valid: True`) and finalizes with status `final` | `tests/test_objective_contracts.py:314-342` |
| Reuse grammar without second scanner | `scripts/check_public_artifacts.py` imports `find_operator_path_violations` | Scanner flags identical three kinds (`absolute-user-home`, `windows-user-home`, `operator-desktop-layout`) | `scripts/check_public_artifacts.py:20-29` |
| Preserve historical contract `oc_0084270d940c98d9@v1` | Inspect git diff | Untouched; not rewritten, deleted, or exempted | Git working tree clean for contract files |
| Check on draft validate and finalize | `_draft_validation` called by `store.validate` and `store.finalize` | Checked before write/immutability; `_validate_final` unmodified | `src/aether_agents/objective_contracts/store.py:624-640` |
