# Implementation Evidence — Unit ABR-298

**Status:** Demonstrated pre-tool credential guard denial of placeholder-only documentation reproduced on unchanged baseline (RED); candidate allows staging auth and placeholder-only documentation on write_file/patch while failing closed on actual credential-shaped fixtures (GREEN). PD-71 edge safety, durable-secret protection, remote/destructive mutation guards preserved, and round-1 supervisor review feedback resolved (traversal rejection, mixed-file patch fail-closed, exact tokenized placeholder matching, clean diff check). Ready for same-card Supervisor review.

- **Unit ID:** `ABR-298`
- **Task ID:** `t_f30fd5f9`
- **Objective Contract:** `oc_ddebf175a40251f7@v1` (SHA-256 `722100569399ac7b61e15bccdcba28fd5fafe1585e4a1f7fbbe3bf5dcf0aa4e8`)
- **Source Requirement:** Issue [#298](https://github.com/DarkArty07/Aether-Agents/issues/298), Supervisor breakdown unit ABR-298 in `specs/autonomous-bug-remediation/tasks.md`

## 1. Repositories and Revisions

- **Repository:** Aether (`aether-agents`)
- **Base Commit:** `b52afd17a93d25e31a48e5c86bd569f45f2f04d4`
- **Worktree Branch:** `aether-agents-2/t_f30fd5f9-abr-298-placeholder-only-documentation-g`
- **Worktree ID:** `t_f30fd5f9`

## 2. Scope and File Boundaries

Exclusive writable boundary:
- `policy/hooks/aether_pre_tool_policy.py`: extended placeholder tokens, updated placeholder recognition to exact/tokenized matching against `PLACEHOLDER_KEYWORDS` (rejecting incidental substrings such as `<monkey>`), added comprehensive multi-target extraction (`_target_paths`), path traversal rejection (`_is_doc_path`), and fail-closed evaluation where all targets must be doc paths; line count kept at 319 lines (strictly less than 320 lines).
- `tests/test_policy_hooks.py`: added comprehensive regression test suite verifying that staging auth placeholders on `write_file` and `patch` of documentation paths are allowed across all three roles (`morfeo`, `supervisor`, `implementer`), while actual credential-shaped fixtures, path traversal attempts, mixed-file patches, spoofed path shapes, and unknown delimited values with incidental keyword substrings fail closed with `CREDENTIAL`.
- `specs/autonomous-bug-remediation/evidence/ABR-298.md`: this unit evidence artifact.

Preserved without edits:
- Objective Contract `.aether/objective-contracts/oc_ddebf175a40251f7/v1.md` (untouched).
- Live installed hooks under `home/` (untouched).
- Maintenance ledgers (`HERMES_LOCAL_PATCHES.md`, fork `AETHER_FORK.md`: untouched).
- PD-71 edge families, credential operation patterns, remote-mutation and destructive operation patterns.

## 3. Findings and Technical Judgement

1. **Root Cause Analysis of Pre-Tool Credential Denial (Baseline RED):**
   - On baseline `b52afd1`, `_contains_secret(args, broad=False)` runs on non-durable tool calls (`write_file`, `patch`).
   - The authorization pattern in `HIGH_CONFIDENCE_SECRET_PATTERNS` was `(?i)\b(?:authorization|proxy-authorization)\s*[:=]\s*(?:bearer|basic)\s+\S+`, which matched any non-whitespace token after the bearer or basic scheme.
   - The sanitization logic only replaced `[REDACTED]`, `<REDACTED>`, `[redacted]`, and `<redacted>` with empty strings. Even though `PLACEHOLDERS` defined values such as `test`, `example`, `dummy`, `changeme`, and `not-set`, those values were only checked when `broad=True` on dictionary keys in durable tools.
   - Consequently, local writes or patches to documentation files (such as `docs/v1.0.0/STAGING_RUNBOOK.md`) containing placeholder staging auth (for example, header lines with `<staging-token>`, `placeholder`, `test`, or `example`) were blocked with `AETHER-IMPLEMENTER-CREDENTIAL: credential material may not be supplied through a tool call`.

2. **Causal Remediation and Technical Judgement:**
   - **Expanded Staging Auth Placeholders:** Updated `PLACEHOLDERS` to explicitly include staging-runbook placeholders consistent with existing convention: `<placeholder>`, `[placeholder]`, `placeholder`, `<token>`, `[token]`, `<staging-token>`, `<staging_token>`, `<auth-token>`, `<api-key>`.
   - **Narrowed Exact/Tokenized Matcher:** In `_is_placeholder(value)`, replaced substring searching with exact membership in `PLACEHOLDERS` or tokenized boundary matching against `PLACEHOLDER_KEYWORDS` (`token`, `placeholder`, `secret`, `key`, `password`, `auth`). Delimited strings with incidental substrings (such as `<monkey>` or `<author>`) do not match keywords as discrete tokens and fail closed.
   - **Bounded Multi-Target Path Extraction and Traversal Rejection:** In `_target_paths`, parsed all `Update File`, `Add File`, and `Delete File` targets from multi-file patches alongside `path`/`file_path` parameters. In `_is_doc_path`, rejected path traversal components (`..`) and normalized paths before checking for documentation directories (`docs/`, `specs/`) or extensions (`.md`, `.markdown`, `.rst`, `.txt`).
   - **Fail-Closed Mixed Payload Evaluation:** All targets must be verified documentation paths (`all(_is_doc_path(t) for t in targets)`). If any target touches code, tests, or traverses outside documentation, `is_doc` is False and the payload fails closed.
   - **No Broad Exemption / Strict PD-71 Preservation:** High-confidence intrinsic credential patterns (private key blocks, OpenAI keys, GitHub tokens, Google API keys, AWS keys, Slack tokens, Stripe tokens, JWT tokens) are never exempted, even in documentation. Code and test fixtures (`is_doc=False`) do not receive documentation placeholder allowances.
   - **Micro-Authorization Constraint:** Kept `aether_pre_tool_policy.py` at 319 lines, satisfying the `<= 319` (`< 320`) line constraint enforced by `test_policy_has_no_micro_authorization_dependencies`.

## 4. Verification and Evidence Matrix

### 4.1 Baseline Test Run (RED)
Executed prior to candidate policy hook changes with regression tests added on baseline `b52afd1`:
```
python3 -m unittest discover -s tests -p 'test_policy_hooks.py' -q
```
- **Exit Code:** 1
- **Result:** FAILED (18 failures in `test_staging_auth_placeholders_allowed_in_docs_while_credentials_blocked` across `morfeo`, `supervisor`, and `implementer` roles, failing with `AssertionError: 2 != 0` due to `AETHER-{ROLE}-CREDENTIAL` denial).

```
uv run --frozen python scripts/run_tests.py -- -q --tb=short tests/test_policy_hooks.py
```
- **Exit Code:** 1
- **Result:** 18 failed, 21 passed, 368 subtests passed.

### 4.2 Candidate Test Run (GREEN)
Executed on candidate tree:
```
python3 -m unittest discover -s tests -p 'test_policy_hooks.py' -q
```
- **Exit Code:** 0
- **Result:** Ran 21 tests in 14.613s OK.

```
uv run --frozen python scripts/run_tests.py -- -q --tb=short tests/test_policy_hooks.py
```
- **Exit Code:** 0
- **Result:** 21 passed, 425 subtests passed in 13.62s.

### 4.3 Linters and Formatting
```
uv run --frozen ruff check policy/hooks/aether_pre_tool_policy.py tests/test_policy_hooks.py
uv run --frozen ruff format --check policy/hooks/aether_pre_tool_policy.py tests/test_policy_hooks.py
git diff --check
```
- **Exit Code:** 0 on all checks; clean diff without whitespace or format errors across `git diff --check` and `git diff --check b52afd1`.

```
uv run --frozen python scripts/check_public_artifacts.py --root .
```
- **Result:** Touched files (`policy/hooks/aether_pre_tool_policy.py`, `tests/test_policy_hooks.py`, `specs/autonomous-bug-remediation/evidence/ABR-298.md`) have zero operator-local paths or public artifact violations. (Sole failure is the preserved historical artifact `oc_0084270d940c98d9/v1.md`).

## 5. Compatibility Impact

- **Unit Compatibility:** `patch` (backward-compatible bug fix allowing placeholder-only documentation through the pre-tool credential guard without weakening high-confidence credential protection).
- **Issue Disposition:** Issue [#298](https://github.com/DarkArty07/Aether-Agents/issues/298) addressed; issue left open per contract rules; no push/PR/merge/close.
