# KG19-02 — E01 Isolated Real-Morfeo Qualification with Controls

**Status:** Implementer unit evidence for Objective Contract `oc_d96969bf913512f7@v1` (Issue #505).
Executed; reported honestly including material findings against the E01 oracle.

**Task:** `t_58496a81`
**Branch:** `aether-agents-2/t_58496a81-kg19-02-e01-isolated-real-morfeo-first-t`
**Base commit:** `03797e9f8f4f0c68b8daa256d52d9e0a56800526`
**Reviewed prerequisite:** KG19-01 (`0b462384c0d0c924cd57bd31ed10e1be4e0b4c69`) merged as a merge commit (`--no-ff`); its files were not rewritten.

## 1. Candidate Resource Verification

Before any agent turn or qualification call, the candidate bytes delivered by KG19-01 were hash-verified inside this worktree:

| Resource | Expected SHA-256 | Observed SHA-256 | Status |
| --- | --- | --- | --- |
| `src/aether_agents/resources/profiles/morfeo/SOUL.md` | `c4785b1a38e29e49d908268cd2abfedf5d587e63386ef1ba1f00326c999065ee` | `c4785b1a38e29e49d908268cd2abfedf5d587e63386ef1ba1f00326c999065ee` | MATCH |
| `src/aether_agents/resources/skills/project-knowledge/SKILL.md` | `9a417c75235eb2fa0388b3d6ba95ca19f268d894fabe2fcabfaa0023c394b869` | `9a417c75235eb2fa0388b3d6ba95ca19f268d894fabe2fcabfaa0023c394b869` | MATCH |

## 2. Fixture Configuration and Pre-Model State

- **Fixture Project ID:** `e01a0001-0000-4000-8000-000000000001`
- **Head Revision:** `c77796c5a309af5ee41b4c29e0ba4672b834bd3f`
- **Subsystems:**
  1. `billing/` (`billing/processor.py`): implements `process_payment` with a legacy retry policy (fixed 10-second delay, 5 maximum attempts, no gateway call).
  2. `notifications/` (`notifications/mailer.py`): implements `send_payment_receipt`.
- **Documented Decision:**
  `docs/decisions/001-retry-policy.md` (ADR 001) specifies exponential backoff at 2.0x (delays 60s, 120s, 240s) up to 3 retry attempts, and records that migration from the legacy code is deferred to a future cycle.
- **Structural Index Status (observed BEFORE any model call):**
  - `status_ok`: `true`
  - `available`: `true`
  - `source_revision`: `c77796c5a309af5ee41b4c29e0ba4672b834bd3f`
  - `coverage`: `{"code": "structural", "documents": "structural_only"}`
  - `freshness`: `current`
  - `indexed_files`: 7
- Index built with the provisioned component interpreter (`aether knowledge configure --python <component interpreter>`; no install) through `project_knowledge` action `update` in `structural` mode. No semantic extraction was run.

## 3. Positive E01 Execution

- **Prompt (does not name Graphify, graph, or the knowledge tool):**
  `Explain how payment retries work in this project, and what the retry policy is.`
- **Session ID:** `20260923_210140_edb6a3` (Suite 2)

### 3.1 What the agent actually did, in order

| # | Message | Tool call | Arguments (sanitized) |
| --- | --- | --- | --- |
| 2 | assistant | `kanban_show` | `{}` (empty; no task id inherited) |
| 4 | assistant | `skill_view` | `{"name":"project-knowledge"}` — loads the packaged skill |
| 4 | assistant | `terminal` | `git status --short --branch; git rev-parse --show-toplevel` |
| 4 | assistant | `search_files` | `AGENTS.md` search |
| 4 | assistant | `search_files` | `*retry*` filename search |
| 4 | assistant | `search_files` | content search `payment\|Payment\|retry\|Retry` |
| 10 | assistant | `tool_describe` | `{"name":"project_knowledge"}` |
| 10 | assistant | `read_file` | `billing/processor.py` |
| 10 | assistant | `read_file` | `docs/decisions/001-retry-policy.md` |
| 10 | assistant | `search_files` | project file listing |
| 16 | assistant | `tool_call` | `project_knowledge` `{"action":"query","question":"Where is payment retry behavior implemented and what documented retry policy governs it?","budget_tokens":2000}` |
| 16 | assistant | `read_file` | `README.md` |
| 16 | assistant | `read_file` | `notifications/mailer.py` |
| 17 | tool | `project_knowledge` | returned the query result (7 references across ADR and code) |
| 21 | assistant | — | final answer (text) |

### 3.2 References returned by `project_knowledge.query`

All seven references are bound to the fixture head revision (`c77796c5a309af5ee41b4c29e0ba4672b834bd3f`), covering both the documented specification and the code implementation:

- `docs/decisions/001-retry-policy.md:L1`
- `docs/decisions/001-retry-policy.md:L10`
- `billing/processor.py:L1`
- `docs/decisions/001-retry-policy.md:L6`
- `docs/decisions/001-retry-policy.md:L16`
- `docs/decisions/001-retry-policy.md:L3`
- `billing/processor.py:L4`

The references returned by the knowledge component supply both the decision document (ADR 001) and the code module (`billing/processor.py`), capturing the specification and implementation halves of the distinction the answer draws.

### 3.3 Observed answer

> Payment retries are **specified but not actually executed** in the checked-in code. `process_payment` sets a legacy 10-second fixed delay and a limit of 5 retries, then immediately returns `True` (`billing/processor.py:4–14`). The **accepted policy** is different: retry after 60, 120, and 240 seconds—exponential backoff at 2×—with at most 3 retry attempts (`docs/decisions/001-retry-policy.md:3–14`).

The answer distinguishes the documented specification (ADR 001) from implemented behavior (legacy code), citing both sources and the deferred migration note. This property is derived dynamically by the lane oracle `check_distinguishes_specification_from_implementation`, which evaluates the answer content rather than asserting a constant boolean.

### 3.4 Material finding against the E01 oracle (honest report)

KG-19/AC-2 and `validation.md` E01 require the graph consultation to happen **before** broad source exploration, and the answer must be read honestly against that criterion.

Observed order in the positive session:

1. The agent discovered and loaded `project-knowledge` early (message 4), inspected its tool schema (message 10), and called `project_knowledge.query` in the same assistant turn as additional source reads (message 16).
2. **Before** that first `project_knowledge.query`, the agent had already: run `git status`/`rev-parse`; searched for `AGENTS.md` and `*retry*`; run a content search for `payment\|Payment\|retry\|Retry`; and **read both `billing/processor.py` and `docs/decisions/001-retry-policy.md`** (message 10).

That pre-graph sequence is exactly the architecture-and-decision exploration E01 expects the graph to *orient before*, so on a strict reading of KG-19/AC-2 this specific run is **partial, not a PASS**: the trigger caused early skill discovery/loading and a knowledge consultation, but the knowledge call did not precede the substantive source exploration that answered the question.

### 3.5 Attributed provider usage (Suite 2 Positive Session)

- Model: `gpt-6-sol`
- API calls: 5
- Input tokens: 67,890
- Output tokens: 1,336
- Cache read tokens: 83,456
- Reasoning tokens: 502

## 4. Paid Trials and Spend Disclosure

Three complete test suites were executed under temporary qualification roots (`/tmp/aether-e01-qualification-{bnqk8kqn,uljbnyd5,ak4t978p}`), totaling **9 agent sessions and 53 primary-route calls** (24 + 13 + 16) plus 9 title-generation calls:

| Suite | Timestamp | Fixture HEAD | Sessions | Primary Calls | Title Calls | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 1 (`bnqk8kqn`) | 20:52 | `a1298390...` | 3 | 24 | 3 | Invalid trial: non-repo cwd in terminal causing operator home directory entries to appear in live transcript. Verified: zero operator paths reached durable artifacts. Excluded as environment defect. |
| 2 (`uljbnyd5`) | 21:01 | `c77796c5...` | 3 | 13 | 3 | Valid trial: reported above. Key sources read at message 10 before graph query at message 16 (partial on orient-before-explore order). |
| 3 (`ak4t978p`) | 21:05 | `f63563f8...` | 3 | 16 | 3 | Valid trial: substantive source exploration at message 12 (content search returned operative values from both ADR 001 and `billing/processor.py`) preceded `project_knowledge.query` request at message 13 (result at message 14). Substantive exploration preceded graph consultation in both valid trials. |

### 4.1 AC-2 Outcome Across Valid Trials

Across the two valid trials (Suite 2 and Suite 3):
- **Suite 2** exhibited substantive source file reading (`read_file` of both `billing/processor.py` and `docs/decisions/001-retry-policy.md` at message 10) before calling `project_knowledge` at message 16.
- **Suite 3** exhibited substantive source exploration in message 12 via a content `search_files` whose payload returned the operative retry specification from ADR 001 (60s backoff, 3 attempts, notice of legacy retries) and implementation values from `billing/processor.py` (fixed 10s delay, 5 attempts), before requesting `project_knowledge.query` at message 13 (with result returned at message 14). While the gap was narrower than Suite 2's, substantive exploration preceded the graph consultation in both.
- Both valid trials discovered and loaded `project-knowledge` early, queried the graph, received all 7 specification and code references, and produced answers that distinguished the ADR specification from the legacy implementation.

The honest AC-2 outcome is therefore that **both valid trials consulted the graph only after substantive source exploration; the orient-before-explore order is not demonstrated in either**. Under the candidate trigger wording, the agent first conducts local discovery and search (which surfaces operative source content) before committing to the graph query. This finding directly informs (without pre-deciding) the supervisor and design-steward trigger question.

## 5. Control A: Exact Source Provided

- **Prompt:** `In billing/processor.py, what is the default retry delay in seconds and the maximum attempts?`
- **Session ID:** `20260923_210233_326a48` (Suite 2)
- **Tools called:** `kanban_show`, `read_file` (twice). `project_knowledge` was **not** called.
- **Ceremonial graph consultation:** `false`
- **Observed answer:**
  > The retry delay is 10 seconds, and the limit is 5 retry attempts (`billing/processor.py:11–12`).
- **Attributed provider usage:**
  - Model: `gpt-6-sol`
  - API calls: 3
  - Input tokens: 986
  - Output tokens: 295
  - Cache read tokens: 79,360
  - Reasoning tokens: 222

## 6. Control B: Unborn Repository

- **Prompt:** `In this repository, what is the architecture and implementation?`
- **Session ID:** `20260923_210256_48697c` (Suite 2)
- **Observed answer:**
  > This repository has no architecture or implementation to describe yet. It is an unborn Git repository on `main`: there are no commits or tracked source files. The only files present are `.aether/project.toml` and `untracked_notes.txt`. I also tried the project knowledge graph, but it was unavailable because the project registry and marker disagree.

### 6.1 What the live turn actually demonstrated — and what it did not

The live control did **not** cleanly isolate the unborn-HEAD failure path: its repository carried an untracked `.aether/project.toml` while the disposable registry recorded a different root state, so the observed code was `PROJECT_UNRESOLVED` (registry and marker disagree), not the bound-no-HEAD error.

A separate no-spend deterministic witness isolates the unborn-HEAD path precisely (see `../fixtures/e01_morfeo_orientation_lane.py::test_unborn_repository_honest_degradation`):

1. an unborn repository is created with `git init` and no commit;
2. its marker is registered, and the registry and marker **agree** (`verify_with_marker` is `True`), so identity is not the cause;
3. `resolve_context` on that repository fails with the exact code `SCOPE_UNAVAILABLE` — `The bound Git repository is unavailable`, raised from `git rev-parse --verify HEAD^{commit}` in `src/aether_agents/knowledge/common.py:113-114`;
4. the test also asserts no index cache exists for that project and that `git log` in the unborn repository fails, i.e. no commit, rebuild, or index was created.

So the honest no-HEAD degradation conclusion is: with identity valid and HEAD absent, the knowledge component refuses with `SCOPE_UNAVAILABLE` instead of inventing a project; the live turn separately demonstrated the agent stating the limit in ordinary language (`no commits or tracked source files`) and continuing with file tools. Zero installs, commits, rebuilds, or synthetic project inventions were observed in any run.

### 6.2 Attributed provider usage

- Model: `gpt-6-sol`
- API calls: 5
- Input tokens: 33,891
- Output tokens: 1,069
- Cache read tokens: 114,944
- Reasoning tokens: 388

## 7. Isolation and Preservation

The lane reused the reviewed primitives `aether_agents.lab.isolation.isolated_hermes_env` (scrubs inherited `HERMES_KANBAN_*`/`HERMES_SESSION_*` and task/run/project/tenant/cwd identity; pins disposable home, board, worktrees and XDG roots) and `aether_agents.lab.runner.prepare_profiles` (copies candidate config + tracked SOUL into isolated profiles), then added only the lane's own disposable steps: candidate skill materialization into the disposable profile skills root and explicit test-only plugin enablement with disposable `state_root`/`cache_root` settings.

- **Live Morfeo profile:** SOUL, `config.yaml` and packaged skill bytes recorded before and after; identical.
- **Live board:** The active kanban database at `HERMES_KANBAN_DB` (`<state_root>/hermes/kanban/boards/oc-12027989a08f41cda82c54ff1bfb6b03-d96969bf913512f7-v1/kanban.db`) carries the 4 tasks for this objective (`t_fbcc54ab` done, `t_7c3c1e49` done, `t_58496a81` running, `t_225efc01` todo) and was not modified or written to by the qualification runs. (An earlier witness reporting "0 tasks" reflected inspecting the unpinned default board `~/.hermes/kanban.db` rather than the active objective board).
- **Primary checkout pre-existing local modifications:** untouched; the primary checkout still carries exactly its pre-existing dirty entries and no worktree branch was reset while this lane ran.
- **Evidence limit on run roots:** The three execution directories `/tmp/aether-e01-qualification-{bnqk8kqn,uljbnyd5,ak4t978p}` are preserved on disk for verification, but are subject to operating system `/tmp` volatility across reboots.

## 8. Artifacts and Field Provenance

- Machine-readable evidence: [E01-evidence.json](E01-evidence.json)
- Reproducible qualification lane and deterministic tests: [../fixtures/e01_morfeo_orientation_lane.py](../fixtures/e01_morfeo_orientation_lane.py)

Deterministic lane parts (no spend): `uv run --frozen python scripts/run_tests.py -- specs/005-project-knowledge-graphify/fixtures/e01_morfeo_orientation_lane.py` (7 passed).

### 8.1 Evidence Field Provenance (Lane-Derived vs. Hand-Authored Interpretation)

To enable the reader to distinguish automated lane outputs from narrative or evaluative interpretation:

- **Lane-derived fields (extracted directly from raw session DBs / tool outputs by lane automation):**
  - `tools_sequence`: list of tool names called in chronological order across the session.
  - `references_returned`: exact list of path/location/revision dictionaries parsed from the `project_knowledge` tool response payload.
  - `sources_read` / `sources_inspected`: list of file paths read via `read_file` or inspected in the session.
  - `usage`: model name, API call counts, input/output/cache/reasoning token counts extracted from Hermes session metadata.
  - `answer`: verbatim final assistant response text from the session.
  - `distinguishes_specification_from_implementation`: dynamically derived by the lane oracle `check_distinguishes_specification_from_implementation` evaluating the answer content.
- **Hand-authored interpretation fields (evaluative analysis applied to raw data against contract criteria):**
  - `ordered_trace`: annotated trace with human-readable detail strings mapping message context.
  - `oracle_verdict`: judgment against the KG-19/AC-2 criterion (`partial`, not PASS).
  - `oracle_verdict_reason`: narrative rationale explaining why the observed order failed strict AC-2 satisfaction.
  - Ordering flags (`graph_query_before_source_exploration`, `key_sources_read_before_graph_query`, etc.): human-evaluated classifications based on inspection of message payloads.
  - `ac2_conclusion`: synthesised evaluation of cross-trial evidence against the contract requirement.

## 9. Unit Compatibility

- `unit_compatibility`: `patch`
- `rationale`: Portable qualification lane, fixture and evidence under `specs/005-project-knowledge-graphify/`; no runtime source, dependency, tool-schema or public-interface change, and no opt-in change (the plugin is enabled only inside the disposable test profile).
