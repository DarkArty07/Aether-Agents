# #435 hardline quoted-grep parser — Supervisor execution breakdown

**Status:** verified decomposition for Objective Contract `oc_13551b9a7a1eaf42@v1`.
**Source:** `.aether/objective-contracts/oc_13551b9a7a1eaf42/v1.md`, SHA-256 `1fefa317a11745af8968862ccd7bdc0e484de4268398ea3d2fa62d63f13a7e5f`, on Aether base `ac66870729a8734fb27c947829fa2bc8aa1b23ac`; technical design `specs/issue-435-hardline-parser/plan.md` at the same base. This execution breakdown does not amend the design or contract.

## Receipt and cross-artifact decisions

- Project UUID in `.aether/project.toml`, the task envelope and contract all equal `12027989-a08f-41cd-a82c-54ff1bfb6b03`; root HEAD equals the contract base and the contract bytes equal the envelope digest. `AGENTS.md`, `DESIGN.md`, the plan and the contract bound authority. `project_knowledge(status)` reports `available=false` at this revision, so source was inspected directly.
- Remote `DarkArty07/aether-hermes` `aether-main` readback is `938bc34fbc797402ccc61d352192e122a344d052`; an **isolated clone**, not the shared/stale `aether-main` checkout or installed Hermes, resolves that commit and `tools/approval.py` blob `ac8b34e44235db9421de0737e6fbabe1f13ffd89`. The isolated clone has Python 3.11.15, `uv sync --frozen --extra dev` and pytest 9.1.1; it is the only fork writer surface for this objective.
- The two sanitized shell strings were recovered from the durable private log as bytes, replacing only private `cd` prefixes with `/tmp/safe-repo` in memory. At the exact fork base both are short, `shlex.split`-valid and raw grep-parse-balanced, but `_normalize_command_for_detection` makes the grep parser report malformed and `detect_hardline_command` returns `_MALFORMED_EXEC_DESCRIPTION`. The design examples' Markdown escaping is **not** a test fixture. Workers must assert literal/escape bytes in committed tests without importing a private log or running the strings.
- Quote/malformed judgement uses the original syntax-preserving command. Normalized/deobfuscated variants must continue to participate in **every** protected-operation match, grep masking and the full guard chain; syntax validity of normalized-only text is not proof of malformed original input. No broad grep exemption, approval-mode change, limit increase or policy reduction. Local parser/helper/test arrangement remains Implementer judgement inside these invariants. Existing malformed quoted grep is hardline; program-bearing malformed-option tests currently assert dangerous classification. This objective preserves existing decisions, not a generic approval-parser redesign; return any material AC2 guarantee conflict to Morfeo before accepting a weaker substitute.
- `HLP-435` is unused in Aether's ledger, patch directory and reconciliation entries. This phase proposes `patches/hermes/HLP-435-hardline-quoted-grep.patch` and a repository-relative evidence record, not an installed entry. It must not claim `ACTIVE_LOCAL`, `MAINTAINED_FORK_ONLY`, merged fork source, or release qualification. The patch must include all changed fork paths, including tests, so `git apply --index` on the exact base reconstructs the reviewed candidate's Git tree byte for byte and reverse-checks cleanly.
- The active #494/RC16 board has reviewed source/ledger and identity units plus a running isolated-transition unit, with integration/cutover pending. RC16's unmerged HLP unit owns `HERMES_LOCAL_PATCHES.md`, the aggregate, fragments and reconciliation tests; its identity unit owns `.github/workflows/policy.yml`. These are **not** independently writable here. This phase records proposed ledger/upstream-retirement text in a new #435 evidence document and leaves current ledger, fragments, aggregate, tests roster, `.gitattributes`, workflow manifest, release-lock and version unchanged. The existing manifest is already three entries behind the tracked non-`specs/` paths (the #435 contract and two #494 contracts); it is an RC16/integration collision, not permission for this source-only unit to modify that workflow. An Aether evidence PR is optional and must not be opened with a known failing manifest; no fork or Aether merge during RC16.
- At the exact fork base, the focused `test_hardline_blocklist.py`, `test_shell_bypass_denylist.py` and `test_approval.py` modules completed; `test_execution_flag_detection.py` has one failing real-binary witness (`executed` missing) under the isolated runner. This is baseline-shared until a candidate comparison proves otherwise, not GREEN. Fork Actions currently cannot be inferred from source tests: if absent, report NOT RUN. No model/browser/live-board/gateway canary is part of this source phase.
- Implementer profile and Supervisor profile exist; no new capacity, role or board. Source-only compatibility hypothesis is `release_impact=patch`, `release_action=defer`, `release_channel=none`, to be reevaluated from reviewed evidence.

## Requirement coverage and graph

| Source obligation | Owner | Observable evidence |
| --- | --- | --- |
| AC1, D1 | HF-435, reviewed by Supervisor | Two exact sanitized shell-byte REDs at the pinned base and GREEN at candidate; balanced/within-limit; neither malformed nor hardline; full guard not unconditionally hardline (ordinary approval allowed) |
| AC2, D1–D2 | HF-435, reviewed by Supervisor; INT-435 rechecks | Genuine malformed quoted grep/executable controls, protected ordinary/quoted/obfuscated/separator/substitution/wrapper cases and yolo hardline floor remain; inert quoted data, bounds and dangerous detector unchanged; no test command executed |
| AC3, D1–D2 | HF-435; INT-435 | Exact fork ancestry/blob, local diff and runner/static receipts; independent same-card review, optional open fork PR, Actions labeled by observed status |
| AC4, D3 | AE-435, reviewed by Supervisor; INT-435 rechecks | HLP-435 patch digest, exact-base apply and reverse, reconstructed tree equal to reviewed fork tree, truthful **proposed** ledger/retirement/upstream assessment; no active or merged claim |
| AC5, D4, authority/preservation | All units; INT-435 terminal | Isolated worktrees and board; untouched fork `aether-main`, Aether `main`, selected pin/runtime, #494 worktrees/board; no merge/tag/deploy/activation/issue closure; release conclusions and residual adoption gates |

```text
Supervisor root decomposition
  -> HF-435 fork parser/security correction and tests (Implementer)
     -> same-card independent Supervisor review
     -> AE-435 portable patch and evidence-only ledger proposal (Implementer)
        -> same-card independent Supervisor review
  -> INT-435 terminal source-phase verification/disposition (Supervisor)
     (depends on root and both independently reviewed units)
```

There is no honest independent implementation parallelism: the fork fix and its tests share `tools/approval.py` as one parser boundary, while the portable patch is *byte-derived from the reviewed fork candidate*. Dividing them would duplicate a shared writable file or fabricate the patch input. RC16 work on the ledger/manifest is an external collision, not a dependency edge that this board can satisfy by mutation. Keep the two implementations serialized and the RC16-owned files preserved. Terminal integration is not a substitute for unit review.

## HF-435 — exact-base fork correction

- **Source/outcome:** AC1–AC3, D1–D2 and contract Testing/Stop Conditions. Prove both original shell-byte REDs at the exact base, then GREEN for syntax/hardline/full guard at the candidate. Preserve malformed and protected controls and `detect_dangerous_command`; no shell string in tests is executed. Excludes Aether artifacts, PRs and runtime activation.
- **Inputs:** isolated fork clone at pinned base/blob with Python 3.11 dev runner; this committed breakdown, contract, plan and existing `tests/tools/` controls. Never read or modify a concurrent fork checkout.
- **Writable:** fork `tools/approval.py` and focused `tests/tools/test_hardline_blocklist.py`, `tests/tools/test_execution_flag_detection.py`, `tests/tools/test_shell_bypass_denylist.py`, `tests/tools/test_approval.py` or one new focused module in `tests/tools/`. Preserve all other fork paths including `AETHER_FORK.md`, deps, lockfiles and workflows. If fixing this requires a wider guard boundary or alters current protection, stop and return exact evidence.
- **Verification:** test literal bytes and baseline RED before code; `HERMES_TEST_FILE_RETRIES=0 scripts/run_tests.sh tests/tools/test_hardline_blocklist.py tests/tools/test_execution_flag_detection.py tests/tools/test_shell_bypass_denylist.py tests/tools/test_approval.py -q` plus any touched neighboring module, no retries/skip-as-fix. Run focused hardline/yolo controls and full guard with synthetic strings under disposable HOME/HERMES_HOME, *not* the worker's profile cache. Run compile/static checks for changed paths, `git diff --check`, and label baseline-shared or unavailable tests separately. Do not change approval mode or run real protected operations to test them.
- **Dependency/completion:** decomposition root only; one local fork branch/commit off exact base with commit/tree identities, focused RED/GREEN and positive-control matrix, tests/skips/failures, compatibility impact, remaining risk and a proposed fork audit paragraph; request same-card Supervisor review. No push, PR, merge or Aether edits.

## AE-435 — portable artifact and truthful proposed disposition

- **Source/outcome:** AC3 provenance, AC4, D3. Make `HLP-435` portable from the independently reviewed fork tree, with SHA-256, exact-base apply/reverse and tree equality, upstream comparison/retirement gate, and explicit source-only state. No installed/merged claim.
- **Inputs:** reviewed HF-435 commit/tree and its handoff, verified fork base/blob, Aether artifact/patch conventions. Recheck fork remote and the RC16 board before any remote effect.
- **Writable:** new `patches/hermes/HLP-435-hardline-quoted-grep.patch` and new `specs/issue-435-hardline-parser/evidence/HLP-435.md` only; an isolated disposable clone for reconstruction. Preserve the Objective Contract, `AGENTS.md`, `HERMES_LOCAL_PATCHES.md`, aggregate/preflight, fragments, test roster, `.gitattributes`, CI manifest and all #494 files. A proposed ledger paragraph and retirement conditions live *inside the evidence document* until the collision gate clears in a successor; do not create a partial active ledger entry or silently bypass reconciliation.
- **Verification:** SHA-256; `git apply --check` / `git apply --index` and `git apply -R --check` in a clean exact-base scratch clone; `git write-tree` equals the reviewed fork tree, including tests, with no private path/secret. Run focused Aether public-artifact and reconciliation tests, documentation/static checks relevant to new files and `git diff --check`; do not label the unchanged aggregate/check as qualification of HLP-435. Run the full Aether bootstrap before a PR if any ledger/manifest is later touched (not authorized for this unit). If existing CI policy forbids the candidate patch absent a shared manifest edit, leave PR unopened and state the gate; do not weaken policy.
- **Dependency/completion:** only after HF-435 same-card review. Local Aether commit(s) with patch/evidence, hash/tree/reverse and upstream readback, compatibility impact and exact residual gates; request same-card Supervisor review. No push, PR, merge or issue mutation.

## INT-435 — terminal source-only verification and closeout

- **Source/outcome:** all AC1–AC5, D1–D4. Independently review both implementation units through same-card lanes; consume their accepted exact commits, recheck the security/parser delta at final fork tree and the patch reconstruction, Aether evidence/proposed ledger truth, current remote bases and #494 collision state. Integrate the reviewed Aether candidate onto the root worktree without rewriting history. An open fork PR is permitted after rechecking remote SHA; do not merge it. Open an Aether PR only if it can pass required checks without touching RC16-owned shared files; otherwise leave the reviewed Aether branch local with reason and preserve it for noncolliding integration.
- **Boundaries:** can correct bounded integration defects in agreed behavior, with separate attribution/verification; substantial changes route through rework. No fork `aether-main` or Aether `main` merge, no installed/runtime/pin/version/approval-mode/gateway/profile change, no tag, package, deployment, protected remote mutation or issue closure. Preserve #494 and unrelated branches/worktrees. No canonical contract edit.
- **Verification:** fork exact revision, reviewed diff, RED/GREEN controls, full guard/yolo, malformed and protected matrix, minimum affected runner, static checks and `git diff --check`; Aether patch apply/reverse/tree and public-artifact, documentation, reconciliation-focused checks. Re-run `scripts/validate_hermes_patch_reconciliation.py --check --fork-checkout <clean exact revision>` only if complete candidate inputs are present; do not equate unchanged aggregate status with HLP-435. Aether full `uv run --frozen python scripts/run_tests.py` and Ruff if a PR/ledger boundary is touched. Inspect real PR/check state (fork Actions absent = NOT RUN), Issue #435 OPEN, Git/worktrees/pin and root `AGENTS.md` coherence. Record omissions with concrete non-applicability reason.
- **Dependency/completion:** root plus both independently reviewed units; same Supervisor flow, terminal. Report separate `release_impact=patch|...`, `release_action=defer|...`, `release_channel=none|...` based on evidence; provisional `patch/defer/none` is not a release. State why #435 stays OPEN and what future noncolliding integration/adoption decision remains. Terminal closeout is the source-phase result only, not full Issue acceptance.

## Stop/authority reminders

Stop on exact fork base/blob mismatch, unreproduced original RED, weakened protected positive, unavailable required review/check, non-reconcilable #494 source/ledger collision or any request for live adoption/merged pin. Preserve candidate/evidence and return the material conflict to Morfeo; don't create a second contract, restart a return budget, bypass hardline or turn a local source-only phase into a release. Independent review and real observed check outcomes, not worker prose, decide promotion.
