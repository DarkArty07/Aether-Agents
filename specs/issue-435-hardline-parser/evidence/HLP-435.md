# HLP-435 evidence — evaluate grep quote structure on raw syntax vs hardline malformed-payload (source-phase evidence)

**Unit:** AE-435 (implementer), task `t_afc4bc76`.
**Parent unit:** HF-435 (implementer), task `t_587b0d54`, independently reviewed and approved by Supervisor.
**Downstream unit:** INT-435 (supervisor), task `t_0a90a1b5`.
**Authority:** Objective Contract `oc_13551b9a7a1eaf42@v1` (SHA-256 `1fefa317a11745af8968862ccd7bdc0e484de4268398ea3d2fa62d63f13a7e5f`) on Aether base `ac66870729a8734fb27c947829fa2bc8aa1b23ac`, Supervisor task breakdown `36fe18a1e56ef6307e4fe84285017ab027e422e1:specs/issue-435-hardline-parser/tasks.md` and technical design `specs/issue-435-hardline-parser/plan.md`.

---

## 1. Producer receipt and artifact identity

| Check / Artifact | Observed value |
| --- | --- |
| Maintained fork remote URL | `https://github.com/DarkArty07/aether-hermes` |
| Fork base commit | `938bc34fbc797402ccc61d352192e122a344d052` |
| Fork base tree | `ae106dfbf45e6615b261674b7cb0dc9252ce14f1` |
| Fork `tools/approval.py` base blob | `ac8b34e44235db9421de0737e6fbabe1f13ffd89` |
| Fork candidate commit | `f459cfa0ad27705e2b27ce91c5658648372f307c` |
| Fork candidate tree | `cac4313200eb7a3359cb435622da17060494af8a` |
| Fork branch | `fix/435-hardline-quoted-grep` (isolated clone at candidate tip) |
| Portable patch path | `patches/hermes/HLP-435-hardline-quoted-grep.patch` |
| Patch byte size | 8,666 bytes |
| Patch SHA-256 | `1836729e2b76ecc390e4467d17fb65ba67a5239ebb34492402b7629a7ac3a563` |
| Patch lines | 193 lines |
| Reconstructed tree | `cac4313200eb7a3359cb435622da17060494af8a` (exact byte-for-byte match) |
| Tree reconstruction proof | `git apply --check` exit 0 and `git apply --index` on clean base `938bc34fbc797402ccc61d352192e122a344d052` produces tree `cac4313200eb7a3359cb435622da17060494af8a` |
| Tree restoration (rollback) proof | `git apply -R --check` exit 0 and `git apply -R --index` restores tree `ae106dfbf45e6615b261674b7cb0dc9252ce14f1` |
| Changed files | 2 files, `+164/-3`: `tools/approval.py` (+5/-3), `tests/tools/test_hardline_blocklist.py` (+159/-0) |
| Portability & privacy | Repository-relative paths only; synthetic `/tmp/safe-repo` paths; zero operator machine paths, private logs, usernames, credentials, or runtime state |
| Candidate status | SOURCE CANDIDATE (not `ACTIVE_LOCAL`, not `MAINTAINED_FORK_ONLY`, not installed, not merged, not a release) |

---

## 2. Parent unit (HF-435) handoff summary

Parent unit `HF-435` (`t_587b0d54`) implemented the fork-side fix and was independently reviewed and approved by Supervisor:
- **Baseline RED reproduced at untouched base `938bc34fbc797402ccc61d352192e122a344d052` without shell execution:**
  - Command A (138 bytes): `cd /tmp/safe-repo && grep -n "for edge in" -A 3 harness.py | head -20; echo "=== check other spot ==="; grep -n "edges\", \[\]" harness.py`. Raw syntax quotes balanced (`_quoted_grep_pattern_spans` malformed is `False`), but normalization stripped backslashes creating an unbalanced quote in the normalized variant, causing `detect_hardline_command` to return `(True, _MALFORMED_EXEC_DESCRIPTION)`.
  - Command B (95 bytes): `cd /tmp/safe-repo && grep -n "PATTERN\|regex\|r\"" scripts/check_public_artifacts.py | head -30`. Raw syntax quotes balanced (`_quoted_grep_pattern_spans` malformed is `False`), but normalized variant had quote imbalance from stripped backslashes, causing `detect_hardline_command` to return `(True, _MALFORMED_EXEC_DESCRIPTION)`.
- **Candidate GREEN verified at reviewed tip `f459cfa0ad27705e2b27ce91c5658648372f307c`:**
  - Both Command A and Command B return `is_hl is False`, `is_dang is False`, and `check_all_command_guards` passes (`approved: True`, `hardline: False`).
  - Command strings are evaluated with isolated `HOME` and `HERMES_HOME` to prevent cache writes.
- **AC2 protection preservation verified:**
  - Genuinely unclosed quotes (`grep "unclosed harness.py`, `grep 'unclosed harness.py`, etc.) and missing arguments (`grep -A`, `grep -B`, `grep -C`, `grep --context`, `grep --after-context`) fail closed with `_MALFORMED_EXEC_DESCRIPTION`.
  - Obfuscated unclosed grep commands (`gr\ep "unclosed harness.py`, `gr''ep 'unclosed harness.py`) fail closed.
  - Hardline malformed grep remains blocked even under session YOLO mode (`test_issue_435_genuine_malformed_grep_hardline_in_yolo`).
  - Inert single-quoted PCRE patterns in grep (`grep -P 'rm -rf --no-preserve-root /' audit.log`) are masked and do not trigger hardline.
  - Executable constructs (double quotes, command substitutions like `grep -P "$(rm -rf /)" audit.log`, separators like `; rm -rf /`) are never masked as inert and trigger hardline block.
- **Suite results:** 409 passed, 1 failed (pre-existing baseline-shared `test_execution_flag_detection.py:255` real-binary execution flag witness failure, diagnosed on base), 5 skipped.
- **Mutation coverage:** 3 mutation pins verified (m1: 2 failed; m2: 1 failed; m3: 3 failed).

---

## 3. Producer verification and checks

| Command / Gate | Result | Evidence |
| --- | --- | --- |
| Binary patch generation | 8,666 bytes, SHA-256 `1836729e2b76ecc390e4467d17fb65ba67a5239ebb34492402b7629a7ac3a563` | `git -C <fork-435> diff --binary 938bc34fbc..f459cfa0ad > patches/hermes/HLP-435-hardline-quoted-grep.patch` |
| `git apply --check` at exact base | Exit 0 | Clean disposable clone at `938bc34fbc797402ccc61d352192e122a344d052` |
| `git apply --index` at exact base | Exit 0 | Applied to index in disposable clone |
| Reconstructed tree check | Exit 0, exact tree `cac4313200eb7a3359cb435622da17060494af8a` | `git write-tree` matches `f459cfa0ad^{tree}` byte for byte |
| `git apply -R --check` rollback | Exit 0 | Clean reverse-check in disposable clone |
| `git apply -R --index` rollback | Exit 0 | Clean reverse-apply to index |
| Restored tree check | Exit 0, exact tree `ae106dfbf45e6615b261674b7cb0dc9252ce14f1` | `git write-tree` matches base tree `938bc34fbc^{tree}` byte for byte |
| `git diff --check` | Exit 0 | Clean, no whitespace or formatting errors |
| `uv run python scripts/check_documentation.py` | Exit 0 | `documentation validation passed` |
| `uv run python scripts/check_public_artifacts.py --root .` | Exit 0 | `public artifact path scan passed: tracked surface + 0 artifact(s)` |
| `uv run pytest tests/test_contract_quality_documents.py` | Exit 0 | 14 passed, 1 skipped (15 collected; skip is `test_native_loader_reads_exact_documents_in_disposable_home` due to uninstalled `hermes_cli`) |
| `uv run pytest tests/test_hermes_patch_reconciliation.py` | Exit 0 | 30 passed |
| `uv run pytest tests/test_public_artifacts.py` | 8 passed, 1 failed | 8 passed, 1 failed (`test_canonical_base_manifest_matches_tracked_non_specs_files` fails due to 4 missing non-specs manifest entries: 3 pre-existing from base `ac668707` [`.aether/objective-contracts/oc_13551b9a7a1eaf42/v1.md`, `.aether/objective-contracts/oc_c770cea3db51d97e/v1.md`, `.aether/objective-contracts/oc_c770cea3db51d97e/v2.md`] plus this candidate's own addition `patches/hermes/HLP-435-hardline-quoted-grep.patch`; `patches/hermes/*` also sets `release=true` in `.github/workflows/policy.yml:95`, and `policy.yml:162` compares against `git ls-files`; Aether-PR opening stays gated on RC16-owned manifest/allow-list reconciliation without editing shared policy/ledger/fragments from this unit) |
| `scripts/validate_hermes_patch_reconciliation.py --check --fork-checkout` | NOT APPLICABLE | Candidate inputs intentionally not created due to RC16 collision; reported honestly as NOT APPLICABLE with reason, not false PASS |

---

## 4. Upstream comparison and retirement gate

- **Historical reference baseline (`NousResearch/hermes-agent@e624e9fde561e1add9388384012b295fde669ade`, `v2026.8.18`):**
  - Inspected `tools/approval.py:601-613`: contains the identical normalize-then-grep pattern where `_normalize_command_for_detection(command)` strips backslashes before quote spans are evaluated. Both valid historical byte shapes fail with `_MALFORMED_EXEC_DESCRIPTION` on baseline upstream code as well.
- **Inspected upstream `main` (`NousResearch/hermes-agent@d0288be5b3330d2442e3907185b8e9d0958297bb`):**
  - Upstream `main` refactored and no longer exposes these hardline methods in `tools/approval.py`. Absence of the code is not evidence of an equivalent safe fix or permission to remove the maintained-fork guard.
- **Retirement Gate:**
  Retire HLP-435 only when an adopted exact upstream Hermes release:
  1. Evaluates quote structure on syntax-preserving command representations rather than stripped deobfuscation outputs;
  2. Does not classify valid quoted/escaped grep or other inspections as malformed executable payloads;
  3. Retains fail-closed protection for genuine unclosed quotes, executable command substitutions, separators, and destructive payloads across ordinary, quoted, obfuscated, and YOLO contexts; and
  4. Passes `tests/tools/test_hardline_blocklist.py` without this patch.

---

## 5. Proposed ledger entry (for downstream integration after RC16 collision clears)

*Note: Shared surfaces (`HERMES_LOCAL_PATCHES.md`, `.github/workflows/policy.yml`, patch reconciliation fragments and aggregate) are owned by unmerged RC16 units. The entries below are proposed text to be adopted by Supervisor / integration when the collision boundary clears.*

### Proposed summary table row for `HERMES_LOCAL_PATCHES.md`

```markdown
| `HLP-435` | `#435` | evaluate grep quote structure on raw syntax to avoid false malformed hardline on valid escaped quotes | upstream v2026.8.18 contains identical normalize-then-grep flaw; upstream main removed hardline methods without safe equivalent | `PROPOSED_SOURCE_CANDIDATE` |
```

### Proposed detailed section for `HERMES_LOCAL_PATCHES.md`

```markdown
## HLP-435 — evaluate grep quote structure on raw syntax vs hardline malformed-payload (proposed candidate, 2026-09-25)

- **Reason (tracked defect):** Aether issue `#435`. Two short, valid read-only grep commands with escaped quotes were unconditionally blocked as hardline `_MALFORMED_EXEC_DESCRIPTION`. The defect stemmed from evaluating quote balance on `_normalize_command_for_detection(command)`: normalization strips backslash escapes (`\"` -> `"`), creating an unbalanced quote in the normalized text that does not exist in the raw shell command.
- **Root cause and minimal semantics:** `detect_hardline_command` in `tools/approval.py` evaluated `_grep_safe_detection_variant` on `normalized` rather than raw syntax. In the candidate fix, `detect_hardline_command` checks malformed grep syntax on `raw_syntax = _mask_quoted_newlines(command)`, preserving original quote escaping. Genuinely malformed grep syntax (unclosed quotes, missing option operands) fails closed. Normalized variants continue to participate in all threat detection variants and the full guard chain.
- **Primary active files (maintained fork `DarkArty07/aether-hermes`, branch `fix/435-hardline-quoted-grep`):** `tools/approval.py` (+5/-3 lines) and `tests/tools/test_hardline_blocklist.py` (+159/-0 lines).
- **Integration identity:** Candidate fork commit `f459cfa0ad27705e2b27ce91c5658648372f307c` (tree `cac4313200eb7a3359cb435622da17060494af8a`) off exact base `938bc34fbc797402ccc61d352192e122a344d052` (tree `ae106dfbf45e6615b261674b7cb0dc9252ce14f1`).
- **Source and review evidence:** Independently reviewed and approved by Supervisor in task `t_587b0d54`. RED reproduced at base on both 138-byte and 95-byte shell byte shapes without execution; GREEN verified on candidate; AC2 security controls, YOLO fail-closed, and mutation tests verified.
- **Portable artifact:** `patches/hermes/HLP-435-hardline-quoted-grep.patch`, SHA-256 `1836729e2b76ecc390e4467d17fb65ba67a5239ebb34492402b7629a7ac3a563`. Applies cleanly (`git apply --check` exit 0) to exact base `938bc34fbc797402ccc61d352192e122a344d052` and reconstructs candidate tree `cac4313200eb7a3359cb435622da17060494af8a` byte for byte.
- **Upstream:** Inspected `NousResearch/hermes-agent@e624e9fde561e1add9388384012b295fde669ade` (`v2026.8.18`) and upstream `main@d0288be5b3330d2442e3907185b8e9d0958297bb`. Baseline has identical defect; upstream main removed hardline methods without an equivalent safe fix.
- **Rollback:** Reverse only `HLP-435` (`git apply --check -R`). No database migration or schema change is involved.
- **Retirement gate:** Retire only when an adopted exact upstream Hermes release evaluates quote structure on raw syntax, eliminates false malformed hardline on valid escaped quotes, retains fail-closed protection for real unclosed quotes and destructive commands in YOLO, and passes `tests/tools/test_hardline_blocklist.py` without this patch.
- **Status:** `PROPOSED_SOURCE_CANDIDATE`. Prepared as a portable patch and evidence candidate in Aether unit `AE-435`; not yet merged into fork `aether-main` or adopted by the installed Aether runtime.
```

---

## 6. Boundaries, non-claims and residual gates

- **Scope boundaries:** Writable paths were strictly limited to `patches/hermes/HLP-435-hardline-quoted-grep.patch` and `specs/issue-435-hardline-parser/evidence/HLP-435.md`.
- **Preserved shared surfaces:** `HERMES_LOCAL_PATCHES.md`, `.github/workflows/policy.yml`, `.gitattributes`, patch reconciliation fragments, aggregate, preflight, test roster, release-lock, version, and all #494 / RC16 worktrees and boards remain untouched.
- **No external publication or runtime mutation:** No git push, no GitHub pull request, no git merge, no tag, no package publication, no service restart, and no profile mutation. Issue #435 remains OPEN.
- **Unit compatibility conclusion:** `release_impact=patch`, `release_action=defer`, `release_channel=none` (provisional source-candidate classification; no installed or release claim).
- **Residual integration and adoption gates:**
  1. Downstream integration unit `INT-435` (`t_0a90a1b5`) executes terminal source-phase integration and verification.
  2. Resolution of RC16 collision on shared surfaces (`.github/workflows/policy.yml` manifest/allow-list where 4 missing non-specs manifest entries gate Aether-PR opening, `HERMES_LOCAL_PATCHES.md`, reconciliation aggregate and test roster); Aether-PR opening remains gated on this reconciliation.
  3. Fork PR creation/merge and eventual effective runtime adoption decision after RC16 stabilizes.
