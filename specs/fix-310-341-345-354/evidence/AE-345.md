# Unit Evidence: AE-345 — Semantic Route Fingerprint and Incomplete Finish Reason

- **Unit:** AE-345
- **Card ID:** `t_15c349ea`
- **Issue:** [#345](https://github.com/DarkArty07/Aether-Agents/issues/345)
- **Objective Contract:** `oc_0084270d940c98d9@v1`
- **Aether Base SHA:** `6562c09abc815738419c80d17929e4cefceb8a2d` (`origin/main`)
- **Candidate Branch:** `aether-agents-2/t_15c349ea-ae-345-semantic-route-fingerprint-and-in`
- **Candidate Commit:** `d604c281ad611df05d53f1fde21df5eefb46b1f2`
- **Review Model:** Same-card Supervisor review (`kanban_request_review`)
- **Unit Compatibility Impact:** `patch`

---

## 1. Pre-mutation Target File Parity

Target production file hash verified before mutation against `oc_0084270d940c98d9@v1` / card body:
- `src/aether_agents/knowledge/semantic.py`:
  - Expected: `866309a52748c3b9fff3e65381e8b37e5d9336e9bd095e8d734e7c5ed84622c1`
  - Observed: `866309a52748c3b9fff3e65381e8b37e5d9336e9bd095e8d734e7c5ed84622c1` (EXACT MATCH)

---

## 2. Regression-First / Fail-First Verification

Focused regression tests were overlaid onto the unmodified base `6562c09` in `tests/test_knowledge_regressions.py`:

**Command:**
```bash
AETHER_GRAPHIFY_PYTHON=... uv run pytest tests/test_knowledge_regressions.py -k "test_ae_345"
```

**Observed Result on Unchanged Base `6562c09`:**
- 2 failed, 26 deselected in 1.46s
- Failures:
  1. `test_ae_345_semantic_route_fingerprint_invalidation_and_caching`:
     - `AssertionError: Changing provider must change semantic fingerprint`
     - `assert 'f20136300ae859d78d34138ce62f67a5439df692a0b396231d81fa876fbc2204' != 'f20136300ae859d78d34138ce62f67a5439df692a0b396231d81fa876fbc2204'`
     - On base `6562c09`, `compute_semantic_fingerprint` completely ignored provider, model, and api_mode.
  2. `test_ae_345_incomplete_finish_reason_not_cached_or_applied`:
     - `AssertionError: assert any('retained' in w.lower() for w in refresh_res['warnings'])`
     - On base `6562c09`, responses with `finish_reason="length"` and valid parseable JSON were accepted as complete, cached, applied to the graph, and overwrote the previous complete snapshot instead of being rejected and retaining the previous complete snapshot.

---

## 3. Candidate Verification

After applying candidate changes to `src/aether_agents/knowledge/semantic.py`:

**Focused Unit Verification:**
```bash
AETHER_GRAPHIFY_PYTHON=... uv run pytest tests/test_knowledge_regressions.py -k "test_ae_345"
```
**Observed Result:**
- `2 passed, 26 deselected in 4.45s`

**Full Regressions Suite Verification:**
```bash
AETHER_GRAPHIFY_PYTHON=... uv run pytest tests/test_knowledge_regressions.py
```
**Observed Result:**
- `27 passed, 1 skipped in 117.84s (0:01:57)`

**Whitespace & Syntax Check:**
```bash
git diff --check
```
**Observed Result:**
- Exit code 0 (clean, no whitespace or formatting errors).

---

## 4. Acceptance Obligations Coverage (AC-345 / D345)

| Obligation | Check | Observed Result | Evidence Location |
| --- | --- | --- | --- |
| Route fingerprint invalidation | Distinct provider, model, or api_mode in `compute_semantic_fingerprint` | Hash differences confirmed across provider, model, and api_mode changes | `tests/test_knowledge_regressions.py:1894-1943` |
| Zero model calls on unchanged explicit route | Re-running update with same explicit route configuration | 0 auxiliary calls made on repeat configured update (`outcome == "unchanged"`) | `tests/test_knowledge_regressions.py:1960-1972` |
| Route change triggers model calls | Re-running update with changed model configuration | New model calls made, fingerprint invalidated (`outcome == "updated"`) | `tests/test_knowledge_regressions.py:1974-1981` |
| Refuse cache publication & reuse on unresolved route | `run_semantic_extraction` with unresolved route markers | 0 files written to semantic cache directory | `tests/test_knowledge_regressions.py:1983-2003` |
| Refuse cache publication on route mismatch | Model call returns route mismatching configured expected route | 0 files written to semantic cache directory | `tests/test_knowledge_regressions.py:2005-2023` |
| `finish_reason=length` rejected from cache & graph | Model returns parseable JSON with `finish_reason="length"` | Not cached (0 files in cache dir), not applied as complete (`state != "complete"`, in `failed_paths`) | `tests/test_knowledge_regressions.py:2076-2096` |
| Retain previous complete snapshot on non-terminal refresh | Refreshing existing complete snapshot with `finish_reason="length"` | Previous snapshot retained (`outcome == "unchanged"`, `snapshot_id == complete_snap_id`, warning present) | `tests/test_knowledge_regressions.py:2054-2074` |
| Legacy omitted finish_reason compatibility | Model returns parseable JSON with omitted `finish_reason` | Complete response accepted (`state == "complete"`, path in `covered_paths`) | `tests/test_knowledge_regressions.py:2098-2114` |

---

## 5. Scope and Safety Verification

- Exclusively modified writable boundaries:
  - `src/aether_agents/knowledge/semantic.py`
  - `tests/test_knowledge_regressions.py`
- Preserved untouched:
  - `src/aether_agents/knowledge/snapshots.py`
  - `src/aether_agents/knowledge/memory.py`
  - `src/aether_agents/objective_contracts/`
  - `HERMES_LOCAL_PATCHES.md`
  - Fork repositories and runtime configs
- No credentials, API keys, secrets, or paid live model calls used.

---

## 6. Exact Ledger Paragraph (for FIX-INT)

```markdown
### HLP-345 / #345 — semantic route fingerprint and incomplete finish_reason

- **Issue:** [#345](https://github.com/DarkArty07/Aether-Agents/issues/345)
- **Commit:** `d604c281ad611df05d53f1fde21df5eefb46b1f2`
- **Evidence:** `specs/fix-310-341-345-354/evidence/AE-345.md`
- **Scope:** `src/aether_agents/knowledge/semantic.py`, `tests/test_knowledge_regressions.py`
- **Upstream relationship:** Aether-owned semantic extraction lifecycle and auxiliary route integration. Hermes `agent.auxiliary_client.call_llm` accepts `route_info` and records provider/model; Aether carries `api_mode` and verifies non-secret route identity against expected configuration.
- **Behavior:** Semantic chunk and overall extraction fingerprints include effective provider, model, and api_mode, preventing stale reuse across route changes. Unchanged explicit route configurations yield zero model calls. Unresolved or mismatched routes refuse cache reuse and cache publication. Responses with explicit non-terminal finish reasons (e.g. `finish_reason="length"`) are treated as incomplete: they are neither cached nor applied to the knowledge graph, and existing snapshot store behavior retains the previously complete snapshot. Missing legacy finish reasons remain compatible where runtime responses provide no finish reason.
- **Rollback:** Revert Aether commit `d604c281ad611df05d53f1fde21df5eefb46b1f2`.
- **Retirement gate:** Permanent Aether knowledge invariant; retired only if semantic extraction architecture or auxiliary client contract is superseded.
```
