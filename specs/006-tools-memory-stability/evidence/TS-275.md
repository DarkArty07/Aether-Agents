# Unit Evidence: U275F — minimal compatible image-route repair (recorded design)

- **Unit:** U275F (repair; implements the Morfeo-recorded TS-275 design)
- **Issue:** [#275](https://github.com/DarkArty07/Aether-Agents/issues/275) — **remains OPEN**; this unit does not close it
- **Objective Contract:** `oc_6176d2be6cb7e6fe@v1`, digest `69765e73e657b7c0854454a4257a43e5f113ed9c4deb9c6559c536b374a5c040` (unchanged; no supersession)
- **Supervisor breakdown:** `specs/006-tools-memory-stability/tasks.md` at `ef69bdc771821416feb3bb65093d3b70013a54a8` (unit U275F)
- **Recorded design implemented:** `specs/006-tools-memory-stability/TS-275-design.md` **V1–V5** as supplied by the design checkpoint commit `ecf1e567ef28dc2ad6fbcdd337f608941e12acea`; blob `cc355b9afa4dd31281349daf96691bb93912f7d9`, sha256 `c964fb82473c77ec7d4ce19d35e21b5388d8fd2e3639a5a54e5acba5913a22eb` (matches the Supervisor-verified parity hash)
- **Review lane:** same-card Supervisor review (`reviewer="supervisor"`)
- **Unit compatibility impact:** `none` — one preparation step inside `tools/vision_tools.py` plus focused tests, in the maintained fork. No public tool schema, argument name, result key, provider, model, endpoint, header, credential, timeout, prompt, transport, config or dependency change.

## 1. Starting point and modification boundary

| Item | Observed |
| --- | --- |
| Aether worktree HEAD at start | `b25af98d4b57941562164794483e10b35638d982` (design checkpoint supplied; clean) |
| Contract digest | re-read from `.aether/objective-contracts/oc_6176d2be6cb7e6fe/v1.md`; matched the assigned digest |
| Maintained fork base | `266e412fb83ad32af92ed391db942f88993d76a2` (= `origin/aether-main`) |
| Repair branch (own worktree) | `fix/275-aux-image-prep-cap` |
| Pre-fix parity with the loaded runtime | `tools/vision_tools.py` sha256 leading-16 `e2892b63cf23498d` in the fork blob *and* in the live runtime's working tree — the pre-fix behavior measured below is the loaded one |
| Aether writable output used | this file only |
| Fork writable product surface | `tools/vision_tools.py`; new focused test module `tests/tools/test_vision_aux_image_prep.py` |

Not touched: the canonical stage files (`spec.md`, `plan.md`, `research.md`, `quickstart.md`,
`evidence.md`, `tasks.md`), `HERMES_LOCAL_PATCHES.md`, the live runtime tree,
`agent/image_routing.py`, `agent/codex_responses_adapter.py`, `agent/auxiliary_client.py`,
`agent/transports/codex.py`, the routing product, and sibling units' surfaces.

## 2. What changed (implementation of the recorded decision)

Fork commit `780a6f6c7550cb6202069a1f8821fe4d4d785633` on `fix/275-aux-image-prep-cap`
(`fix(vision): apply the proactive embed cap before the auxiliary request`):

| File | Change |
| --- | --- |
| `tools/vision_tools.py` | `+29` lines. Blob `e008983f7f20a78026473cde09220714e8734a3e` → `6303559bc5790a5f00a9a339f4bc426f62a1ae3a`; file sha256 `b09720eab0e9994c4b47c25f81cb204f52df73b3d8e2f6ece03c3d668cb77491` |
| `tests/tools/test_vision_aux_image_prep.py` | new, 16 tests; blob `19892440d5076d337840c649eca2b9ed18d24a9d`; file sha256 `a802de4f116ce3554e68227669d8e6e981c7aaa9210ce5a6011679ad2d56c262` |

Inside `vision_analyze_tool`, after the existing source resolution / supported-format
normalization / optional region crop / ordinary encoding / 20-MB hard-limit handling, and
before the messages are built and the first auxiliary request is sent:

- byte comparison on the encoded data-URL length against the existing `_EMBED_TARGET_BYTES` (4 MiB);
- longest-side inspection against the existing `_EMBED_MAX_DIMENSION` (7,900 px);
- when either bound is exceeded, the existing `_resize_image_for_vision` is called with **both**
  explicit caps, via `_run_encode_on_cpu_executor` for both inspection and resize, passing the
  existing `_scale_info`;
- `_build_scale_note` is retained, so an actual rescale stays disclosed in the result;
- an image inside both bounds is sent byte-identical — no resize and no re-encode.

No new constant, config knob, dependency, retry class or code path was added. The reactive
retry block, the 20-MB refusal, the size classifier and the empty-content retry are untouched.
`git diff --stat` for the commit: `2 files changed, 469 insertions(+)`.

## 3. Offline preparation observations (existing primitive, both caps)

Re-run on the three approved fixtures (private, outside Git; complete SHA-256 verified before
use — tall `9a7641f6…fdf3b`, square `ebc32c8d…ca4a5`, ordinary `e87e8160…ea5fd`):

| Input | Primitive result with `max_base64_bytes=_EMBED_TARGET_BYTES`, `max_dimension=_EMBED_MAX_DIMENSION` | Scale metadata |
| --- | --- | --- |
| `ts275-tall-800x40000.png` | `100x5000` px; data-URL length 18,654 | `800x40000 → 100x5000` retained |
| `ts275-square-5000x5000.png` | `5000x5000` px unchanged; decoded bytes sha256 `ebc32c8d…` (identical to source) | none |
| `ts275-synthetic.png` | `640x360` px unchanged; decoded bytes sha256 `e87e8160…` (identical to source) | none |

These reproduce the design checkpoint's V4 primitive results exactly and are feasibility
evidence, not acceptance.

## 4. Pre/post oracle through the provisioned route

Two fixture names are shared by the oracle: the tall image (`800x40000`, image-only token
`QUARTZ-77`, filled rectangle) and the two within-limit controls (`MARLIN-31` square,
`ZEPHYR-42` ordinary). The question asked never names a token, a colour or the answer; it
asks for any text, each filled shape's fill colour as an RGB triple and the background
colour. Each run is a single call of the provisioned `vision_analyze_tool` with
`HERMES_PROFILE=<the provisioned role>` and its own configuration; the outgoing call was
observed (never altered) at the auxiliary-call boundary, recording the exact data URL,
decoded size, pixel dimensions and kwargs.

| Run | Code | Tool result | Sent payload |
| --- | --- | --- | --- |
| **PRE** (tall) | live pre-fix revision `e2892b63cf23498d` | `success:false` — `Error code: 400 - {'detail': '…'}` (generic client detail, no size hint) | `800x40000`, decoded sha256 `9a7641f6…` (byte-identical to the fixture), data-URL length 247,518, **one** attempt, no downscale |
| **POST** (tall) | fork candidate `780a6f6c` | `success:true` — analysis reads **`QUARTZ-77`** and the filled rectangle's colour (reported `RGB (0, 205, 110)`, drawn `(0, 200, 120)`), and correctly treats the white bottom-right outlines as unfilled | `100x5000`, data-URL length 18,654, **one** attempt |
| **POST** (square) | fork candidate | `success:true` — reads **`MARLIN-31`** and `RGB (255, 140, 0)` (drawn `(255, 140, 0)`) | `5000x5000`, data-URL length 123,450, decoded sha256 `ebc32c8d…` = source (byte-identical) |
| **POST** (ordinary) | fork candidate | `success:true` — reads **`ZEPHYR-42`**, the amber triangle and red square colours and the dark-green background | `640x360`, data-URL length 10,290, decoded sha256 `e87e8160…` = source (byte-identical) |

Supporting private observations (identity retained in board-side metadata, deliberately not
reproduced here):

- The effective branch was re-resolved by executing the runtime's own resolution functions with
  the role's live configuration: `decide_image_input_mode → text`, native fast path not
  selected → the **auxiliary vision branch** of `_handle_vision_analyze`; the constructed client
  is the Codex-Responses auxiliary client on the local routing hop with the role-scoped vision
  operation/service query; main provider/model, base URL and credentials were never changed.
- The routing product's own record for the PRE request holds the upstream reason — a per-side
  **image decode limit** rejection of the `image/png` payload at `input[0].content[1]` — while the
  client-visible body is reduced to a generic detail. The three POST requests were accepted
  (`200`) on the same operation, one request each.
- The POST analysis opened with the existing scale disclosure: `Image downscaled from 800x40000
  to 100x5000 for vision; multiply any coordinates you report by 8.00 …`; the two within-limit
  controls carried no scale note.
- The tall POST required no zoom/tiling and no oracle weakening; the 7,900 px maximum was
  reached through the existing bounded halving steps (100×5,000), exactly as the design
  checkpoint anticipated.

## 5. Fork suites and static checks (fork-declared runner)

| Check | Command shape | Observed |
| --- | --- | --- |
| New focused module | `scripts/run_tests.sh tests/tools/test_vision_aux_image_prep.py` | 16 passed, 0 failed |
| Vision suites | `… tests/tools/test_vision_tools.py tests/tools/test_vision_native_fast_path.py tests/tools/test_vision_region.py tests/tools/test_vision_scale_disclosure.py tests/tools/test_vision_aux_image_prep.py tests/tools/test_image_source.py tests/run_agent/test_image_shrink_recovery.py` | 7 files, 114 passed, 0 failed |
| Auxiliary / adapter / transport / routing suites | `… tests/agent/test_auxiliary_client.py tests/agent/test_auxiliary_client_chat_and_attribution.py tests/agent/test_codex_responses_adapter.py tests/agent/test_image_routing.py tests/agent/transports/test_codex_transport.py tests/tools/test_computer_use_vision_routing.py` | 6 files, 353 passed, 0 failed |
| Whitespace | `git diff --check` | clean |
| Lint | `ruff check tools/vision_tools.py tests/tools/test_vision_aux_image_prep.py` | All checks passed |
| Types | `ty check tools/vision_tools.py tests/tools/test_vision_aux_image_prep.py` | 9 diagnostics, all pre-existing: the identical set (same path+rule+message keys) is produced by the same check at the untouched base `266e412`; the new test module adds none |

The new module covers: dimension-only, byte-only and both-bounds preparation on the **first**
request; exact-edge and within-bounds byte preservation (including a decoded-byte SHA-256
comparison); crop-before-cap ordering and both disclosure clauses; non-image and missing
sources rejected before any model call; no retry for a generic 400 and no broadening of the
existing size-classified retry; dimension inspection and resize observed on the dedicated
`vision-encode` executor thread (not the event-loop thread); no temp residue and an untouched
user original; the native branch still applying the same policy; and the chat-to-Responses
conversion carrying the prepared data URL byte-exactly.

## 6. Preservation, scope and residue

- The design's byte-preservation obligation is demonstrated on the provisioned route itself:
  both within-limit controls were transmitted decoded-SHA-256-identical to their source files,
  and the deliberate resize applies only to the over-bound image, with its scale disclosed.
- No public interface, tool-call identity, response chronology, task/service attribution,
  provider, model, route, endpoint, header, credential, timeout or prompt content changed.
- No credential was read or printed; the routing product was read only (its journal/metrics) and
  never modified; the live runtime tree was read, not edited.
- Unrelated dirty runtime files, other worktrees and sibling units were untouched. The fork
  worktree is clean at `780a6f6c`; the branch is unpushed. The three real POST calls plus the
  PRE recheck are the only external effects, all on the already-provisioned route.

## 7. Ready-to-consolidate ledger entry (owner: terminal card `t_e4ba4b4e`)

`HERMES_LOCAL_PATCHES.md` is read-only for this unit. Proposed entry, to be honest at
consolidation time (integration + adoption state belong to the terminal card):

| ID | Aether issue | Local repair | Upstream tracking | Status |
| --- | --- | --- | --- | --- |
| `HLP-275` (proposed) | `#275` | the auxiliary vision path applies the existing 4 MiB / 7,900 px proactive embed policy before its first request, so a small-byte extreme-dimension image is prepared instead of being rejected on the provider's decode limit; within-bounds images stay byte-identical and any rescale is disclosed | no upstream issue/PR filed by this unit; the vendor documents a different attachment pipeline and does not document this path's decode limit | candidate committed on `fix/275-aux-image-prep-cap` (`780a6f6c`); not yet integrated into the maintained fork or adopted into the runtime |

Primary active files for the entry: `tools/vision_tools.py`,
`tests/tools/test_vision_aux_image_prep.py`.

## 8. Limits, pending acceptance and publication

- **Pending, not claimed:** the scoped runtime canary after adoption (reload the runtime on the
  integrated fork revision and repeat the same call) is terminal-card work; independent
  acceptance, integration and publication are not part of this unit.
- The upstream gateway's exact per-side decode threshold remains unknown; the cap reused here is
  the repository's existing conservative value, inside the observed accept (5,000 px) / reject
  (40,000 px and above) gap, and invents no new constant.
- Only the role's provisioned vision lane was exercised; other provider lanes are unmeasured.
- Rescaling to 100×5,000 preserved enough information for the image-only oracle here; if a
  future case cannot be read after preparation, the honest outcome is that evidence, not a
  weakened oracle or unrecorded tiling.
- No push, PR, merge, tag, release, deployment or issue mutation was performed; #275 stays open.
