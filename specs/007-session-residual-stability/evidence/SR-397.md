# SR-397 Implementation Evidence — write-capable probe isolation and pre-write gate

**Unit:** U397 (task `t_9f54c3cf`)
**Objective Contract:** `oc_291b2fb34b413d92@v2` (`.aether/objective-contracts/oc_291b2fb34b413d92/v2.md`, SHA-256 `9fd10684ba41c790f542836b69d0fa5056a36e1b6146e59ef17723c760431442`)
**Source:** `specs/007-session-residual-stability/spec.md` §SR-397, `plan.md` §D397, `quickstart.md`, approved breakdown `specs/007-session-residual-stability/tasks.md`
**Owning issue:** #397 (Supervisor probes wrote synthetic tasks to the live monitor board; audit rows were deleted; direct SQL cleanup is prohibited)
**Base:** `8d70acc8d9756db1f796e44e083c3baa77ede4d3` (verified `git rev-parse HEAD` before editing, clean `git status`)
**Candidate commit:** `c5ce601` (branch `aether-agents-2/t_9f54c3cf-u397-write-capable-probe-isolation-and-p`)
**Phase:** Implementer unit; **no live effect performed** — no push, no PR, no issue/board mutation, no live-board write, no service restart, no profile/credential/provider/model change, no Monitor-owned file touched
**Unit compatibility impact:** `patch` (`release_impact=patch`, `release_action=defer`, `release_channel=none` remain INT-owned aggregation)

---

## 1. Consumed accepted Monitor v4 / D15R laboratory isolation result (read-only)

Verified against the merged revision `42bf4b9abc8c85359a38b61c2f7a7c8fa8cae5ae` (PR #405; reviewed D15R candidate `b59e0e06bcffe6722f84aadd3c44995a165a479a`; evidence `specs/telegram-monitor/evidence/MON-D15R.md`). Nothing was duplicated, modified or re-implemented; no Monitor-owned path is in this unit's diff.

| Inspected artifact at `42bf4b9` | Blob |
| --- | --- |
| `scripts/telegram_monitor_lab.py` | `a58659bf4a272e76aa07117391d72a645d243c6d` |
| `scripts/qualify_telegram_monitor.py` | `05f7fc6468ae06539bf01fd0eabe7c837bd6d644` |
| `specs/telegram-monitor/evidence/MON-D15R.md` | `ec486bc46f5943c5ddcbdcb36b27ac58e055fdf3` |
| `specs/telegram-monitor/qualification-isolation.md` | `e31c368dd2409723716651ed244f866a321811e0` |
| `.aether/objective-contracts/oc_f8c9fc9320587cf3/v4.md` | `5214ad2517d2dc3b3b80074b84c65db5be8aa7eb` |

What the accepted D15R child environment provides (`child_environment`):

- removes every inherited production routing selector: `INHERITED_SELECTORS` (`HERMES_HOME`, `HERMES_PROFILE`, `HERMES_SESSION_ID`, `HERMES_TENANT`, `HERMES_KANBAN_DB`, `HERMES_KANBAN_BOARD`, `HERMES_KANBAN_TASK`, `HERMES_KANBAN_RUN_ID`, `HERMES_KANBAN_WORKSPACES_ROOT`, `HERMES_KANBAN_GOAL_MAX_TURNS`, `TMPDIR`, `AETHER_MONITOR_QUALIFICATION_SCHEDULE`, all `XDG_*`) **and** every `HERMES_KANBAN_*` prefix (`INHERITED_SELECTOR_PREFIXES`); the built environment therefore never contains `HERMES_KANBAN_DB`;
- pins `HOME`, `HERMES_HOME`, `TMPDIR`, each XDG root, `PYTHONDONTWRITEBYTECODE` and `PYTHONPATH` inside the private laboratory root, and adds only the provisioned access names;
- verifies fail-closed before effects: `context_problems` (each redirected root set, absolute, equal to the planned lab path, resolving inside the lab root; plugins metadata inside and on `PYTHONPATH`) and `writer_problems`/`_effective_root_problems` (resolves `WRITER_EFFECTIVE_ROOTS` = `hermes_home`, `kanban_home`, `kanban_db`, `boards_root`, `projects_db` through the loaded native modules and refuses `writer-root-*` codes when any escapes the laboratory);
- the fixture child resolves its own effective roots before its first write (`escapes()` → `fixture-root-escape`) and seeds boards through the shipped `_provision_execution_board` writer with canonical `board=<slug>` paths, so multi-board writes resolve under `<lab>/hermes/kanban/boards/<slug>/kanban.db`.

**Conclusion:** the accepted D15R result satisfies the multi-board half of SR-397 (canonical multi-board probes, no raw Kanban selector, canonical `board=` paths inside the private laboratory, effective-root refusal before the first writer). The residual gap this unit implemented is the **single-board Aether-owned** half: a shared constructor/gate for any write-capable Aether harness or canonical review probe that may invoke native Kanban writers, including the `os.environ.copy()` fallback that the Monitor path never had.

## 2. Residual gap implemented (Aether-owned, Monitor-disjoint files)

New module `src/aether_agents/lab/isolation.py` (single dependency-free module; the canonical home of the moved `HarnessError`, `preflight_disposable_destinations`, `isolated_hermes_env`, `native_python_for`):

- `resolve_writer_roots(environ=None, *, native=None)` resolves the **effective** home, board root, boards root, database, projects database and workspaces a loaded writer would use; native resolvers (`hermes_constants:get_hermes_home`, `hermes_cli.kanban_db:{kanban_home,boards_root,kanban_db_path,workspaces_root}`, `hermes_cli.projects_db:projects_db_path`) are authoritative in the child; the environment-derived form mirrors the same override precedence (`HERMES_KANBAN_DB` outranks `HERMES_HOME`) for a foreign process.
- `writer_root_problems(roots, *, private_roots)` / `verify_writer_context(...)` compare each resolved root with the declared private roots: absolute, **non-symlink**, resolvable, and **contained** (equal or descendant); any mismatch raises `HarnessError` with bounded codes (`<root>-escape`, `<root>-symlink`, `<root>-not-absolute`, `<root>-unresolved`, `private-root-*`) *before* any writer runs.
- `verify_child_writer_context(private_roots)` is the in-child gate: resolves this process's roots with the loaded writer modules and refuses before the first write.
- `probe_child_writer_roots(python, environ)` runs the self-contained read-only `CHILD_WRITER_ROOTS_PROBE` in the provisioned interpreter; `require_verified_writer_context(...)` consumes it and refuses on mismatch, recording `child_probe: native|unavailable` in a decision-only receipt (root names and counts, never paths).
- `scrub_inherited_identity(environ)` is the single scrub definition (`HERMES_KANBAN_*`, `HERMES_SESSION_*`, `TERMINAL_CWD`, `HERMES_CWD`, `HERMES_DELEGATED_CHILD_CONTEXT`, `HERMES_PROJECT_ID`, `HERMES_TENANT`, `AETHER_PROJECT_ID`) reused by the constructor and the matrix launcher.

Wiring of write-capable helpers:

| File | Change |
| --- | --- |
| `src/aether_agents/lab/persistent.py` | `run_persistent_session` **no longer falls back to `os.environ.copy()`**: it requires an isolated `env` plus its private `run_root`/`hermes_root`, or an explicit disposable `run_root` from which it constructs `isolated_hermes_env`; the pre-write gate runs before the native session child is launched |
| `src/aether_agents/lab/runner.py` | moved definitions re-exported for the historical surface; `live_run` gates the constructed context before the first Hermes invocation; `_observe_native_affinity_controls` gates its native control writer child; `dispatch_until_settled` call sites declare the disposable roots |
| `src/aether_agents/lab/observation.py` | `live_observation` gates the constructed context before `_invoke_morfeo` |
| `src/aether_agents/lab/dispatch.py` | `dispatch_until_settled` (the `hermes kanban dispatch` writer) verifies declared `run_root`/`hermes_root` before the first dispatch |
| `src/aether_agents/lab/matrix.py` | the scenario launcher scrubs inherited routing identity from the child environment |
| `src/aether_agents/lab/__init__.py` | lazily exports the new public lab names |

## 3. Reviewer procedure

- `src/aether_agents/resources/skills/supervisor-decomposition/SKILL.md` (Supervisor/reviewer canonical procedure): review step 9 now requires a write-capable probe or review step to construct one disposable board context and refuse a context that resolves outside its private roots before the first writer; accidental live/shared-board mutation is preserved as evidence and escalated through the supported lifecycle; a new subsection "Accidental mutation is evidence, not cleanup" states that direct SQL deletion of tasks, comments, events or runs is never cleanup.
- `docs/guides/execution.md` (reader-facing, non-Monitor): the review/integration section records the same rule plus the disposable-copy reproduction and byte/row-stable live-board fingerprint obligation.

## 4. Native destination precedence unchanged

`HERMES_KANBAN_DB` still outranks `HERMES_HOME` for native writers; production workers keep requiring the explicit dispatcher pin. The gate verifies a context, it does not rewrite resolution. Disposable reproduction copies a foreign target; the live board is never written by this unit.

## 5. Requirement mapping

| SR-397 obligation / card outcome | Check that actually ran | Observed result | Evidence |
| --- | --- | --- | --- |
| Consume accepted D15R read-only; confirm the multi-board half | `git show 42bf4b9:<path>` inspection of the five artifacts above (child environment, `context_problems`, `writer_problems`, `_effective_root_problems`, fixture self-check) | Verified: no raw Kanban selector, canonical `board=` inside the lab, effective-root refusal before the first writer → multi-board half satisfied; nothing modified | §1, blob table |
| Any Aether harness/review probe that may invoke native writers constructs one disposable context first | `require_verified_writer_context(...)` in `live_run`, `live_observation`, `_observe_native_affinity_controls`, `run_persistent_session`, `dispatch_until_settled` | Wired; helper-level refusal proven (tests below) | `src/aether_agents/lab/{runner,observation,persistent,dispatch}.py` |
| Child-side resolution of home, board root, DB, projects DB, workspaces compared to private, non-symlink, contained roots | `test_child_gate_resolves_every_effective_root_with_the_native_modules` (native child resolves all six roots; asserts containment and exact disposable paths) | PASS (9 passed with the release-locked Hermes checkout on `PYTHONPATH`) | `tests/test_lab_writer_isolation.py` |
| Mismatch refuses before writing | `test_pre_write_gate_refuses_a_mismatch_with_zero_rows_written`, `test_child_side_gate_refuses_before_writing_and_allows_the_disposable_target` | PASS: `kanban_db-escape`; foreign copy row/byte-identical before and after; child never launched | same |
| No write-capable helper falls back to `os.environ.copy()` without the gate | `test_persistent_session_refuses_without_a_declared_disposable_context` (behavioral: poisoned ambient env, helper refuses, child never launched) + `test_every_write_capable_helper_constructs_or_verifies_a_context` (source assertion over the five write-capable modules) | PASS on candidate; RED on base (`DID NOT RAISE HarnessError`) | same |
| Live-board fingerprint and row-count canary stay byte/row stable | ambient live board fingerprint (bytes + per-table counts/content digests, read-only) captured before/after the focused lanes; `test_inherited_raw_selector_mutates_the_foreign_copy_not_the_live_snapshot` | `LIVE BOARD FINGERPRINT UNCHANGED`; the vulnerable pattern mutated the disposable copy while the stand-in live snapshot stayed identical | §6.3 |
| Reviewer procedure forbids direct SQL cleanup and requires preserving/escalating accidental mutation | skill + guide edits (§3); document/loading check `tests/test_contract_quality_documents.py`, `tests/test_documentation.py` | 42 passed, 1 skipped (documented text checks, not behavioral proof) | §6.4 |

## 6. Verification commands and observed results

### 6.1 RED — same tests on the untouched base `8d70acc`

```bash
# archive export of the base revision (no Git state in this repository touched)
git archive 8d70acc | tar -x -C /tmp/sr397-base
cp tests/test_lab_writer_isolation.py tests/test_e2e15_qualification.py /tmp/sr397-base/tests/
cd /tmp/sr397-base
PYTHONPATH=<release-locked checkout v2026.8.18> <venv>/bin/python -m pytest -q -p no:randomly -rf --tb=line tests/test_lab_writer_isolation.py
```

Result: **6 failed, 3 passed** on base.

| Base failure | Failure mode |
| --- | --- |
| `test_persistent_session_refuses_without_a_declared_disposable_context` | `Failed: DID NOT RAISE HarnessError` — the base helper launched the native session child with the inherited `HERMES_KANBAN_DB` (foreign-target copy) and only then reconciled; this is the #397 vulnerability reproduced at the helper level |
| `test_persistent_session_constructs_the_disposable_context_and_writes_only_there` | `TypeError: unexpected keyword argument 'run_root'` (no disposable context construction existed) |
| `test_dispatch_writer_verifies_the_declared_disposable_roots` | `TypeError: unexpected keyword argument 'run_root'` |
| `test_every_write_capable_helper_constructs_or_verifies_a_context` | source assertion failed: `os.environ.copy()` was present in `persistent.py` |
| `test_pre_write_gate_refuses_a_mismatch_with_zero_rows_written` | `ImportError: cannot import name 'isolation' from 'aether_agents.lab'` |
| `test_child_gate_resolves_every_effective_root_with_the_native_modules` | `ImportError: cannot import name 'isolation' from 'aether_agents.lab'` |

Base passes: the vulnerable-pattern reproduction (it demonstrates the danger, not the fix) and the two child-script tests, which passed only because the developer venv's *editable install* resolves `aether_agents` from the candidate tree even when the archive is on `sys.path`; the base-level RED for those two is carried by the six failures above and by the commit-level absence of `src/aether_agents/lab/isolation.py` at `8d70acc`.

### 6.2 GREEN — candidate

```bash
uv run --frozen python scripts/run_tests.py -- -q -p no:randomly -rs tests/test_lab_writer_isolation.py
# → 9 passed in 0.98s  (release-locked Hermes checkout on PYTHONPATH; native child tests executed)

uv run --frozen python scripts/run_tests.py -- -q -p no:randomly \
  tests/test_lab_writer_isolation.py tests/test_e2e15_qualification.py tests/test_e2e_harness.py \
  tests/test_lab_formalization.py tests/test_e2e_matrix.py tests/test_observation_qualification.py
# → 134 passed, 2 skipped in 28.20s (both skips pre-existing/conditional, not caused by this unit)

uv run --frozen python -m pytest -q -p no:randomly --cov=aether_agents.lab.isolation \
  --cov=aether_agents.lab.persistent --cov=aether_agents.lab.dispatch --cov-report=term-missing \
  --cov-fail-under=0 tests/test_lab_writer_isolation.py tests/test_e2e15_qualification.py \
  tests/test_e2e_harness.py tests/test_lab_formalization.py
# → isolation.py 80%, persistent.py 80%, dispatch.py 73% (focused lanes only)
```

### 6.3 Live-board preservation canary

```bash
python3 /tmp/sr397_live_board_fingerprint.py > /tmp/sr397-live-before.json   # read-only snapshot
uv run --frozen python scripts/run_tests.py -- -q -p no:randomly \
  tests/test_lab_writer_isolation.py tests/test_e2e15_qualification.py \
  tests/test_e2e_harness.py tests/test_lab_formalization.py
# → 65 passed, 1 skipped in 9.05s
python3 /tmp/sr397_live_board_fingerprint.py > /tmp/sr397-live-after.json
diff -q /tmp/sr397-live-before.json /tmp/sr397-live-after.json
# → LIVE BOARD FINGERPRINT UNCHANGED
```

Repeated at the final revision over `tests/test_lab_writer_isolation.py` + `tests/test_e2e15_qualification.py`
(25 passed in 2.08s): `TIGHT CANARY: LIVE BOARD FINGERPRINT UNCHANGED`.

The fingerprint is byte digest + per-table row counts and content digests of the board the ambient dispatcher environment selects (`tasks`, `task_events`, `task_runs`, `task_links`, `kanban_session_affinity`, …). The ambient `HERMES_KANBAN_DB` of this worker points at a live contract board; the fingerprint is identical before and after each run, and the tests themselves only wrote their disposable copies. A comparison spanning the whole working session does differ, exclusively in `task_events`/`task_runs`/`tasks` rows produced by the running dispatcher and this worker's own lifecycle/heartbeats — not by any test in this unit (the tight before/after runs above isolate the test effect).

### 6.4 Repository gates

| Gate | Command | Result |
| --- | --- | --- |
| Sync | `uv sync --frozen` | environment already exact/frozen; no resolution change |
| Focused lanes | see §6.2 | PASS |
| Ruff check | `uv run --frozen ruff check src tests` | `All checks passed!` |
| Ruff format check | `uv run --frozen ruff format --check src tests` | 5 files would be reformatted — **all five are pre-existing and untouched by this unit** (`knowledge/semantic.py`, `objective_contracts/hermes_plugin.py`, `tests/test_knowledge_regressions.py`, `tests/test_objective_contracts.py`, `tests/test_observation_packaging.py`; `git status` shows them unmodified). Every file changed by this unit is formatted (`ruff format --check src/aether_agents/lab tests/test_lab_writer_isolation.py` → all formatted) |
| mypy | `uv run --frozen mypy src/aether_agents` | `Success: no issues found in 54 source files` |
| Coverage (project gate, CI form) | `PYTHONPATH=<release-locked checkout> COVERAGE_FILE=<outside the repository> uv run --frozen coverage run -m pytest -q -p no:randomly --ignore=tests/test_observation_performance.py` then `uv run --frozen coverage report --format=total` | candidate: **2 failed, 1249 passed, 65 skipped**, `total 71`; clean base archive under the identical command: `total 70`. The CI job additionally appends the isolated graphify-component coverage (`"$AETHER_GRAPHIFY_PYTHON" -m coverage run --append -m pytest -q tests/test_graphify_worker_native.py`) with `AETHER_TEST_COMPONENT_INSTALL=1`, which this local environment does not provision, so the absolute total is below the `fail_under = 78` floor by construction; the comparable base-vs-candidate numbers show **no coverage regression**. Changed-module coverage from the focused lanes: `isolation.py` 80%, `persistent.py` 80%, `dispatch.py` 73% |
| Full suite | `uv run --frozen python scripts/run_tests.py -- -q -p no:randomly` | **2 failed, 1252 passed, 65 skipped, 445 subtests passed** — the two failures are the documented pre-existing ones and reproduce identically on the untouched base export (`test_tracked_public_surface_contains_no_operator_paths`, `test_initial_review_requires_an_independent_reviewer`) → 0 regressions |
| Documentation | `uv run --frozen python scripts/check_documentation.py` | `documentation validation passed` |
| Doc/loading checks | `uv run --frozen pytest -q tests/test_contract_quality_documents.py tests/test_documentation.py tests/test_policy_hooks.py` | 42 passed, 1 skipped, 425 subtests passed |
| Build | `uv build` | built `aether_agents-0.24.0.tar.gz` + wheel; `dist/` removed afterwards |
| Public-artifact scan | `uv run --frozen python scripts/check_public_artifacts.py --root . --artifact dist/*.whl --artifact dist/*.tar.gz` | fails on exactly the inherited findings in `.aether/objective-contracts/oc_0084270d940c98d9/v1.md` (`absolute-user-home`, `operator-desktop-layout`); the same two findings reproduce on the untouched base export → no new finding, gate not weakened |
| `uv sync --frozen` | as above | PASS |

## 7. Residual risk

- The gate is a cooperating-process integrity control, not an OS sandbox: it prevents an *unintended* laboratory context from reaching a live board; a process that deliberately fabricates its own environment is out of scope (same boundary as the Monitor laboratory).
- When the child interpreter cannot import the native modules, verification falls back to the environment-derived form and the receipt records `child_probe: unavailable`; containment and pin checks still run, but the native confirmation is absent. In this environment both forms were exercised.
- `run_persistent_session` now refuses without a declared disposable context; external callers of the exported alias `persistent_session_run` must pass `env`+`run_root` or `run_root`. This is the intended fail-closed change and is recorded as the unit's compatibility impact (`patch`).
- The lab gate resolves `boards_root`/`workspaces_root` through the runtime's native modules; a future Hermes revision that renames a resolver degrades to the environment-derived form (`child_probe: unavailable`) rather than guessing.

## 8. Deferred integration notes (for the INT terminal card)

1. **`.github/workflows/policy.yml` manifest (shared file, not edited here).** Two new tracked files need manifest lines:
   - `src/aether_agents/lab/isolation.py` — place next to the other `src/aether_agents/lab/*.py` entries (after `src/aether_agents/lab/dispatch.py`, before `src/aether_agents/lab/__init__.py`).
   - `tests/test_lab_writer_isolation.py` — place next to `tests/test_lab_formalization.py`.
2. **`scripts/qualify_observation.py` (outside this unit's writable surface; inspected, not changed).** `_qualification_environment` (≈ line 631) still builds the Hermes-core-test environment with `os.environ.copy()` and keeps ambient `HERMES_HOME`/`HERMES_KANBAN_*`. It is the exact-Hermes qualification lane rather than an Aether Kanban probe, so it is not a demonstrated SR-397 residual; if INT judges it in scope, the exact deferred edit is to scrub inherited `HERMES_KANBAN_*`/`HERMES_SESSION_*`/`TERMINAL_CWD`/`HERMES_CWD` and pin a disposable `HERMES_HOME` there.
3. **Monitor-owned files.** No Monitor path needed a change; `src/aether_agents/monitor/**`, `scripts/telegram_monitor_lab.py`, `scripts/qualify_telegram_monitor.py`, `specs/telegram-monitor/**` and `docs/guides/telegram-monitor.md` are untouched, so no Monitor-side deferred edit exists.
4. **Issue closeout.** #397 closes only on the INT runtime canary (`quickstart.md` §Runtime canaries: “run the reviewed writer probe against a disposable board while hashing/counting the selected live board before and after; only the disposable DB may change”).

## 9. Preserved

Accepted Monitor D15R/v4 code, evidence and private laboratory roots/receipts/logs; production and operator state (live board fingerprint unchanged, verified read-only); no direct SQL deletion of tasks, comments, events or runs; no push/PR/issue/board mutation; every unrelated branch/worktree/stash; the canonical Objective Contract bytes.
