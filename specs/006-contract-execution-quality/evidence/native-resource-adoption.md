# Native resource adoption — organic observation

## Delivered file state

The owner-selected direct adoption is complete at the file level. The existing explicit
canonical registration now lists the three new procedures alongside the previous three.
Inventory expectations were reconciled in the existing packaging/lifecycle/documentation
checks; those checks were not executed locally in this delivery.

The actual provisioned native homes received the three authored skill files per role
and the three authored SOUL files. [The sanitized receipt](native-resource-adoption.json)
records the exact resource hashes: 12 target files matched their intended sources.
Metadata for 1,749 untargeted static files remained unchanged. The generated Morfeo skill
usage counter changed during ordinary tool use and was preserved, not rolled back.
That counter is runtime bookkeeping, not a private skill document or configuration.

The installation is operator-managed, not an existing ReleaseStore profile bundle.
There was no ownership marker to forge or adopt. Only the explicitly authorized resource
files were updated through native guarded file tools; new same-name skill collisions
were refused during preflight, and the installed SOUL deltas were inspected before
replacement. No config, model/provider, tool selection, credential, session, board,
service or release-manager setting was changed. No agent, model probe or service restart
was initiated. Existing sessions may retain previously loaded instructions; subsequent
ordinary sessions consume the updated resources.

## Preservation and rollback

Exact previous SOUL bytes and a private per-resource manifest were saved before any
live write. Backup hashes were read back. New skill targets were recorded as absent
before installation. The private backup location is recorded in the operator's delivery
record and is outside temporary task/worktree cleanup; it contains no copied credentials.

For a later explicitly requested rollback:
1. Read the private manifest and compare each current target to the installed hash.
2. If a target has changed since adoption, preserve it and reconcile the conflict;
   do not overwrite later user work.
3. Restore the three prior SOUL byte streams through the normal authorized file surface.
4. Remove only the nine new skill files/directories whose exact bytes and ownership still
   match this operation; preserve all other skills and all generated current usage state.
5. Read back the selected file state and record the rollback. No rollback drill was run.

## Observation and closeout boundary

No new local tests, synthetic scenarios, benchmarks, fresh-process qualification probes
or model-driven evaluation were run. The 52 earlier document/loading tests remain
historical evidence only. Required remote repository checks are not disabled; their
actual status belongs to the PR closeout, not this file-operation receipt.

[Issue #317](https://github.com/DarkArty07/Aether-Agents/issues/317) remains open for
organic expected-versus-observed behavior on real authorized work. Root AGENTS guidance
and the execution guide make this status discoverable to subsequent sessions. Neither
installation nor absence of bug reports establishes prompt quality or throughput.

The owner-withdrawn Supervisor root remains stopped with no children. No replacement
pipeline or anonymous product implementation was launched. Source delivery/installation
closes independently from the ongoing organic behavior observation.

Compatibility assessment: `release_impact=minor` for the additive canonical procedures;
`release_action=defer`; `release_channel=none`. There is no public API/schema removal or
new provider requirement, and existing procedures/role boundaries are preserved. No tag,
GitHub Release, package publication or deployment is part of this adoption. Repository
merge and resource file readback are distinct from a qualified public installed release.
