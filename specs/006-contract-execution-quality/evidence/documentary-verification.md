# Personal documentary verification by Morfeo

## Current delivery decision and status

The owner explicitly withdrew the delegated route and requires personal authorship of
the critical documents. This pass is Morfeo's direct source-document work, not a resumed
Supervisor/Implementer implementation. The old finalized Objective Contract remains
immutable; its historical routing is not authority to restart against the current owner
instruction. The native root remains blocked, with no children or Implementer runs.

This is an AUTHOR SELF-CHECK, not an independent reviewer approval or live Q1–Q7 result.
The original full-system acceptance remains unsatisfied until distribution, actual role
behavior, activation and closeout have evidence. Nothing here relabels those requirements
as fulfilled by a text check. No active-profile copy, configuration or service changed.

## Applied documentary improvements

The personally authored candidate already contains three role-specific procedures,
focused changes in the three SOUL resources, R2/R3/R7 reconciliation, specification,
plan and a retained full-system qualification design. This pass adds:

- question-driven inspection with a clear transition from enough information to output;
- distinctions between decided, assumed, locally delegated and illustrative statements;
- cold-read contract self-check, without pretending the author is independent;
- a compact material-decision/evidence aid in the existing plan, not a new registry;
- a concrete parallel split example with an actual shared-file serial counterexample;
- an explicit start-of-implementation condition once the unit's inputs are verified.

Templates/examples remain illustrative. No universal test-first rule, worker quota,
tool changes, timing classifier, new permission or generic prompt-evaluation engine was
introduced. Root AGENTS discovery remains applicable; no new index is required.

## Seven authored cases: reasoning walkthrough, not observed agent behavior

The cases below are controlled examples. Decisions are Morfeo's documented review of
the written instructions; no downstream model was invoked or assumed to obey them.

| Case | Concrete input/problem | Morfeo's self-check decision and reason | Supporting source section |
|---|---|---|---|
| D1 proportionality | Greeting text changes from Hello to Hola; existing verifier already defines success; no shared API/data change | Direct action needs neither contract nor architecture. The pipeline-only trigger and exclusions preserve this; a compact sufficient contract is not rejected merely for lacking diagrams. | objective-contract-design: When to Use; Procedure 5; Verification |
| D2 missing shared decision | Two units consume an export API but error/result shapes and authority to choose them are absent | Return the specific shared design gap to Morfeo. A private helper name is not that gap and stays with Implementer. No blanket request to re-explain the project. | supervisor-decomposition: Procedure 1/3; implementation-evidence: Procedure 2 |
| D3 contradictory preservation | Contract requires real board/session dogfood and also zero changes to all board/session bytes | These conditions conflict. The author must resolve the owning acceptance before execution; the receiver cannot silently reinterpret it as preservation of unrelated state. | objective-contract-design: Procedure 8; supervisor-decomposition: Procedure 1 |
| D4 complex sufficient interface | Queue states, exact transitions/result shapes, single writer, idempotent completion and invalid transition error are supplied | Preserve those material choices and map duplicate/invalid transitions to expected checks. Internal helper organization stays local. Artifact completeness is not proof the queue already works. | objective-contract-design: Procedure 5–7; Writing rules and compact evidence |
| D5 real independence | Formatter/exporter have separate files, fixed interfaces and separate verifiers; neither consumes the other | Two sibling units share the verified root prerequisite, with no formatting-to-export dependency. Same-card review precedes terminal integration. This is graph reasoning, not a claim that real worker intervals overlapped. | supervisor-decomposition: Concrete split example; Procedure 6/9 |
| D6 necessary order | Both proposed units would modify adapter.py, or a consumer needs a verified interface artifact | Name the collision or required artifact and preserve ordering. Do not refactor for a quota or bypass review to create apparent parallelism. | supervisor-decomposition: Procedure 3/4; Concrete split example |
| D7 incomplete delivery | Happy path passes but malformed input mutates source data; summary claims PASS | The preservation acceptance is missing/violated. Inspect code and real negative-check evidence, return rework on the native lane, and do not treat summary success as completed acceptance. A private helper choice needs no decision card. | implementation-evidence: Procedure 5–8; Evidence shape; supervisor-decomposition: Procedure 9 |

Cross-role check: material intent/design stays with Morfeo; contract-supported shared
execution decisions and decomposition stay with Supervisor; reversible local technical
choices stay with Implementer. All three skills deny self-granted authority. A runtime-
supplied review graph is followed rather than replaced by a parallel lifecycle.

## Actual execution evidence

Executed from the candidate source with the already provisioned Hermes interpreter,
explicit disposable HOME/state/data/config directories and no model call:

```text
python -m pytest -q tests/test_contract_quality_documents.py tests/test_documentation.py tests/test_objective_contracts.py
52 passed
```

The new document module provides eight checks: three role-specific metadata/single-file
checks, three executable-template absence checks, one documented review-lane consistency
check and one native loader check. The last test invokes the actual Hermes `skill_view`
in a separate process with an isolated temporary home, compares returned content SHA-256
to source bytes for all three skills, and requires each resolved source path to remain
inside that temporary home. It does not modify installed skills, invoke a model, prove
packaged distribution, or count as the native role-run loading gate in the full plan.

`document-resource-manifest.json` binds the six source document bytes reviewed here.
All checks must be interpreted at their real boundary: substring assertions are wording
regressions, not behavioral understanding; exact-byte loading is loading, not obedience.

## Unapplied refinements and limits

An optional final refinement to the SOUL wording and current delivery status headers was
attempted with the normal multi-file patch tool. Native protected-instruction approval
timed out. The tool reported zero changes; Git verification showed only the three
successful skill edits. No alternate write route or guard/config bypass was attempted.
The SOUL resources remain at the earlier corrected, 44-test-verified candidate; the
additional question-driven stop conditions are currently explicit in their skills only.
The related issue is #315. This file records that boundary; it is not a substitute edit
of the denied instruction text or a claim that the blocked patch was applied.

The retained spec/plan/quickstart describe the earlier broader system qualification,
not new permission to resume its stopped dispatch. Current owner direction governs.
Source registration still contains the original three-skill runtime allowlist. Therefore
these new sources are not advertised as installed or activated, and a full new-resource
package/lifecycle qualification has not been claimed. No full-system PASS, speedup,
review approval, GitHub merge, release or activation is inferred from these 52 tests.

## Closeout disposition

This pass preserves the personally authored documents in a normal local commit and
leaves the broader issue open with an accurate delivery status. The source branch and
stopped worker worktree remain intentional residue. Unrelated branches, worktrees,
stashes, private runtime resources and another objective's untracked files are preserved.
No release/tag/package publication applies to this unactivated documentary refinement:
release_impact=none for this pass alone; release_action=defer; release_channel=none.
The earlier planned additive package impact remains unevaluated until that package is
actually implemented and qualified.
