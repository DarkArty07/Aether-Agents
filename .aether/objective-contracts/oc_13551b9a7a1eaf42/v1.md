---
artifact_type: "aether.objective-contract.v1"
project_id: "12027989-a08f-41cd-a82c-54ff1bfb6b03"
contract_id: "oc_13551b9a7a1eaf42"
version: 1
status: "final"
title: "#435 — hardline quoted-grep parser source correction without RC16 interference"
created_at_utc: "2026-09-26T04:49:32Z"
created_at_local: "2026-09-25T22:49:32-06:00"
finalized_at_utc: "2026-09-26T04:52:31Z"
finalized_at_local: "2026-09-25T22:52:31-06:00"
author_profile: "morfeo"
created_in_session: "20260925_200905_e42c91"
finalized_in_session: "20260925_200905_e42c91"
supersedes: null
change_reason: null
observation_trace_id: "ctr_46c1ec0b29caf00481d778a11b59115d"
---

# Objective Contract: #435 — hardline quoted-grep parser source correction without RC16 interference

## Owner Intent

The owner authorized work on existing Aether Agents issue #435 while a separate Morfeo/Supervisor pipeline qualifies #494 RC16. The requested practical outcome is a correctly classified short, valid read-only quoted/escaped grep inspection without weakening genuinely malformed or destructive hardline protection. Conduct this as a separate reviewed source phase; do not mutate the concurrent session's worktrees, selected Hermes pin, runtime, gateways or release. The wider existing bugfix program plan already authorizes normal source/GitHub workflow, but this contract's current no-interference limit controls this phase. Issue #435 remains the objective Issue; no duplicate is needed.

## Objective

Produce a reviewed, isolated maintained-Hermes-fork source candidate that removes #435's false malformed-payload verdict for the two originally observed valid quoted/regex grep commands, preserving hardline decisions for malformed executable payloads and real protected operations. Supply deterministic RED/GREEN fixtures and a reproducible portable patch/evidence candidate in Aether. End this phase with inspectable source/review/provenance and no fork-main, Aether-main or installed-runtime adoption; final issue acceptance requires a later noncolliding integration/adoption decision and remains open.

## Decisions and Assumptions

DECIDED: Source-of-truth design is `specs/issue-435-hardline-parser/plan.md` at Aether authoring base `dc4872834a2c670c506100fd60c69960247124f2`; canonical behavior is Issue #435. Start fork work in an isolated checkout at remote `DarkArty07/aether-hermes@938bc34fbc797402ccc61d352192e122a344d052`, whose `tools/approval.py` blob is `ac8b34e44235db9421de0737e6fbabe1f13ffd89`; do not fetch/update the shared fork checkout. The two historically failed commands are represented with private path prefixes replaced by `/tmp/safe-repo`; their *shell bytes* must be verified in test fixtures, not copied via re-escaped Markdown. For quote/malformed judgement inspect syntax-preserving original shell representation; retain deobfuscated/normalized variants for all protected-operation matching, grep masking and full guard-chain decisions. A normalized-only unbalanced quote is not itself proof of malformed original input. Never broaden allowlists, bypass hardline, change approval mode or raise limits. ASSUMPTION: the raw lexer can distinguish the original valid quote structure without losing the protected threat variants; prior direct classifier RED on the unchanged blob is recorded in the existing local Objective Plan, not a new current test PASS. If the split is unsound on a protected positive control, stop and return the exact conflict rather than selecting an unsafe fallback. Implementer may choose local parser/helper structure inside these invariants; Supervisor owns review and executable unit routing.

## In Scope

In the isolated maintained-fork branch: investigate and correct the #435 parser/grep-safe variant boundary in `tools/approval.py`, add deterministic fixtures for both original valid quote/regex shapes and malformed/protected controls in existing `tests/tools/` modules or a focused new module, and qualify full guard-chain behavior. In the isolated Aether branch: prepare a reversible HLP-435 portable patch against the exact fork base, byte-for-byte reconstruction evidence and a truthful ledger/upstream-retirement candidate; no current-active claim before merge/adoption. Supervisor independently reviews the security/parser boundary, reconciles source SHA/test evidence, and may open a normal fork PR without merging it; any Aether evidence PR likewise stays unmerged during RC16. Report explicit residual integration/adoption gates.

## Out of Scope

No editing the installed Hermes tree, global/shared fork checkout or #494/RC16 worktrees; no fork `aether-main` merge, Aether `main` merge, release-lock/pin/version change, gateway or TUI restart, profile/config/approval-mode mutation, credential acquisition/widening, tag/package publication, deployment, cleanup of other worktrees, external upstream PR or closure of Issue #435 in this phase. No generic approval-parser redesign or broad grep exemption. #434 and #522 local candidates stay separate. CI on an open fork PR is not an installed-behavior claim; Actions absent means NOT RUN, not green.

## Authority

Owner controls product intent and any changed protected-effect/release authority. This handoff authorizes local reversible source/tests in isolated worktrees and normal already-provisioned Git/GitHub branch/PR publication for #435 only, under standard protected review/checks; it does not authorize merge into either default/source branch while #494 RC16 may be using exact source/ledger, nor any active runtime effect. Supervisor decomposes, reviews and owns pipeline integration/disposition; Implementer makes equivalent safe local coding choices. Existing hardline safety is non-negotiable: a genuine protected denial cannot be bypassed via another tool, broad exception, settings change or shell trick. A test runner may classify synthetic command strings but must not execute a destructive command. Recheck the exact fork remote SHA and ongoing #494 board before any remote effect; stop on collision rather than choosing a new base silently.

## Deliverables

D1: exact-base fork branch/commit with minimal parser correction and deterministic safe fixtures for both reported calls plus real malformed/protected positive controls, diff and local runner receipts. D2: independent Supervisor review at one exact fork tree, including evidence that raw syntax checking and normalized hardline detection both remain correct; an open fork PR is permissible but not a merge/CI PASS claim. D3: portable `HLP-435` patch/evidence candidate in the isolated Aether branch, hash, exact apply/reverse check and reconstructed candidate tree; a truthful proposed ledger/retirement disposition that explicitly says source candidate, not ACTIVE_LOCAL/installed. D4: handoff with source/test/provenance references, remaining #494 collision gate, release conclusions and reason Issue #435 stays OPEN. Preserve current fork `aether-main`, Aether `main`, rc15 runtime pin and all #494 state.

## Acceptance Criteria

AC1 (false-positive correction): on the exact fork base, tests for each of the two sanitized original shell byte shapes report balanced syntax/within parser limits yet return `_MALFORMED_EXEC_DESCRIPTION`; on the reviewed candidate both are no longer malformed/hardline. The full `check_all_command_guards` path must not unconditionally hardline-block these read-only strings; ordinary approval, if applicable, is not a test failure. Fixtures never execute their shell commands. AC2 (protection preservation): real unclosed quoted executable/grep payloads still fail closed; real destructive operations in ordinary, quoted, obfuscated, separator/substitution and wrapper contexts still hit hardline even in yolo; inert quoted grep/prose stays data, parser bounds and `detect_dangerous_command` behavior are retained. Check baseline and candidate, not just an allow-only test. AC3 (review and base): the fork candidate is traceable to `938bc34fbc797402ccc61d352192e122a344d052` or stops for an explicit base change; targeted and affected suites, static checks, clean diff, and independent Supervisor review report commands, revisions, outcomes and skips. Fork Actions not run are labeled NOT RUN. AC4 (portable provenance): HLP-435 patch applies/reverses on the exact clean base and reproduces the reviewed tree byte for byte; proposed Aether ledger state, upstream comparison and evidence never misstate source candidate as installed or merged. AC5 (noninterference): separate worktrees/board, no changes to fork `aether-main`, Aether `main`, selected release-lock/runtime, #494 worktrees or board. No merge, tag, activation, deployment or Issue closure is counted as success for this source-only contract.

## Testing Standard

Resolved project standard (not a default): Aether `CONTRIBUTING.md:31-96` requires focused tests during iteration, Python 3.11 as PR development lane with 3.12/3.13 smoke, full `uv run --frozen python scripts/run_tests.py` for a Hermes-baseline/release boundary, Ruff check/format-check, relevant reconciliation validation and `git diff --check`; no test weakening. Maintained fork `CONTRIBUTING.md:201-210` prefers hermetic `scripts/run_tests.sh` and `AGENTS.md` governs its local test isolation. First record deterministic RED of both #435 fixtures at exact fork base (no shell execution), then GREEN on candidate. Minimum fork candidate run: `HERMES_TEST_FILE_RETRIES=0 scripts/run_tests.sh tests/tools/test_hardline_blocklist.py tests/tools/test_execution_flag_detection.py tests/tools/test_shell_bypass_denylist.py tests/tools/test_approval.py -q`, plus any neighboring module touched by the fix; verify true malformed, destructive and whole-guard controls explicitly. Run fork static checks for changed Python and diff; report baseline-shared full-suite failures rather than calling them GREEN. For Aether HLP candidate run focused patch/reconciliation tests, exact `git apply --check` / reverse and reconstructed-tree comparison; run `uv run --frozen python scripts/run_tests.py`, Ruff check/format-check and `git diff --check` before any Aether PR when patch ledger/manifest is touched. `scripts/validate_hermes_patch_reconciliation.py --check --fork-checkout <clean exact revision>` is required only after its candidate inputs exist; do not represent an unchanged aggregate check as qualification of HLP-435. No real gateway, provider, live board, browser or model canary is required or authorized in this source phase; live adoption is separately gated.

## Stop Conditions

Stop and preserve evidence if exact remote fork base/blob or project binding changes; if the original byte fixtures do not produce both base REDs; if a proposed GREEN weakens any malformed/destructive/obfuscation/full-guard positive control; if source/ledger/manifest paths collide with live #494 RC16 work; if required review or test evidence is unavailable rather than silently waiving it; or if work requires a protected remote mutation, credential, settings change, runtime activation, merged fork pin or unrelated issue. Diagnose one bounded correction under the same accepted objective when review finds a local defect; do not restart failure budgets by creating a new card/contract. A candidate-only result is an incomplete Issue #435, not license for repeated retries or a source-only closure claim.

## Canonical References

Owner/current scope: issue `https://github.com/DarkArty07/Aether-Agents/issues/435` and owner's 2026-09-25 instruction to work without interfering with #494 RC16. Project identity `.aether/project.toml` UUID `12027989-a08f-41cd-a82c-54ff1bfb6b03`; project governance `AGENTS.md`, `DESIGN.md`, `specs/r10-security-and-authority/spec.md`. Material design/evidence: `specs/issue-435-hardline-parser/plan.md` in this authoring branch. Procedure/test owners: `CONTRIBUTING.md:31-96`, fork `AGENTS.md`, fork `CONTRIBUTING.md:201-210`, fork `tools/approval.py`, `tests/tools/test_hardline_blocklist.py`, `tests/tools/test_execution_flag_detection.py`, `tests/tools/test_approval.py`, `HERMES_LOCAL_PATCHES.md` and `scripts/validate_hermes_patch_reconciliation.py`. The ignored `.aether/plans/2026-09-23_145339-existing-behavior-bugfix-program.md` is Morfeo continuity only and cannot be required in the worker workspace; private historical log paths never enter public artifacts. Exact current fork URL/revision, observed blob and upstream comparisons are in the design file, not inferred from installed runtime. Examples in the design are examples, not new universally allowed command classes.
