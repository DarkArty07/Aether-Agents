---
artifact_type: "aether.objective-contract.v1"
project_id: "12027989-a08f-41cd-a82c-54ff1bfb6b03"
contract_id: "oc_f8c9fc9320587cf3"
version: 5
status: "final"
title: "Telegram Monitor: autonomous hourly progress across projects and sessions"
created_at_utc: "2026-09-11T22:37:41Z"
created_at_local: "2026-09-11T16:37:41-06:00"
finalized_at_utc: "2026-09-11T22:43:27Z"
finalized_at_local: "2026-09-11T16:43:27-06:00"
author_profile: "morfeo"
created_in_session: "20260909_020805_b428d8"
finalized_in_session: "20260909_020805_b428d8"
supersedes: "oc_f8c9fc9320587cf3@v4"
change_reason: "Post-merge Morfeo reception found the natural production report contained zero work items while valid reportable work existed. Fix the source-binding defect by resolving each canonical board contract and project marker from its validated worktree_base_ref Git object before merge, preserving fail-closed identity and read-only behavior."
observation_trace_id: "ctr_d69d0470f951729c18a2650216f323b2"
---

# Objective Contract: Telegram Monitor: autonomous hourly progress across projects and sessions

## Owner Intent

Deliver the principal Telegram Monitor defined by TM-001..007, using one hourly production cron and the existing Telegram conversation with project/origin/contract identity first. The owner continues to authorize autonomous documentation, implementation, testing, integration and scoped activation using provisioned access. After the material live-scope contradiction was demonstrated, the owner approved Morfeo's presented isolated-laboratory design and explicitly requested renewed Supervisor handoff. This revision changes the qualification isolation method and its narrowly bounded authority, not the product outcome, existing credential restrictions or unrelated policy.

## Objective

Preserve the merged and activated Telegram Monitor, repair the one production acceptance failure found by Morfeo reception, re-enable it and obtain a report with at least one correctly attributed real work item while reportable work exists. Do not rebuild the monitor or repeat the laboratory.

## Decisions and Assumptions

D1..D17 and all accepted production behavior remain. D18 is the only new material decision: when canonical board metadata carries a validated `worktree_base_ref`, resolve the project marker and finalized contract bytes from that exact Git object in the registered repository. Validate marker UUID and contract identity/version/status/session fields against the board before binding. Use a non-shell argument array or equivalent read-only Git object API. Never use task workspace paths, mutable uncommitted files, current branch, recency or another board as authority. Rejected legacy sources remain labeled but cannot suppress a separately valid board.

## In Scope

Modify only the read-only source binding and focused tests/documentation/evidence needed for D18; integrate normally; update the already provisioned runtime to the reviewed merged correction; re-enable the preserved job; run one immediate native production canary while real reportable work exists; require at least one correctly attributed item, confirmed Telegram delivery, preserved schedule and next due; reconcile #367 and the existing rollback.

## Out of Scope

No new laboratory, scheduler, model/provider/destination/credential, percentage engine, registry rewrite, board/session mutation, cleanup of legacy sources, suppression of coverage gaps, mutable-worktree authority, shell interpolation, unrelated bug repair, package publication, tag or release.

## Authority

The owner's current instruction requires autonomous completion without leaving the functionality incomplete. This authorizes the D18 source repair, ordinary tests/review/GitHub closeout, scoped runtime update, monitor off/on, gateway restart only if required during a verified zero-worker window, and one real canary through the already provisioned model/Telegram route. No authority to widen credentials, change providers/models/destination, bypass checks or mutate unrelated work.

## Deliverables

One focused reviewed D18 correction and evidence; merged default-branch revision; provisioned runtime updated to that correction; monitor re-enabled with the existing job/destination/schedule; a production snapshot containing one or more real work items correctly attributed to portable project, origin session and contract/version while current reportable work exists; confirmed Telegram receipt; issue #367 closed only after Morfeo reception; residual unrelated source gaps visible; release_impact patch, release_action defer, release_channel none unless evidence requires otherwise.

## Acceptance Criteria

All prior material criteria remain satisfied by existing evidence except production attribution. D18 acceptance additionally requires: a contract existing only at a valid board `worktree_base_ref` is loaded from that Git object and yields a BoardBinding/work item; marker or contract mismatch, unreachable ref, invalid ref and mutable-worktree-only content fail closed; unrelated invalid boards contribute gaps without erasing the valid item; current real active Aether work produces at least one correctly attributed production snapshot item and confirmed report; zero-item service notice does not pass; source databases and Git worktrees remain unmodified.

## Testing Standard

Use focused source tests with a real temporary Git repository and canonical board metadata, fail-before/pass-after and negative controls; existing monitor source/collector/runtime/reporting suites; exact repository static/docs/package gates; baseline comparison for unrelated failures. Final canary uses the existing active Aether board whose contract is present only at its board base ref and must verify item count >=1, exact project/session/contract identity, Telegram confirmation, `0 * * * *`, next due and no source mutation. No new live laboratory.

## Stop Conditions

Stop and preserve the paused job on unresolved Git-object identity, absent validated base ref for the target canonical board, source mutation, a protected-edge denial, inability to update/restart without interrupting workers, objective-caused regression, zero attributed items while reportable work still exists, or unconfirmed production delivery. Do not close #367 or remove rollback evidence on stop.

## Canonical References

Exact project/repository remain `.aether/project.toml` UUID `12027989-a08f-41cd-a82c-54ff1bfb6b03` and `DarkArty07/Aether-Agents`. Owning artifacts: `specs/telegram-monitor/spec.md` D18, `plan.md` source design, `quickstart.md` D18 checks, `evidence/authoring.md` reception finding, merged PR #405/main `42bf4b9`, production report `rpt_26ad1a292db123d939ee37720c7075c8`, issue #367, and the active canonical board whose validated `worktree_base_ref` is the test oracle. Immutable v1-v4 remain preserved.
