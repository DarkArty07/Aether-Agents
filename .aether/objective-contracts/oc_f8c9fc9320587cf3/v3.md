---
artifact_type: "aether.objective-contract.v1"
project_id: "12027989-a08f-41cd-a82c-54ff1bfb6b03"
contract_id: "oc_f8c9fc9320587cf3"
version: 3
status: "final"
title: "Telegram Monitor: autonomous hourly progress across projects and sessions"
created_at_utc: "2026-09-11T02:22:19Z"
created_at_local: "2026-09-10T20:22:19-06:00"
finalized_at_utc: "2026-09-11T02:26:40Z"
finalized_at_local: "2026-09-10T20:26:40-06:00"
author_profile: "morfeo"
created_in_session: "20260909_020805_b428d8"
finalized_in_session: "20260909_020805_b428d8"
supersedes: "oc_f8c9fc9320587cf3@v2"
change_reason: "Resolve the live preflight interface gap returned by Supervisor after integrated deterministic verification: normalize both supported HERMES_HOME conventions to the exact Morfeo profile home, derive the provisioned reference destination through Hermes' native dotenv loader without persisting or emitting credentials, compare the lab's ephemeral in-memory access against that reference, and authorize one corrected live attempt after review because the preserved refusal occurred before the experiment budget boundary."
observation_trace_id: "ctr_d2df0c88edfb0f7c02e16726c3a7e580"
---

# Objective Contract: Telegram Monitor: autonomous hourly progress across projects and sessions

## Owner Intent

Deliver the principal Telegram Monitor defined by TM-001..007, using one hourly production cron and the existing Telegram conversation with project/origin/contract identity first. The owner continues to authorize autonomous documentation, implementation, testing, integration and scoped activation using provisioned access. After the material live-scope contradiction was demonstrated, the owner approved Morfeo's presented isolated-laboratory design and explicitly requested renewed Supervisor handoff. This revision changes the qualification isolation method and its narrowly bounded authority, not the product outcome, existing credential restrictions or unrelated policy.

## Objective

Finish the working, independently verified Telegram Monitor without discarding approved MON-01..05 or the reviewed MON-05-R1 correction. Resolve unfinished qualification through D13's native-runtime laboratory and verify the same candidate's actual installation-wide production gateway execution before activation acceptance and normal GitHub closeout. Root Supervisor scope is verified executable continuation/decomposition, not product implementation or self-review; preserve prior commits/evidence and do not duplicate already accepted outcomes. Native CLI/TUI controls, durable reports/receipts, direct and pipeline attribution, manual-off priority, preservation and rollback remain required.

## Decisions and Assumptions

D1..D13 remain as finalized in v2. D14 resolves the terminal live-preflight interface: qualification accepts `HERMES_HOME` as either the installation multi-profile root or the exact `profiles/morfeo` home and normalizes both to one verified Morfeo profile, rejecting ambiguity/conflict and never using cwd/current worker identity. The provisioned reference destination is resolved in a restricted child rooted at that profile by invoking Hermes' native `hermes_cli.env_loader.load_hermes_dotenv()` before `gateway.config.load_gateway_config()`; it returns only bounded target/configuration digests and presence flags and makes no model/send call. The lab independently receives only already-provisioned access in ephemeral memory and must digest-match the reference. Injecting lab access into the reference probe is rejected because it would compare the same injection to itself. The preserved initial `lab-config` refusal occurred before lab creation, credential loading, model/sender effects or the experiment attempt boundary, so the live budget remains one corrected attempt after implementation and independent review. Any later live failure stops normally. Assumptions are supported by the inspected profile-scoped gateway and native dotenv/config interfaces; no profile, credential, provider or model mutation is assumed.

## In Scope

Complete TM-001..007, D1..D14 and AC-1..9 while preserving every independently approved unit and integrated deterministic result. Add only the bounded D14 harness/profile-resolution, provisioned-reference dotenv/config probe, lab-side independent comparison, regression/documentation/evidence updates and the existing final integration/live/activation/closeout work. Preserve the integrated candidate HEAD `19dfc9d68e797f7d365f078ef65be70fbd16f158` as input rather than rebuilding it. Correct profile/destination preflight through a separately reviewed implementation unit, then allow exactly one corrected live attempt with a new private no-overwrite target; if successful continue production activation and closeout. Record the prior refusal as failed preflight evidence, not qualification success. Update exact allowlists/docs only as mechanically required. No other product behavior is added.

## Out of Scope

No new scheduler implementation, permanent daemon, Telegram receiver/bot/topic/destination, product per-job namespace/scope selector, cross-machine collector, frontend redesign, inbound work control, percentage/ETA/time-billing engine, provider/model rebinding, credential acquisition/widening or persisted credential copies. D13 permits only the bounded isolated native scheduler instance, not a second production service. No production registry replacement/restoration/cleanup, source DB rewrite, forced active-worker interruption, SOUL edits, unrelated runtime repair, publication/deployment, destructive cleanup or history/check bypass. Preserve separately approved current role/tool guidance when integrating old unit commits; do not reset it to the unit's stale base. Immutable v1 remains unchanged.

## Authority

The owner's continuing autonomous objective plus D13 approval authorizes Morfeo to resolve this returned material interface decision and issue v3. D14 authorizes read-only use of the exact already-provisioned Morfeo profile and its native dotenv/config loader, ephemeral in-memory reuse of existing access by lab children, one corrected live qualification attempt after independent review, and subsequent previously authorized production activation/closeout on success. It does not authorize exposing/copying/persisting credential values, accepting secret/destination/model/profile overrides, changing profiles/config/providers/models/router/settings, acquiring/refreshing access, starting another receiver/service/dispatcher, patching Hermes, bypassing gates, modifying the production registry, interrupting agents, force/history rewrite, package publication or deployment. Supervisor may integrate/close normally and choose reversible implementation details only within D14; material behavior/interface changes return to Morfeo.

## Deliverables

Preserved accepted monitor implementation plus the completed isolated qualification harness and matching test/CLI/package/documentation surfaces; independent review and current integrated verification; a private laboratory receipt with real model/Telegram/hourly/idle evidence and scheduler shutdown; separate actual production gateway first-delivery/activation/rollback evidence; sanitized AC mapping in specs/telegram-monitor/evidence; normal PR/check/merge/#367 and residue inventory. The accepted revisions and the unapproved MON-06 candidate are listed in qualification-isolation.md with their actual evidence locations. Do not present source inspection, document tests, reused prior reviews or lab qualification as fresh full product acceptance. Keep all live handles, private paths, source content and credentials outside public artifacts. Supervisor provides the executable remaining breakdown, not Morfeo implementation units.

## Acceptance Criteria

All AC-1..9 remain mandatory. Additional D14 acceptance: both supported `HERMES_HOME` shapes normalize to the same exact Morfeo profile; missing/linked/ambiguous/conflicting/wrong-profile paths refuse before credential/model/send/lab effects; the provisioned reference probe uses the native dotenv loader then gateway config and emits only bounded non-secret digests/presence; a negative control proves lab access is not injected into the reference side; lab target/route/config exactly match the provisioned reference before spending; no secret appears in stdout/stderr/receipt/lab files/public artifacts; the preserved refusal remains unchanged and no-overwrite; exactly one corrected live attempt uses a new output target only after independent review. The complete acceptance still requires one smoke, two natural active cuts, one natural idle cut with zero narration/send, cooperative stop, separate production first hourly delivery/ack, rollback, green PR/merge/#367 closeout and residue audit. A preflight pass alone is not live qualification.

## Testing Standard

Retain v2's regression/deterministic/exact-Hermes/full/static/docs/coverage/package/public-artifact and live testing standard. Add D14 focused tests for both HERMES_HOME forms; missing/wrong/ambiguous/symlink profile rejection; native dotenv-loader-before-gateway-config call order; no access-value output/persistence; target digest equality; lab/reference independence; inherited current-worker selector rejection; and unchanged prior receipt. Run the new tests first against a reproduction of the broken preflight and then the corrected candidate. Independent Supervisor review must inspect the actual environment passed to each child without printing values. Re-run integrated gates proportionately, preserving baseline comparison for unrelated failures. Only after review run one corrected live command with the existing `--live --wait-hourly-boundaries 2 --output NEW_PRIVATE_TARGET --json`; no manual cuts or fabricated receipts. The first external-effect/live failure after corrected preflight stops. Production verification remains separate and mandatory.

## Stop Conditions

Stop before another live attempt on unresolved/ambiguous Morfeo profile identity, inability to load the existing profile dotenv through the supported native loader, destination/route/config digest mismatch, any credential value in output/files/receipts, candidate/native artifact drift, unsafe output/lab paths, unavailable scheduler/sender/model route, protected-edge denial, or inability to preserve active agents/shared state. The preserved preflight refusal is not itself a consumed experiment attempt; exactly one corrected attempt remains after reviewed D14 implementation. Any failure after the corrected preflight begins external-effect qualification stops with durable private evidence and no automatic rerun. Do not choose injection into the reference probe, profile/config mutation, alternate destination/provider/model, credential acquisition, Hermes patch or manual scheduler simulation as a workaround. Preserve integrated HEAD and issue #367 open on failure.

## Canonical References

Exact project/repository remain `.aether/project.toml` UUID `12027989-a08f-41cd-a82c-54ff1bfb6b03` and `DarkArty07/Aether-Agents`. Owning artifacts are `specs/telegram-monitor/spec.md` (D14), `plan.md`, `quickstart.md`, `qualification-isolation.md`, `research.md`, `tasks.md`, evidence directory, issue #367 and this Objective Contract v3. V1/v2 are immutable preserved predecessors. Integrated continuation input is `19dfc9d68e797f7d365f078ef65be70fbd16f158` with deterministic verification recorded on v2 card `t_e7a3aafb`; approved MON-01..05, MON-05-R1, MON-06R and MON-LC1 commits/evidence remain reusable after exact verification. Native interfaces inspected/required: `hermes_cli.env_loader.load_hermes_dotenv`, `gateway.config.load_gateway_config`, Hermes profile-path conventions, `cron.scheduler_provider.InProcessCronScheduler`, monitor runtime/delivery adapters. GitHub issues #390/#396/#397 and existing unrelated failures remain separately owned and are not silently folded into D14.
