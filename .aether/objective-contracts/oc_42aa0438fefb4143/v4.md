---
artifact_type: "aether.objective-contract.v1"
project_id: "12027989-a08f-41cd-a82c-54ff1bfb6b03"
contract_id: "oc_42aa0438fefb4143"
version: 4
status: "final"
title: "Milestone 1: finish single-catch-up watch behavior in PR #472"
created_at_utc: "2026-09-19T05:37:03Z"
created_at_local: "2026-09-18T23:37:03-06:00"
finalized_at_utc: "2026-09-19T05:43:18Z"
finalized_at_local: "2026-09-18T23:43:18-06:00"
author_profile: "morfeo"
created_in_session: "20260918_153924_3f5e2c"
finalized_in_session: "20260918_153924_3f5e2c"
supersedes: "oc_42aa0438fefb4143@v3"
change_reason: "Protected CI at milestone-3 head 5d4c9e78 exposed issue #476: a semantics test races the supervised flusher and can return JOURNAL_BUSY. Milestone 3a makes that test deterministic without changing product behavior, then requalifies PR #471 on a new SHA."
observation_trace_id: "ctr_2931bf7c77d455c55e0f31e0508d55e6"
---

# Objective Contract: Milestone 1: finish single-catch-up watch behavior in PR #472

## Owner Intent

Repair only issue #476 on PR #471 by removing test-only flusher scheduling nondeterminism, then obtain fresh protected CI and independent review on a new SHA. Preserve the red v3 evidence and keep `main` and runtime unchanged.

## Objective

Update `test_product_checkpoint_can_classify_a_required_unit_without_native_inference` to disable the supervised Flusher only for that authority/classification test, following the existing pattern in `test_observation_journal_storage.py`. Prove deterministic pass on Python 3.11, preserve real JOURNAL_BUSY coverage, push to #471, and obtain fresh green CI/review.

## Decisions and Assumptions

Do not change Collector, JournalWriter, Flusher, lock budgets or checkpoint behavior. In the target test, monkeypatch `aether_agents.observation.capture.collector.Flusher.start` to a no-op before either collector starts, with a comment that the test proves authority/classification, not flusher scheduling. Existing explicit fsync/JOURNAL_BUSY tests must remain unchanged and pass.

## In Scope

One targeted test edit, v4 contract/policy registration, deterministic stress verification, affected checkpoint/journal suites, protected CI on the new #471 head, independent read-only review in a dedicated no-affinity card, and factual #476/#417 evidence.

## Out of Scope

Product source changes; weakened lock/fail-open behavior; deleting or loosening JOURNAL_BUSY tests; merging #471 to main; rerunning/overwriting the failed v3 evidence; runtime, gateway, profile, router, credential, tag, release or deployment mutation; #473/#474/#475 repair and unrelated cleanup.

## Authority

Owner authorization covers autonomous source/PR/CI correction. Implementer may edit only the named test and run isolated local tests. Supervisor may register v4, push the reviewed fix to existing PR #471 and coordinate CI/review. No merge to main or runtime/external release effect is authorized in v4.

## Deliverables

1. Test-only fix commit. 2. Repeated Python-3.11 target-test evidence and affected-suite results. 3. Fresh #471 head with v4 contract/policy and all seven protected checks green. 4. Independent review approval. 5. Durable #476/#417 summary, with v3 red run preserved.

## Acceptance Criteria

AC-1: source delta outside v4 contract/policy is only `tests/test_observation_contracts.py`; product code is byte-identical to 5d4c9e78. AC-2: target test passes 50 consecutive isolated Python-3.11 runs; the explicit active-fsync/JOURNAL_BUSY test and checkpoint/journal suites pass. AC-3: all seven protected checks succeed at the exact new #471 SHA. AC-4: independent review confirms the monkeypatch is test-scoped, follows repository precedent and does not weaken contention coverage. AC-5: #471 remains open/draft; main, failed v3 run, runtime, tags and releases are unchanged.

## Testing Standard

Use an isolated Python 3.11 environment and exact locked dependencies. Run the target test 50 sequential times, the explicit `test_checkpoint_sink_fails_open_while_active_fsync_holds_writer_lock`, full `tests/test_observation_contracts.py` and `tests/test_observation_journal_storage.py`, Ruff/format and `git diff --check`. Final evidence is all seven protected GitHub checks at the exact new head; no rerun of the old red SHA counts.

## Stop Conditions

Stop on any required product-code/lock-budget change, inability to reproduce/pass deterministically under Python 3.11, failure or modification of real JOURNAL_BUSY coverage, red/cancelled protected check at the new SHA, material independent-review finding, force/rewrite requirement, or any main/runtime/tag/release/deployment mutation. Use a dedicated no-affinity review card to avoid #475; stop before main.

## Canonical References

Issue #476 and comment `issuecomment-5739697738`; failed run `35423137009`, job `105844331877`, SHA `5d4c9e78a9e629c6abc0622bc5b9305d18038e2d`; target test `tests/test_observation_contracts.py:1632-1673`; accepted flusher-isolation precedent `tests/test_observation_journal_storage.py:1290-1296`; real contention test `:1399+`; PR #471; main `30731a1975274a5a20a2648bb56ba19cf9bf55a8`; v3 contract and issue #475 evidence.
