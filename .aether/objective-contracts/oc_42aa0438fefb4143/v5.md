---
artifact_type: "aether.objective-contract.v1"
project_id: "12027989-a08f-41cd-a82c-54ff1bfb6b03"
contract_id: "oc_42aa0438fefb4143"
version: 5
status: "final"
title: "Milestone 1: finish single-catch-up watch behavior in PR #472"
created_at_utc: "2026-09-19T06:31:50Z"
created_at_local: "2026-09-19T00:31:50-06:00"
finalized_at_utc: "2026-09-19T06:37:03Z"
finalized_at_local: "2026-09-19T00:37:03-06:00"
author_profile: "morfeo"
created_in_session: "20260918_153924_3f5e2c"
finalized_in_session: "20260918_153924_3f5e2c"
supersedes: "oc_42aa0438fefb4143@v4"
change_reason: "Protected CI at 112ec835 exposed issue #477: the reader-fence oracle can miss tight scheduling waits under runner load. Milestone 3b makes the test synchronize on the real LOCK_EX attempt and requalifies PR #471 without product-code changes."
observation_trace_id: "ctr_99a6dfcfe066e522badc8685472ddd59"
---

# Objective Contract: Milestone 1: finish single-catch-up watch behavior in PR #472

## Owner Intent

Repair only issue #477 on PR #471 by replacing scheduler-dependent reader-fence timing with deterministic synchronization on the real LOCK_EX attempt, then obtain fresh protected CI and independent review on a new SHA. Keep `main` and runtime unchanged.

## Objective

Modify only `test_rebuild_waits_for_open_reader_before_publishing_candidate` so the main thread waits until the rebuild worker has actually invoked the real POSIX `flock(LOCK_EX)` while the reader lease is open, then releases the reader and verifies the real exclusive-lock probe and rebuild completion.

## Decisions and Assumptions

Test wrapper must delegate every flock call to the original function and signal only the rebuild worker's LOCK_EX attempt. That signal, not elapsed time, proves the worker reached the blocking fence while the reader was open. Keep the real non-blocking shared-lock probe and pre-release non-completion assertions. A single generous 30-second timeout is only a deadlock safety cap, not semantic evidence. Product code and lock budgets remain byte-identical.

## In Scope

One targeted test edit, v5 contract/policy registration, 100 fresh target repetitions, affected journal-storage/core qualification, fresh seven-check protected CI on #471, separate no-affinity review, and factual #477/#417 evidence.

## Out of Scope

Product source, projection/lease implementation, lock budgets, weakened assertions, removal of the real flock probe, mere unchanged-SHA rerun, merging main, runtime/gateway/profile/router changes, tags, releases, deployment and unrelated issues.

## Authority

Owner autonomy covers source/PR/CI correction. Implementer may edit only the named test. Supervisor may register v5, push the reviewed change to existing PR #471 and observe CI. No role may merge main or mutate runtime, services, profiles, router, tags or releases in this milestone.

## Deliverables

1. Test-only reader-fence oracle commit. 2. Repetition and affected-suite evidence. 3. Fresh #471 head with v5 contract/policy and seven green checks. 4. Independent no-affinity approval. 5. #477/#417 summary preserving failed run 35425434843 and stopping before main.

## Acceptance Criteria

AC-1: source delta outside v5 contract/policy is only `tests/test_observation_journal_storage.py`; `src/`, scripts and dependencies are byte-identical to 112ec835. AC-2: target oracle observes the worker's real LOCK_EX attempt before releasing the reader and retains the real exclusivity probe. AC-3: target passes 100 consecutive isolated Python-3.12 runs and affected journal/core suites pass. AC-4: all seven protected checks succeed at the exact new #471 head without rerunning failed jobs. AC-5: independent no-affinity review has no unresolved material finding. AC-6: main remains 30731a19 and runtime remains unchanged/ready.

## Testing Standard

Use isolated Python 3.12 with locked deps. Run the target 100 sequential times, the journal-storage file, the locked 472-node core qualification, Ruff/format and diff checks. Then push one new #471 SHA and require the seven protected checks to complete SUCCESS on attempt 1; local tests never substitute for GitHub CI.

## Stop Conditions

Stop on any product-code need, inability to observe real LOCK_EX deterministically, target repetition failure, weakened probe/assertion, red/cancelled protected check, material review finding, branch drift/force requirement or any runtime/main/tag/release effect. Do not use rerun-until-green; a red new SHA becomes new evidence.

## Canonical References

Issue #477 and design comment `issuecomment-5739943669`; prior issue #329 and PR #370, especially commits `6960dc69`/`ac9423d9`; failed run `35425434843`, job `105850434336`, SHA `112ec8351aef1cc3b87cb4011428234954d60fa0`; target `tests/test_observation_journal_storage.py:2307-2373`; current main `30731a1975274a5a20a2648bb56ba19cf9bf55a8`; AGENTS.md.
