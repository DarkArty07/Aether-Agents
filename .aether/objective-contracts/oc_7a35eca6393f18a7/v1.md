---
artifact_type: "aether.objective-contract.v1"
project_id: "12027989-a08f-41cd-a82c-54ff1bfb6b03"
contract_id: "oc_7a35eca6393f18a7"
version: 1
status: "final"
title: "Autonomous follow-up: Aether handoff integrity, goal-mode recovery and qualification bugs"
created_at_utc: "2026-09-10T05:52:47Z"
created_at_local: "2026-09-09T23:52:47-06:00"
finalized_at_utc: "2026-09-10T06:14:11Z"
finalized_at_local: "2026-09-10T00:14:11-06:00"
author_profile: "morfeo"
created_in_session: "20260909_130929_38f448"
finalized_in_session: "20260909_130929_38f448"
supersedes: null
change_reason: null
observation_trace_id: "ctr_d9e5b384f9a99cb1aae5503df646a5ce"
---

# Objective Contract: Autonomous follow-up: Aether handoff integrity, goal-mode recovery and qualification bugs

## Owner Intent

The owner authorized Morfeo to continue autonomous Aether Agents bug remediation without requiring per-issue confirmation, while explicitly excluding issue #275 (vision_analyze provider/backend HTTP 400). This contract covers the remaining open bugs #354, #369, #357 and #352 and permits normal bounded implementation, independent review, integration, issue reconciliation and Telegram progress reporting within the stated project boundaries.

## Objective

Remediate and close, or provide an evidence-backed non-reproducible/external disposition for, the four currently open bug issues other than #275:

- P0 #354: Objective Contract handoff can provision Supervisor and descendant worktrees from the moving Project primary checkout instead of the recorded contract base, omitting the exact finalized artifact or importing unrelated lineage.
- P0 #369: goal-mode review handoff can demand an already-accepted independent verdict before entering review, and the recovery controller can request a block kind rejected by the goal-mode allowlist.
- P1 #357: the real PluginContext qualification harness can fail after merge on Python 3.13 while local/retry execution passes, with the current runner preserving only insufficient redacted diagnostics.
- P1 #352: concurrent lifecycle qualification can time out waiting for child results; determine whether this is product synchronization or fixture/environment nondeterminism without weakening deadlines or assertions.

Start with #354 and #369 because they threaten handoff/review authority and autonomous recovery, then process #357 and #352 in parallel or sequence according to verified file ownership and hotspots. The terminal outcome must reconcile #371 and the four source issues honestly; #275 remains open and untouched.

## Decisions and Assumptions

1. This is a pipeline objective: the four bugs cross Aether orchestration, Hermes runtime/goal mode, CI qualification and lifecycle concurrency; Supervisor must decompose them into bounded units with independent review and terminal integration. Morfeo does not implement product units or create child units.
2. Priority is risk-based: #354 and #369 first; #357 and #352 next. Parallelism is allowed only where exclusive writable surfaces and dependency gates are explicit.
3. #354 must be fixed at the source of Project/base/worktree selection, not by moving branches, rewriting history, mutating the shared primary checkout, raw database edits or creating duplicate Projects. The implementation must preserve exact project identity, contract digest, recorded base and descendant ancestry when another flow owns the primary checkout.
4. #369 must preserve genuine independent review and completion gates while removing the circular prerequisite for entering review. Recovery must preserve authoritative escalation semantics; no allowed-kind widening may turn a protected denial or incomplete work into success.
5. #357 must emit a privacy-preserving, structured failure classification/state sufficient to diagnose the failing PluginContext harness. No secret-bearing or raw-transcript logging is authorized. A retry alone is recovery evidence, not a fix.
6. #352 must be investigated on unchanged baseline and candidate with product-vs-fixture attribution. Do not increase deadlines, weaken commit/result assertions, skip the test, or close it as fixed without a causal reproduction. If no product defect is demonstrated, retain an honest non-reproducible disposition.
7. The current Aether checkout is at HEAD da7bcef5854763bf5b70a5540f707722c158dfbb while origin/main is 89c372fa6af09c6cbd5ce9a18dc7dd78ed70dfc4; this local primary has pre-existing unpushed work and is not to be published wholesale. Supervisor must verify the provisioned project base and reconcile normally.
8. The maintained Hermes fork currently resolves to 8a6b33ae480373015178b80e87c88fe0abda3919 at aether-main; re-resolve the actually imported source before editing and prefer upstream comparison before a fork repair.
9. Telegram progress is an operational projection only: report durable cards/results/blockers, distinguish liveness from semantic progress, redact private data, and remove the objective monitor at terminal closure.

## In Scope

Authorized implementation scope:

- DarkArty07/Aether-Agents tracked source, tests, workflows and evidence needed for #354, #357 and #352.
- The already provisioned maintained Hermes fork `DarkArty07/aether-hermes` only for the demonstrated #369 runtime/goal-mode source, with upstream comparison and no in-place mutation of its protected base branch.
- Tests, sanitized diagnostic/reporting paths, fixtures and small documentation/evidence updates directly required to prove these four bugs.
- Normal project-linked worktrees, Conventional Commits, independent review, PR/check/merge and issue reconciliation for this objective.

Issue-specific expected behavior:

#354: `prepare_handoff` and subsequent project-linked worktrees use the contract-recorded base/lineage and exact artifact bytes even while the Project primary checkout is owned by another flow; descendant units inherit the correct lineage; unrelated refs remain preserved.

#369: an otherwise complete unit can enter independent review without requiring the verdict it is asking the reviewer to produce; same-card reviewer independence remains enforced; goal-mode recovery accepts or correctly translates the controller's authoritative recovery signal without bypassing genuine gates.

#357: the exact real PluginContext qualification failure is diagnosable through privacy-safe structured state, and the corrected source/harness passes the exact post-merge qualification on Python 3.13 while preserving 3.11/3.12 and policy gates.

#352: the timeout is either corrected at its demonstrated product synchronization cause with a regression, or retained open with exact evidence that only fixture/environment nondeterminism was demonstrated.

## Out of Scope

Out of scope:

- Issue #275 and any provider/backend credential, model, quota, paid-traffic or router investigation associated with it.
- Features, documentation objectives, proposals, observations and release objectives including #367, #368, #348, #334, #317, #316, #274, #261 and #251.
- Any open or newly created bug not #354/#369/#357/#352 unless it directly blocks the current objective; non-blocking defects must be reported separately and not fixed here.
- Live runtime/profile/home edits, activation, service restart, deployment, settings/protection changes, credential acquisition/widening, force operations, history rewrite, test skipping, timeout inflation, check bypass, raw board-database mutation, raw private logging or destructive cleanup.
- Publishing a stable/prerelease, tags, packages or releases; compatibility impact, release action and channel must be classified independently at closeout.
- Closing an issue because a retry passed, because the symptom did not occur once, or because diagnostics were suppressed.

## Authority

Authority order is the owner's current instruction (resolve all current Aether bugs except #275) followed by the accepted R0 constitution, DESIGN.md, applicable specifications, this finalized Objective Contract, root AGENTS.md and repository procedures. The owner authorizes autonomous prioritization and routine repository/GitHub closeout for these four bugs, including normal commits, branches, pushes, PRs, required checks, merges, issue comments/closure and objective-owned cleanup. This does not authorize credentials/providers/expense, deployment, service activation, runtime-profile mutation, settings/protection changes, force/no-verify/admin bypass, history rewrite, destructive cleanup or #275 work.

Morfeo owns objective intent, contract and priority. Supervisor owns verified decomposition, unit coverage, integration order and terminal board closeout. Implementers own only explicitly assigned units and local implementation freedom inside stated writable surfaces. Independent reviewers own acceptance verdicts and may not accept their own implementation. The maintained Hermes fork is edited only when current source evidence locates #369 there; upstream is inspected first, but upstream state does not override the owner-approved maintained fork strategy. A protected-edge denial is authoritative; an unexpected denial of ordinary local/reversible work triggers bounded recovery, not bypass.

## Deliverables

1. Finalized project-bound Objective Contract and one short Supervisor handoff envelope.
2. Supervisor breakdown covering exactly #354, #369, #357 and #352, with explicit ownership, dependencies, review lanes and terminal integration; no implementation unit for #275.
3. For each issue: baseline identity, minimal reproduction/diagnostic, causal classification, smallest in-scope implementation if warranted, regression tests, focused/affected/full verification and independent review evidence.
4. #354 clean concurrent-flow canary covering root and descendant worktree ancestry, exact contract artifact/digest and absence of unrelated primary-checkout lineage.
5. #369 same-card independent-review and recovery/triage canaries demonstrating no circular gate, no self-approval, no incomplete-work bypass and correct origin recovery signaling.
6. #357 privacy-safe diagnostic evidence naming the exact failing state/assertion class plus repeated Python 3.13 qualification and post-merge CI evidence; retain the issue if no causal repair is proven.
7. #352 deterministic concurrency/interleaving evidence or an explicit retained-open disposition; no deadline/assertion weakening.
8. Integration preserving accepted unit commits, normal PR/check/merge lifecycle in Aether and, if needed, the maintained fork; reconcile #371 and each source issue from real terminal evidence.
9. Final report containing issue dispositions, tests/checks, compatibility/release conclusions, activation status, blockers/limits and objective-owned branch/worktree/process/monitor cleanup audit.
10. Telegram progress reporting driven only by durable board changes and removed at terminal closure.

## Acceptance Criteria

Acceptance requires all of the following:

1. The task graph covers #354/#369/#357/#352 exactly and explicitly excludes #275.
2. #354 is closed only after a real/disposable concurrent-flow canary shows: contract finalization/handoff do not mutate or depend on the moving primary checkout; the Supervisor root begins at the recorded contract base or a verified descendant containing the exact artifact; descendant unit worktrees inherit the root lineage; unrelated concurrent commits are absent; existing refs and work remain preserved.
3. #369 is closed only after exercising the actual goal-mode path: a complete implementation requests independent review successfully; the reviewer—not the implementer—accepts or requests changes; incomplete work remains rejected; recovery signal/kind is accepted or deterministically translated and reaches the exact origin without raw DB edits, self-completion or permission widening.
4. #357 is closed only with an identified causal defect and regression whose exact Python 3.13 qualification passes repeatedly and whose post-merge job passes on the merged candidate. Privacy-safe diagnostics must make any recurrence actionable. A green retry without causal repair leaves it open.
5. #352 is closed only with a demonstrated causal product/fixture defect and regression; repeated unchanged-source runs plus deterministic interleaving cover result collection and one-commit invariants. If no causal defect can be reproduced, keep it open with exact evidence and next boundary.
6. Every implementation diff stays within its assigned scope, passes `git diff --check`, focused and affected tests, static/documentation gates, and the repository's full canonical runner. Known unrelated failures are preserved and attributed, not hidden.
7. Aether and fork PRs, when applicable, follow normal green merge paths. Required checks pass; non-required failures are named. No branch is called integrated before remote merge evidence.
8. Issues close only after merged evidence supports their exact disposition. #275 remains OPEN and unchanged except no mutation. Tracker #371 closes only after the full component is terminal.
9. Final release fields are explicit: `release_impact`, `release_action`, `release_channel`. No release, tag, deployment or runtime activation occurs unless separately authorized.
10. Objective-owned merged worktrees/branches/monitors/processes are cleaned after durable merge evidence; pre-existing, unrelated, blocked or unmerged residue is preserved and reported.

## Testing Standard

Evidence-first testing standard:

- Re-resolve and record repository, branch/base, exact imported source and Python/runtime identity before each unit. Compare current upstream before any maintained-fork correction.
- Establish RED from the source issue or an equivalent deterministic isolated canary before changing behavior. Preserve original failure evidence. For nondeterministic issues, use bounded repeated runs and explicit interleaving/synchronization instrumentation; do not count process liveness as progress.
- Add focused regressions for the demonstrated cause and positive controls for preserved behavior. Run focused tests on baseline and candidate when practical, affected subsystem suites, adjacent lifecycle/approval/project tests, privacy/credential/protected-edge controls, and the repository canonical full runner `uv run --frozen python scripts/run_tests.py` unless a narrower repository-documented equivalent is required by the unit.
- #354: use disposable repositories/Projects/worktrees and concurrent-flow ownership to test finalization, prepare_handoff, root provisioning and descendant provisioning. Verify exact SHA/digest/ancestry and no primary-checkout mutation.
- #369: exercise the real kanban tool/DB/goal gate/controller path, including complete-to-review, incomplete rejection, explicit reviewer independence, changes-requested cycle, recovery origin signal and triage/unblock semantics.
- #357: improve the qualification runner only as needed to preserve structured safe assertion/state data. Run the named real PluginContext test, adjacent qualification corpus, repeated fresh processes on Python 3.13, 3.11/3.12 compatibility, and actual PR/post-merge CI. Do not expose secrets/private transcripts.
- #352: exercise spawned-process admission, queue result collection, lock/commit invariants and deterministic competing transitions. Keep the existing timeout and one-commit assertion unless a causal correction itself changes the public contract with explicit justification.
- Run Ruff check/format, MyPy on affected source, compileall, documentation/public-artifact checks and `git diff --check` as applicable. Build/package checks apply when package resources or runtime fork source changes.
- Independent review must inspect source, diff and actual test output; self-report is not acceptance. Final integration reruns combined focused and full gates from the integration tree.
- Report test commands, counts, skips/failures, commit/PR/check state and known limits exactly. A retry, heartbeat, absence of failure in one run or redacted log hash alone is not proof of correction.

## Stop Conditions

Stop or escalate the affected unit when:

- project/contract/base/digest/source identity is missing, ambiguous or conflicts with the finalized artifact; the provisioned worktree already exhibits #354 contamination before a safe isolated recovery canary is possible;
- a change would require touching #275, credentials/providers/models/paid traffic, repository settings/protection, deployment/activation, live home/profile files, raw board DB, unrelated dirty runtime patches, destructive cleanup, history rewrite or check bypass;
- a genuine protected-edge denial occurs; do not route around it;
- #354 cannot be repaired without redefining portable Project identity, contract authority or worktree architecture beyond the accepted behavior;
- #369 cannot be repaired without weakening independent review, letting incomplete work pass, or redefining goal-mode lifecycle authority; return a precise contract/design mismatch rather than adding exceptions;
- #357 diagnostics would expose raw secrets/private transcript content, or a stable causal failure cannot be obtained after bounded instrumented qualification; preserve the issue open with the exact next evidence boundary;
- #352 remains nondeterministic after bounded repeated/instrumented runs and no causal invariant violation appears; do not keep expanding timeouts/fix variants—retain it open with evidence;
- a third focused repair variant appears for the same cause, a unit repeatedly discovers new prerequisites without advancing, or a supposedly local correction becomes architectural/multi-responsibility; return to Supervisor/Morfeo rather than expanding silently;
- upstream source already fixes the exact defect but adoption would require a material broader merge; report the bounded patch/adoption decision instead of importing unrelated upstream changes;
- required tests regress outside the objective and the failure cannot be attributed to a pre-existing documented baseline.

A blocker on one issue does not stop independent units; terminal integration waits for honest dispositions for all four. The owner is unavailable, so ask only for genuinely material decisions that cannot be resolved from this contract, repository policy or evidence.

## Canonical References

- Owner instruction in this session: resolve all current Aether bugs except #275; clarification explicitly names #275.
- `.aether/project.toml`: portable project `12027989-a08f-41cd-a82c-54ff1bfb6b03`, repository `DarkArty07/Aether-Agents`, default branch `main`.
- `AGENTS.md`: R0 authority/procedure, stabilization scope, upstream-first maintained-fork rule, routine verification and Project Canonical Skill discovery.
- `.specify/memory/constitution.md`: accepted R0 constitution and owner authority.
- `DESIGN.md`: three-role architecture, protected-edge boundaries, evidence-gated lifecycle and issue-backed defects.
- GitHub #371: non-duplicate tracker for this four-bug follow-up.
- GitHub #354: concurrent Project primary checkout causes root/descendant handoff lineage contamination; latest comments preserve real regressions after PRs #363/#365.
- GitHub #369: goal-mode judge circularly blocks `kanban_request_review`; recovery controller/allowed block-kind mismatch; current upstream retained the same path at issue intake.
- GitHub #357: Python 3.13 PluginContext post-merge failure; retry recovered without causal fix; recurrence at Aether merge `89c372fa6af09c6cbd5ce9a18dc7dd78ed70dfc4`, run `34436209305`, job `102741693933`.
- GitHub #352: intermittent `_queue.Empty` in concurrent lifecycle test; immediate targeted rerun passed; prior queue improved fixture admission but did not demonstrate product cause.
- Explicit exclusion: GitHub #275 remains open and outside authority.
- Current observed repository state at contract authoring: local Aether HEAD `da7bcef5854763bf5b70a5540f707722c158dfbb`; remote `origin/main` `89c372fa6af09c6cbd5ce9a18dc7dd78ed70dfc4`; maintained fork `aether-main` `8a6b33ae480373015178b80e87c88fe0abda3919`. Re-resolve at execution time.
- Aether Canonical procedures: objective-contract-design, agentic-work-routing, implementation-evidence, supervisor-decomposition, sdlc-review, git-github-closeout and semver-release as role/applicability requires. Root checkout currently has no `.aether/skills/` Project Canonical Skill directory.
