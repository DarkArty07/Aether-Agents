---
artifact_type: "aether.objective-contract.v1"
project_id: "12027989-a08f-41cd-a82c-54ff1bfb6b03"
contract_id: "oc_63345d36f393baf9"
version: 3
status: "final"
title: "Repair affinity flow liveness and record HLP-280"
created_at_utc: "2026-09-01T18:16:48Z"
created_at_local: "2026-09-01T12:16:48-06:00"
finalized_at_utc: "2026-09-01T18:19:33Z"
finalized_at_local: "2026-09-01T12:19:33-06:00"
author_profile: "morfeo"
created_in_session: "20260831_225907_9bdaeb"
finalized_in_session: "20260831_225907_9bdaeb"
supersedes: "oc_63345d36f393baf9@v2"
change_reason: "Resolve an execution-order contradiction found by Supervisor preflight. Retain the safer pre-activation implementation PR merge gate, then authorize exactly one second, narrowly scoped evidence-only PR after successful activation/canary to record actual PID/state/backup/hash/canary/rollback facts. V2 created no child cards and must not execute."
observation_trace_id: "ctr_4fdb78354e4e0fa45edb25f4c09a1f8f"
---

# Objective Contract: Repair affinity flow liveness and record HLP-280

## Owner Intent

The owner explicitly approves implementing the #280 affinity-flow liveness correction in Aether's downstream Hermes runtime and, after the live v1 execution exposed a second same-class stall, also approves controller-first routing for automatic non-affinity child terminal failures (`protocol_violation`, `gave_up`, and equivalent circuit-breaker outcomes). The production cap is raised from 100 to 130 changed lines. Every framework byte changed, added or activated must be recorded in `HERMES_LOCAL_PATCHES.md` with exact scope, evidence, backup, patch identity, activation state and retirement gate. The v1 candidate and its verified evidence are reusable antecedents but v1 must not be activated.

## Objective

Close Aether issue #280 by completing the HLP-211b liveness state machine across both explicit and automatic blocker paths: (a) an affinity terminal controller with pending `flow_attention` cannot remain parent-gated in `todo` after explicit external recovery; (b) an affinity coordinator cannot persist a silent non-dependency block; and (c) an automatic terminal failure of a non-affinity child inside an affinity flow cannot leave the terminal controller parent-gated without `flow_attention`, `origin_signal`, or `flow_terminal`. Implement and qualify the minimal downstream Hermes change, preserve ordinary parent gating and silent internal recovery, publish an independent HLP-280 patch and complete ledger record, activate it reversibly, and prove the exact owner-facing liveness path without relying on an external watcher.

## Decisions and Assumptions

Approved decisions:

1. Repair downstream Hermes atomically; no plugin, cron, watcher or SQL compensation.
2. Reuse `kanban_unblock`; add no tool, CLI verb, table, status, service or notification kind.
3. Pending `flow_attention` may requeue a valid terminal controller from `blocked|todo` to `ready`; ordinary parent gating remains.
4. Rediscovery may requeue `todo` idempotently but never auto-promote deliberately `blocked`.
5. Reject a non-`dependency` affinity block without explicit `input|revision|recovery` before mutation.
6. Automatic non-affinity child terminal failures route one deduplicated controller-first attention; terminal-affinity failures retain `flow_terminal`; ambiguous/cross-flow graphs fail closed.
7. Keep gateway/TUI behavior unchanged and owner-silent for repairable work.
8. Build from exact active bytes in isolation; v1 commit `655b0cb` is antecedent only.
9. Final HLP-280 production delta is at most 130 changed lines in `kanban_db.py` plus minimal `kanban_tools.py`; every byte is indexed.
10. Publication uses exactly two reviewed PRs: first behavior/patch/pre-activation ledger (`RELOAD_PENDING`) merged before runtime mutation; second evidence-only PR after successful activation/canary records actual backup, hashes, PID/state, canary and rollback facts. The second PR changes no behavior.
11. No upstream PR; canonical docs are English and evidence is bounded.

## In Scope

- Preserve and reverify v1 RED/GREEN evidence for external recovery and silent affinity-block rejection.
- Add RED/GREEN coverage for non-affinity child `protocol_violation`/`gave_up` and representative spawn/crash/timeout circuit breakers.
- Implement guarded controller requeue, idempotent attention rediscovery, silent-block rejection, and controller-first automatic-failure routing with Project/flow/claim isolation.
- Build from exact active bytes in an isolated candidate; generate an independent HLP-280 patch with apply/reverse/byte reconstruction and a 130-line production cap.
- Reconcile `HERMES_LOCAL_PATCHES.md` completely, including HLP-247, status headings, hotspots, every HLP-280 byte/hash/test/backup/activation/retirement fact, plus CHANGELOG, limitations, #280 and policy allowlist.
- Independently review and merge the implementation PR while activation fields remain explicitly pending.
- Only after that merge, verify active hashes, create/read back the exact backup, apply reviewed hunks, reload and run both no-watcher canaries.
- Then publish exactly one second evidence-only PR recording actual activation facts; it may modify ledger/evidence/docs only and must pass review/checks.
- Close #280 only after the evidence PR merges and both live paths are proven.

## Out of Scope

- Upstreaming the full HLP-211 affinity architecture or opening a NousResearch PR.
- Replacing affinity, Kanban, the dispatcher, TUI delivery or gateway notification routing.
- Adding a generic liveness daemon, cron monitor, watcher, database table, new status or new public recovery verb.
- Rewriting or consolidating every historical Hermes patch artifact; only HLP-280 and index truth needed for this repair are included.
- Retiring HLP-209/HLP-263 or any other patch already present upstream; those require separate retirement qualification.
- Changing Aether's Objective Contract semantics, role authority, concurrency topology, provider/model configuration or Project/board isolation.
- Acquiring/widening credentials, adding providers, paid evaluation, deployment outside the already-provisioned local Aether runtime, or modifying unrelated services.
- Modifying the active shared Hermes checkout during development or overwriting complete dirty files during activation.
- Treating a passing synthetic test as proof of owner-facing wake; the controlled live path is separately required.

## Authority

The owner authorizes the downstream Hermes HLP-280 repair, automatic non-affinity failure routing, a 130-line production cap, complete patch-index attribution, routine GitHub publication and reversible activation using existing access.

Authority specifically permits exactly two Aether PRs: (1) implementation/patch/pre-activation ledger, which must merge before runtime mutation; and (2) a post-activation evidence-only PR recording actual backup, hashes, PID/state, canaries and rollback facts. The second PR may change ledger/evidence/docs only and must pass normal review/checks.

This does not authorize new credentials/providers/spend, upstream publication, unrelated framework cleanup, constitutional changes, destructive migration, force push, bypassing checks, activating before PR1 merge, behavior changes in PR2 or overwriting concurrent dirty work. Active-target drift, ambiguous ownership or inability to remain within the approved lifecycle design stops activation.

## Deliverables

1. Reviewed HLP-280 kernel/tool candidate covering explicit and automatic blocker paths, within 130 production changed lines.
2. Deterministic RED/GREEN and negative controls for requeue, silent affinity blocks, protocol violations and representative circuit breakers.
3. Independent portable HLP-280 patch with SHA-256, apply/reverse and byte-reconstruction evidence; policy allowlist updated.
4. Truthful pre-activation ledger/index/docs: every framework byte and v1 antecedent recorded, HLP-247/status/hotspots reconciled, activation fields marked `RELOAD_PENDING`.
5. First Aether implementation PR containing behavior, tests, final patch and pre-activation documentation; independently reviewed, checks green and merged before runtime mutation.
6. Exact runtime backup/manifest, reviewed hunk application, healthy reload/readback, synthetic and live no-watcher acceptance for both liveness paths.
7. Second Aether evidence-only PR containing actual backup, hashes, PID/state, canary, rollback and issue evidence; no behavior changes, independently reviewed, checks green and merged.
8. #280 closeout and cleanup report distinguishing v1/v2/v3 artifacts, objective worktrees/backups and pre-existing dirty runtime state.

## Acceptance Criteria

- RED reproduces both zero-frontier defects; GREEN fixes them without broad lifecycle changes.
- Explicit unblock of a valid pending-attention controller yields `ready`, one claim and one `flow_attention_requeued`; rediscovery requeues `todo` but not deliberately `blocked`.
- Silent non-`dependency` affinity block is rejected before mutation.
- A non-affinity child automatic terminal failure routes exactly one attention to the unique valid terminal controller; terminal-affinity failures retain `flow_terminal`; retries, ordinary parent gating, unrelated/ambiguous graphs and cross-Project/flow cases retain current behavior.
- Repairable work is owner-silent; explicit signals and `flow_terminal` deliver once; session/workspace affinity, leases and single-writer guarantees hold.
- No gateway/TUI production change or new tool/status/table/service; production delta is at most 130 changed lines.
- Patch applies/reverses/reconstructs exact candidate bytes and every framework hunk/hash is indexed.
- First implementation PR is reviewed, green and merged with activation facts honestly pending before any runtime mutation.
- Pre-activation hashes match; backup readback succeeds; reviewed hunks apply; reload/readback is healthy; both no-watcher canaries pass without duplicate/concurrent writers.
- Second PR is evidence-only, contains actual activation/rollback facts, changes no behavior, and is reviewed/green/merged.
- Required Hermes/Aether suites and both PR check sets pass; only a proven baseline-equivalent skip outside acceptance may remain.
- On canary failure exact rollback restores the last green baseline. #280 closes only after the evidence PR merges.

## Testing Standard

Use RED-GREEN-REFACTOR with isolated homes/DBs/workspaces, task-owned `/home` TMPDIR and `HERMES_DELEGATED_CHILD_CONTEXT` removed.

RED/GREEN must cover external controller recovery, silent affinity-block rejection, non-affinity child protocol-violation `gave_up`, representative spawn/crash/timeout trip paths, parent gating, idempotence, isolation, leases and delivery deduplication. Run affected Kanban/tool/TUI/gateway/terminal-guard suites and Hermes's canonical per-file full runner. The known cron env-isolation failure may be named only after exact baseline equivalence and must not cover acceptance; no other failure is accepted. Run Ruff/format, compile/type checks and `git diff --check`.

Patch gates: exact pre-hashes; raw diff/130-line cap; apply/reverse; byte-for-byte reconstruction; non-target preservation; SHA-256 readback; no foreign HLP hunks.

Aether gates: reconciliation/documentation/public-artifact validators, focused tests, full `scripts/run_tests.py`, Ruff/format/mypy/build and required GitHub checks.

Publication/activation order is mandatory: (1) review and merge the implementation PR with `RELOAD_PENDING`; (2) recheck hashes, backup/readback, apply reviewed hunks, reload once and prove health; (3) run deterministic probes and both live no-watcher paths; (4) update only factual ledger/evidence/docs; (5) review/check/merge the evidence-only PR; (6) close #280. The evidence PR must contain no runtime behavior or patch-byte changes.

Report configured, persisted, exercised and activated separately. Reuse v1 evidence only after exact byte applicability is independently verified.

## Stop Conditions

Stop before further mutation if either RED fails to reproduce; active hashes/ownership drift; candidate provenance is not exact; production exceeds 130 changed lines; gateway/TUI production, a new tool/status/table/service or broad refactor is required; automatic routing cannot be centralized without over-routing retries; parent gating, failure accounting, affinity fencing, isolation or deduplication regress; patch apply/reverse/reconstruction or complete byte attribution fails; a new full-suite failure remains; or activation needs new credentials/providers/spend/unrelated service effects.

Do not activate before the reviewed green implementation PR merges. Do not place behavior/patch-byte changes in the evidence PR. Do not close #280 before the reviewed green evidence-only PR merges. V1 and v2 are superseded and forbidden for activation/execution; preserve their evidence only.

If reload/canary fails, restore exact backup, reload once and run the last-known-good canary; stop when green. At most two v3-focused behavior repair variants are allowed. Any protected-edge denial, duplicate writer/wake, repairable owner wake or zero-frontier recurrence stops the objective.

## Canonical References

Aether base for v3 authoring: `main` at `1a942401ce36454af96e04408fc0cab71738fecb`. V1 (`92115444...`) and v2 (`ce398e1b...`) are superseded and forbidden for activation. V2 preflight created no child cards; its Supervisor blocker is durable evidence for the two-PR sequence.

Authorities/evidence: `.aether/project.toml`; `AGENTS.md`, `DESIGN.md`, `ROADMAP.md`; `docs/authority.md`, lifecycle/policy/troubleshooting guides; `HERMES_LOCAL_PATCHES.md`, `CHANGELOG.md`, policy manifest, patch reconciliation validator, existing Hermes patches, and Aether issue #280 with comments `5490963306` and `5497624814`.

Reusable antecedents only: v1 candidate `655b0cb0349e26cfaaf196849680ed83bed56067`, unactivated patch SHA-256 `3c9c284daa4a193793350d14c14f69f737dc0e552756229837fdbbe69d110ef4`, its 95-line/test evidence and live automatic-failure events. Loaded Hermes remains project-relative `home/.venv-hermes/src/hermes-agent`, distribution `0.20.1`, committed HEAD `0b288979e2322c02ab42c05f1e183bb31cfa5aa9`; v1 active extraction hashes remain the activation gate.

Upstream `NousResearch/hermes-agent@b6f42c667a59469b477220254fec6e2486e30e51` lacks affinity/flow primitives; PR #75951 covers same-card resume only. Historical task IDs are evidence, never fixtures.
