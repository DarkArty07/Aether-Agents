---
artifact_type: "aether.objective-contract.v1"
project_id: "12027989-a08f-41cd-a82c-54ff1bfb6b03"
contract_id: "oc_0a0db5e78c4e24c1"
version: 1
status: "final"
title: "Close canonical-skill safety and autonomous stewardship qualification gaps"
created_at_utc: "2026-09-04T23:19:11Z"
created_at_local: "2026-09-04T17:19:11-06:00"
finalized_at_utc: "2026-09-04T23:23:53Z"
finalized_at_local: "2026-09-04T17:23:53-06:00"
author_profile: "morfeo"
created_in_session: "20260903_202114_898663"
finalized_in_session: "20260903_202114_898663"
supersedes: null
change_reason: null
observation_trace_id: "ctr_2f57fb93815d8e5f0389982f6c546618"
---

# Objective Contract: Close canonical-skill safety and autonomous stewardship qualification gaps

## Owner Intent

Christopher wants every remaining gap from the post-merge audit of PR #299 finished so this session and the autonomous Git/GitHub stewardship capability can close honestly. Preserve the implemented three-role design and active local behavior; correct only the learned-skill collision, documentation/SOUL coherence, capability-state reporting, and the missing fresh-session end-to-end proof. Do not add functionality beyond what was already accepted in oc_a63ccc67b95411eb@v1.

## Objective

Deliver one reviewed corrective release candidate that: (1) makes Aether Canonical Skill activation and deactivation fail closed without overwriting or deleting unknown same-name Learned Profile Skill bytes; (2) reconciles the three packaged SOULs and current documentation without losing existing obligations; (3) records autonomous Git/GitHub closeout, SemVer/release conclusions, AGENTS.md stewardship, and conditional Issue handling in the capability registry with honest qualification status; and (4) uses Morfeo's already proven active-profile canonical-skill discovery plus this objective's fresh Supervisor/Implementer sessions and normal GitHub closeout as end-to-end evidence that canonical procedure discovery drives terminal closure without copying the full procedure into root or terminal card bodies.

## Decisions and Assumptions

Decisions:
1. This is a bug-fix/coherence closure of oc_a63ccc67b95411eb@v1, not a new product capability. Exactly three roles remain; no new loader, registry, daemon, scheduler, state machine, role, Hermes patch, or workflow engine.
2. Issue #300 is the canonical GitHub defect record. Do not create another Issue.
3. Collision policy is fail-closed and transactional. Before any profile mutation, preflight every canonical-skill target in every managed profile. A missing target is writable. An existing target is writable only when the existing valid `aether-observer.json` identifies the previously active immutable release and the target bytes equal that release bundle's exact role/skill bytes. Any absent, malformed, stale, ambiguous, unsafe, or byte-mismatched ownership evidence stops the transition before any profile file changes. Identical bytes without proven prior Aether ownership remain unknown and must not be claimed.
4. Updating an Aether-owned canonical skill to new canonical bytes is allowed after the prior-release proof. Unknown Learned Profile Skills are preserved byte-for-byte; no rename, namespace, backup copy, auto-promotion, or shadow registry is introduced.
5. Deactivation removes a canonical-skill target only when the profile activation marker identifies the immutable active release and target bytes equal that exact release bundle. Unknown or drifted targets are preserved and surfaced honestly. Existing transaction snapshot/rollback behavior remains, but preflight must prevent partial profile mutation rather than relying only on rollback.
6. Existing non-canonical profile content, unrelated skills, configuration, sessions, memories, credentials and provider/model choices remain untouched.
7. SOUL edits are surgical: remove duplicate portability/AGENTS.md wording, state that release compatibility evidence supports `release_impact`, and make Implementer report unit-level compatibility evidence while Supervisor owns aggregate conclusions. Preserve every other existing obligation and exact three-role boundary.
8. `docs/capabilities.toml` remains the sole status registry. Add or reconcile distinct traceable entries for autonomous Git/GitHub terminal closeout, SemVer/release conclusion procedure, AGENTS.md stewardship, and conditional non-duplicate Issue intake/reconciliation. Status must be no stronger than evidence. `implemented` is permitted only where this candidate's required merge/check/Issue/cleanup evidence makes the claim true at integration and the notes preserve still-unqualified public installed lifecycle as a separate boundary; otherwise use `partial`. Generated reference must match.
9. This objective is the E2E. Morfeo has already discovered/loaded the canonical procedures from its active profile in the current session and creates a root handoff containing only the canonical envelope. Supervisor must discover/load task-relevant canonical skills itself and create concise contract-derived cards; neither root nor terminal body may copy the full GitHub/SemVer procedures. Implementers receive fresh sessions. The final fresh Supervisor session must use the canonical skills and durable board/Git/GitHub state to perform normal push, PR, checks, merge, Issue #300 reconciliation, cleanup and terminal reporting.
10. Release conclusions are independent: expected impact is `patch` because this corrects compatible defective behavior and truthful documentation; default action is `defer`; channel is `none`, unless the actual diff proves a materially different result. No version bump, tag, GitHub Release, package publication or deployment follows from this expectation.
11. After merge and durable cleanup, Morfeo may reconcile the already owner-authorized local active SOUL/Skill copies from merged source, preserving private state and refusing any collision. New-session adoption requires no service restart; existing sessions keep their stored prompt bytes.
Assumptions: the exact existing profile activation marker and immutable release bundle can establish prior Aether ownership without introducing new state; all required GitHub credentials are already provisioned; current main remains the intended base; no current private profile has a same-name collision, as separately verified before the prior local activation.

## In Scope

1. Add TDD regressions and the minimal lifecycle correction for safe same-name canonical/learned skill collision handling across install, update, activation failure, rollback and deactivation.
2. Reconcile the packaged Morfeo, Supervisor and Implementer SOULs only for the identified duplicate/ambiguous lines, with preservation tests for all prior obligations.
3. Update `docs/capabilities.toml`, regenerate `docs/reference/capabilities.md`, and adjust only directly affected current documentation/spec wording needed to report status and behavior truthfully.
4. Verify Aether Canonical Skill source/package/profile-bundle byte identity, Project Canonical discovery convention, and Learned Profile Skill preservation.
5. Execute focused tests, exact-Hermes qualification, complete suite/coverage, docs, type/lint/format, build, public-artifact/privacy and policy-manifest checks required by the repository.
6. Use Issue #300 and this objective's own normal GitHub lifecycle as the fresh-session E2E: preserved unit commits, independent review/rework, integrated verification, normal branch push, PR, all required checks, green merge, Issue reconciliation, remote/local cleanup and durable terminal evidence.
7. After pipeline closure, reconcile the merged SOUL/Skill bytes into the three private live profiles and verify discovery from the active Morfeo profile without modifying config, memory, credentials or unrelated learned skills.

## Out of Scope

No new product capability, fourth role/profile, custom skill loader/index/registry, scheduling or maintenance automation, Hermes core/fork patch, new lifecycle state machine, generic release orchestrator, public installer redesign, repository creation/settings mutation, credential acquisition/widening, provider/model change, paid inference, package publication, version bump, tag, GitHub Release, deployment, migration, destructive cleanup, force/history rewrite, bypass/admin merge, or unrelated documentation/refactor. Do not modify current private runtime profiles before the reviewed GitHub result is merged. Do not clean pre-existing, concurrent, active, blocked, unmerged, unknown or unrelated branches/worktrees/stashes/processes. Do not treat installed/public Aether release qualification as completed; retain its separately documented boundary.

## Authority

Christopher authorizes this corrective objective, its normal repository/GitHub lifecycle within `DarkArty07/Aether-Agents`, Issue #300 reconciliation, and post-merge local reconciliation of the already active private SOUL/Skill copies. Christopher retains product intent, constitutional change, acceptance of incompatible behavior, protected effects, public stable-release gates and any authority not stated here. Morfeo owns contract correction, route selection, post-merge private-profile reconciliation and final owner-facing verification. Supervisor owns executability, decomposition, independent review, aggregate release conclusions, integration, normal branch/PR/check/merge, Issue #300 reconciliation and residue cleanup. Implementer owns bounded TDD implementation units, local commits, tests, docs and unit-level compatibility evidence; it never publishes. Routine existing-credential GitHub closeout is authorized; package publication, deployment, version/tag/Release, credentials, settings, protected/destructive operations, force/history rewrite and bypass are not.

## Deliverables

1. Minimal product code and tests proving collision-safe, transactional canonical-skill activation/update/rollback/deactivation using existing immutable release/profile evidence.
2. Three surgically reconciled packaged SOULs with preservation tests.
3. Reconciled capability registry and generated reference for Git/GitHub closeout, SemVer/release conclusions, AGENTS.md stewardship and conditional Issues, with accurate statuses and limits.
4. Any minimal current docs/spec reconciliation directly required by those behavioral/status corrections.
5. Full local verification evidence and exact changed-file/privacy review.
6. A merged normal PR that preserves implementation-unit history, has all required checks green, links and closes/reconciles Issue #300, and reports `release_impact`, `release_action`, `release_channel` independently.
7. Durable board evidence that the fresh Supervisor session discovered and used `git-github-closeout` and `semver-release` without the full procedures being copied into card bodies.
8. Zero objective-owned branches/worktrees/stashes/processes after durable merge evidence, while all unrelated/pre-existing state is preserved.
9. Post-merge local runtime reconciliation: source/live SOUL and canonical-skill byte identity for all three roles, native `skill_view` discovery, preserved private config/memory/credentials/unrelated skills, and rollback backups.

## Acceptance Criteria

AC1. A failing regression first reproduces same-name Learned Profile Skill replacement under the pre-fix implementation and records expected RED evidence.
AC2. Activation performs a complete three-profile/canonical-skill ownership preflight before changing any profile bytes.
AC3. A missing canonical-skill target installs normally.
AC4. A target proven Aether-owned by a valid prior profile activation marker plus byte identity with that marker's immutable release bundle updates normally.
AC5. Any existing target without complete prior Aether ownership proof causes a clear integrity refusal, even when its bytes happen to equal the incoming canonical Skill.
AC6. A collision refusal preserves every profile product/private file byte-for-byte and does not move the active release pointer.
AC7. Same-name Learned Profile Skills survive failed install/update, rollback and deactivation byte-for-byte.
AC8. Deactivation removes only target bytes proven owned by the exact immutable release named by the profile activation marker; unknown/drifted files remain and produce honest evidence.
AC9. Existing unrelated private Skills, configuration and profile state remain unchanged; symlink/no-follow and permissions invariants remain green.
AC10. No new loader, registry, namespace, skill rename, backup convention, Hermes patch or role is introduced.
AC11. Morfeo, Supervisor and Implementer packaged SOULs preserve all prior tested role, authority, handoff, recovery and safety obligations.
AC12. Morfeo wording states that compatibility evidence supports `release_impact` and that impact/action/channel are the three separate conclusions; duplicate portability wording is removed.
AC13. Supervisor has one unambiguous root-AGENTS coherence obligation and retains ownership of aggregate conclusions and pipeline publication.
AC14. Implementer explicitly reports unit-level compatibility evidence/conclusions only and never owns the aggregate release decision or publication.
AC15. `docs/capabilities.toml` and generated `docs/reference/capabilities.md` contain distinct traceable coverage for autonomous Git/GitHub terminal closeout, SemVer/release conclusions, AGENTS.md stewardship and conditional Issue intake/reconciliation.
AC16. Capability status and notes distinguish prompt/skill guidance, actual PR #299/#current-objective evidence, private live activation, and still-unqualified public installed lifecycle; no overclaim of runtime enforcement or stable release.
AC17. Current lifecycle/execution/authority/project-initialization docs remain consistent with the registry and three-role design.
AC18. The finalized Objective Contract remains unchanged and project-bound; root handoff contains only its exact envelope and opaque routing side data.
AC19. Fresh Supervisor/Implementer sessions load the updated live SOULs and discover task-relevant canonical skills. Durable evidence names the loaded skill(s) and source without copying their full body into cards.
AC20. This objective reaches a normal PR with all required checks green and a non-bypass merge to `main`; unit commits remain individually inspectable.
AC21. Issue #300 is linked and closed/reconciled only after its acceptance is satisfied.
AC22. The final board report contains exact commit/PR/check/test/Issue/cleanup evidence and independent `release_impact`, `release_action`, `release_channel` values.
AC23. Every objective-owned local/remote branch and worktree is removed only after durable merge evidence; unrelated/pre-existing state remains and is reported separately.
AC24. Post-merge live `home/profiles/{morfeo,supervisor,implementer}` SOULs and the three canonical Skill copies match merged source byte-for-byte, while config, memory, auth and unrelated learned-skill bytes remain unchanged.
AC25. Native Hermes `skill_view` resolves each canonical Skill from an active profile after reconciliation.
AC26. Repository policy manifest, documentation generator, public-artifact privacy scan, build, type/lint/format, focused tests, full suite and configured coverage floor all pass.
AC27. No version bump, tag, GitHub Release, package publication, deployment or service restart occurs; any non-applicable closeout step has a concrete reason.

## Testing Standard

Strict TDD for the lifecycle bug: add one minimal same-name collision test before production code, run it against the pre-fix implementation, and record a behavioral failure (not a fixture/import error). Implement the smallest fail-closed ownership preflight, rerun GREEN, then add vertical cases for proven prior Aether ownership/update, no-marker identical bytes, rollback/deactivation preservation, unrelated private state, symlink and permissions. Do not weaken or rewrite tests to accept destructive behavior.
Focused verification must cover `tests/test_observation_lifecycle.py`, `tests/test_observation_packaging.py`, `tests/test_objective_contracts.py`, `tests/test_documentation.py`, `tests/test_project_init.py`, `tests/test_a1_contracts.py`, and policy/public-artifact tests affected by the final diff. Run `uv run --frozen python scripts/check_documentation.py`, exact-Hermes baseline/qualification commands required by `.github/workflows/policy.yml`, `uv run --frozen python scripts/run_tests.py`, configured coverage without lowering 78%, compileall, mypy, Ruff check/format, `uv build`, public-artifact/privacy scanning, policy manifest equality and `git diff --check`.
The external E2E is this objective's own fresh-session pipeline. Root and terminal card bodies must not duplicate the canonical skill procedures. Evidence must prove skill discovery/use, preserved unit history, normal branch push, PR, required checks, non-bypass merge, Issue #300 reconciliation and cleanup. GitHub Actions is authoritative for remote checks. Post-merge Morfeo verifies live source/target SHA-256 or byte identity, skill discovery, unchanged private files and rollback backups. No live provider-product call, tag, release, package publication, deployment or service restart is required or authorized.

## Stop Conditions

Stop before decomposition on portable project, exact-path Hermes Project, contract digest or base-commit mismatch. Stop and return to Morfeo if collision safety requires a new skill loader/registry/namespace, Hermes core change, changed three-role authority, new state machine, or loss/rename/migration of private learned data; if no existing immutable release/marker evidence can prove prior Aether ownership; if the fix cannot fail before any profile mutation; if a current private same-name collision is discovered before post-merge reconciliation; if SOUL edits remove a prior obligation; if capability entries would overclaim unqualified public activation; or if the actual compatibility impact differs materially from a compatible patch.
Block protected actions: credentials, settings mutation, force/history rewrite, bypass/admin merge, arbitrary API mutation, package publication, version/tag/Release, deployment, destructive cleanup or service restart. Preserve concurrent/unrelated work. A failing GitHub check is diagnosed from logs and corrected only if objective-caused; unrelated infrastructure failure remains visible and must not be bypassed. Do not close Issue #300 or clean branches/worktrees before durable green merge evidence. If the fresh-session pipeline cannot discover the canonical skills, report the exact runtime/profile boundary rather than copying the full procedure into cards or claiming E2E success.

## Canonical References

Project marker `.aether/project.toml`: portable project `12027989-a08f-41cd-a82c-54ff1bfb6b03`; exact path-bound Hermes Project `p_227bd972`; repository `DarkArty07/Aether-Agents`; default branch `main`; base `03bee433e0cc3737a750afc89086d2ef57db0996`. Canonical defect: GitHub Issue #300. Predecessor Objective Contract: `.aether/objective-contracts/oc_a63ccc67b95411eb/v1.md` and PR #299 merge `03bee433e0cc3737a750afc89086d2ef57db0996`. Authorities: current Christopher instruction; `.specify/memory/constitution.md`; `DESIGN.md` PD-13, PD-15, PD-30, PD-33 and canonical artifact relationships; `specs/r5-topology-and-isolation/spec.md`; `specs/r8-workspaces-and-integration/spec.md` including FR-824; `specs/r9-state-and-recovery/spec.md`; `specs/r10-security-and-authority/spec.md`; `specs/r13-synthesis-and-release/spec.md`; `specs/001-aether-v1-productization/spec.md`; root `AGENTS.md`; `docs/authority.md`; `docs/capabilities.toml`; `docs/guides/{lifecycle,execution,project-initialization,policy-and-recovery}.md`; `docs/roles-and-authority.md`; packaged SOULs; Aether Canonical Skills `canonical-skill-governance`, `git-github-closeout`, and `semver-release`; `src/aether_agents/lifecycle.py`; `.github/workflows/{policy,release}.yml`; tests named in the testing standard. Runtime source/current-profile verification is local evidence only and never portable contract content.
