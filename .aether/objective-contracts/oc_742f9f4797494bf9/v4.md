---
artifact_type: "aether.objective-contract.v1"
project_id: "12027989-a08f-41cd-a82c-54ff1bfb6b03"
contract_id: "oc_742f9f4797494bf9"
version: 4
status: "final"
title: "Aether 1.0.0-rc.2: truthful prerelease successor, activation and rollback proof (#261)"
created_at_utc: "2026-09-17T19:45:32Z"
created_at_local: "2026-09-17T13:45:32-06:00"
finalized_at_utc: "2026-09-17T19:45:32Z"
finalized_at_local: "2026-09-17T13:45:32-06:00"
author_profile: "morfeo"
created_in_session: "cursor_rc2_v4_contract_20260917_134532"
finalized_in_session: "cursor_rc2_v4_contract_20260917_134532"
supersedes: "oc_742f9f4797494bf9@v3"
change_reason: "v3 hardcodes recovery tip e1af59c0 even though main advanced, while aether update --local requires the same annotated v1.0.0-rc.2 tag that public release later uses on that candidate commit. v4 removes the pre-merge SHA pin: the recovery/publish candidate is the clean origin/main merge of this correction PR; after merge, freeze main, record the exact SHA in a local/durable preflight receipt, create exactly one local annotated v1.0.0-rc.2 tag on that frozen commit, never move/delete/retarget it, and push that same tag publicly only after recovery canaries/REGATE/final gates. Also replaces false drain wording with supported immediate interruption/checkpoint semantics."
observation_trace_id: "ctr_7f1fbd707617ba722583d940df686f29"
---

# Objective Contract: Aether 1.0.0-rc.2: truthful prerelease successor, activation and rollback proof (#261)

## Owner Intent

The owner authorizes full autonomous operational execution inside the approved Aether 1.0.0rc2 objective and provisioned access, including contract correction, reviewed source integration, local immutable runtime promotion, service restart, GitHub prerelease publication, deployment, rollback/reactivation, issue reconciliation and cleanup. The recovery gate is explicit: the temporary live bootstrap and REGATE review run 173 do not count. REGATE may be accepted only after canonical #450 and #461 source fixes are independently reviewed and merged, promoted as an immutable versioned runtime through the supported `aether update` path, and a real board canary proves both review-phase restoration and superseded-worker reap. After that gate, continue REGATE review, bundle, publish and terminal activation. Rc.1 remains immutable, rejected and unactivated. This authority expands executable effects, not scope: do not invent unrelated work, acquire or widen credentials, expose secrets, waive acceptance, bypass a genuine protected edge, force or rewrite history, publish to PyPI, or claim stable or WSL qualification.

## Objective

Deliver one coherent Aether 1.0.0rc2 from corrected, reviewed and merged source. The maintained-fork input remains exact merge `aed6591a69f453a1867b73628603e7b53ba40ffc` (tree `2258311f9aa52c58fda9bdbd03183ba50ac85df0`), carrying both #450 and #461. The selected Aether recovery and later publish candidate is not a pre-merge hardcoded tip: it is the clean `origin/main` merge result of the reviewed `@v4` correction PR that lands this contract and its required reconciliations. After that merge, freeze `main`, record the exact resulting SHA in a local/durable preflight receipt (without requiring another source commit), create exactly one local annotated tag `v1.0.0-rc.2` on that frozen commit, and never move, delete or retarget it. First replace the temporary patched runtime with a clean immutable local candidate built from that tagged Aether commit and fork `aed6591…` through `aether update --local`, verify source identity, doctor/service health and real #450/#461 board behavior, then obtain a fresh valid independent review of the preserved REGATE candidate. Only after that recovery gate and the final tip gates may the flow qualify deterministic release bytes, push the same already-created annotated tag and publish GitHub prerelease `v1.0.0-rc.2`, activate the downloaded rc.2 release, prove rollback and forward reactivation without reverting mutable state, reconcile public claims and issues, and close objective-owned residue. Rc.2 is a prerelease candidate, not stable 1.0.0.

## Decisions and Assumptions

DECIDED: `oc_742f9f4797494bf9@v1`, `@v2` and `@v3` are superseded. `@v3` restored the hard recovery gate after `@v2` wrongly treated REGATE run 173 as valid, but introduced a tip/tag contradiction: it pinned recovery to Aether `e1af59c0d0f2684cc149fd362d6933dadb964c2c` while `main` continued past that commit, and while the supported local-candidate path (`lifecycle.LifecycleManager.local_candidate` / `_release_tag_at`) requires the candidate commit to carry exactly one annotated release tag matching `VERSION` — the same `v1.0.0-rc.2` identity public release later uses. That pre-merge hardcode is retired. DECIDED: Run 173 and its completion remain historical evidence, not acceptance. The preserved REGATE implementation commit `10bce0e715355412a7f5350a4d162deeb6a61b8e` and its five attachments may be reused as content evidence if unchanged, but a new independent review claim must occur only after immutable recovery promotion and the real canaries. DECIDED: executable Hermes source is exact clean `DarkArty07/aether-hermes:aether-main` merge `aed6591a69f453a1867b73628603e7b53ba40ffc`; `.patch` files, HLP records and the temporary bootstrap are audit/reconstruction evidence only and MUST NOT be replayed or layered onto the promoted runtime. Historical Aether tip `e1af59c0…` remains evidence of the reviewed #462 fork-pin reconciliation only; it is not the recovery or publish tip. DECIDED: avoid circular self-pinning — this contract may name the selected Aether candidate as the clean merge result of its own reviewed `@v4` correction PR; the exact 40-character SHA is resolved only after that merge and recorded in a local/durable preflight receipt, not by editing another source commit into the contract. DECIDED: immediately after the `@v4` correction PR merges to `origin/main`, freeze `main` against further tip-advancing commits until the local annotated `v1.0.0-rc.2` tag exists on that frozen commit and the recovery → canary → REGATE → final-gate → public-tag-push boundary for that tag completes or stops; create exactly one local annotated tag `v1.0.0-rc.2` on the frozen merge commit; never move, delete or retarget it; do not create a second rc.2 tag. DECIDED: the pre-REGATE recovery promotion uses the existing supported local-candidate interface, with a non-mutating preview followed by explicit activation from that tagged frozen Aether commit and clean fork `aed6591…`. It creates a versioned immutable runtime and replaces the temporary bootstrap; it is neither the public rc.2 release nor acceptance of final release bytes. This promotion is a dedicated interruption boundary using supported semantics: checkpoint the preview, backup and exact transition inputs durably; interrupt Aether-owned TUI/gateway/worker processes immediately with no drain or wait-for-idle; leave unrelated services untouched; allow the Aether-owned gateway to restart through the supported updater; and verify the transition from a successor/controller using the persisted receipt rather than claiming that a killed worker continued. DECIDED: after promotion, a real native canary must prove #450 predecessor reap before successor mutation, the long-running-current negative control, #461 phase-less routed recovery preserving `review`, and explicit ready recovery remaining `ready`; then the REGATE candidate receives fresh independent review from `source_status=review`. DECIDED: public push of annotated tag `v1.0.0-rc.2` and GitHub prerelease publication use that same already-created local tag only after recovery canaries, fresh REGATE review and final tip gates pass; publication must not create a different tag target. DECIDED: #460 remains open unless a separately reviewed producer fix and canary warrant closure; another ambiguous collaboration root stops the flow. DECIDED: release conclusions remain `release_impact=major`, `release_action=publish`, `release_channel=prerelease`. ASSUMED: the `@v4` correction PR merges cleanly onto the current pre-correction `main` base used for this stewardship (`a4425cb970af74deaf291d325c1fa29d115a18ec` at authoring), the local-candidate path still accepts clean explicit commits bearing exactly one matching annotated release tag, and preserved REGATE bytes remain unchanged; dirty source, a moved/retargeted rc.2 tag, changed REGATE bytes, or an additional unreviewed fork commit invalidates the affected evidence. LOCALLY DELEGATED: equivalent private implementation details, scratch layout, preflight-receipt placement and test ordering remain Implementer choices within these identities, gates and preservation rules.

## In Scope

1. Preserve accepted rc.2 source, PR/check, Pages and rc.1 immutability evidence when revision impact supports reuse; do not repeat heavy suites merely for ceremony. 2. Materialize this v4 contract, reconcile only the canonical files required for this tip/tag correction (policy manifest, `AGENTS.md`/`CHANGELOG.md`/`tasks-rc2` as needed), land by normal reviewed green PR, and execute on the contract's own canonical board; preserve earlier boards/versions as historical evidence and do not mutate completed REGATE verdicts into acceptance. 3. After the `@v4` correction PR merges: freeze `main`, record the exact merge SHA in a local/durable preflight receipt, and create exactly one local annotated tag `v1.0.0-rc.2` on that frozen commit without moving, deleting or retargeting it. 4. Preview and activate one immutable local recovery candidate through `aether update --local` from that tagged Aether commit and fork `aed6591…`, replacing the temporary patched runtime without patch replay or mutable-state rollback. 5. Verify installed source/lock identity, clean doctor and service health, then run the #450/#461 positive and negative canaries against the promoted runtime, including a real same-card review lifecycle whose claim records `source_status=review` and a successor boundary with no terminal predecessor alive. 6. Reuse the unchanged REGATE candidate/evidence but perform a fresh independent content review after item 5; rerun the website gate only if revision/change-impact or evidence inspection reveals a material reason. 7. At the frozen tagged tip, confirm release-facing source, HLP/reconciliation artifacts and lock coherence with fork `aed6591…`, and run the canonical Aether, website, HLP and release-input gates; do not advance `main` with another tip-changing source commit solely to re-pin the candidate. 8. Build and qualify two clean deterministic eight-member bundles, including schema-4 lock identity, privacy/member/normal-resolver checks and both fork defect matrices. 9. Push the same local annotated tag and publish/download-verify GitHub prerelease `v1.0.0-rc.2` without mutating rc.1 and without retargeting the tag. 10. Activate the published rc.2 bytes, prove state/projection, three-role and product surfaces, rollback and forward reactivation, replacing the local recovery candidate. 11. After observed activation, update only capability claims disproven by evidence, land by normal reviewed green merge, verify the authorized automatic existing-site Pages deployment/live content when triggered, reconcile Issues #261/#239/#439/#440/#445/#446/#450/#461 truthfully, keep #460 open absent its own fix, and audit objective-owned residue.

## Out of Scope

Stable v1.0.0; PyPI/OIDC/container publication; acquiring or widening credentials/providers; deployment to another machine, project or target; unrelated features or refactors; force/lease/mirror push, history rewrite, bypass/admin or direct-main push; deleting, rewriting, replacing or retargeting rc.1 or rc.2 tags/releases/assets; treating REGATE run 173 or any temporary live patch as acceptance; replaying `.patch` files into a promoted runtime; direct board-database edits or fabricated lifecycle events; manual Pages dispatch or unrelated Pages workflow/environment/settings changes; mutable-state rollback; fabricated WSL2/macOS/Windows evidence; closing #460 without a separately reviewed producer fix and canary; hardcoding a pre-merge recovery SHA that circularly self-pins this contract; inventing drain/wait-for-idle semantics the updater does not provide. Automatic deployments to the existing Aether Pages site caused by reviewed green merges required by this objective remain in scope.

## Authority

Authorized for this approved Aether objective: autonomous contract correction; source changes; independent review; ordinary branch push, PR, required checks and green merge; GitHub repository/settings/workflow changes when objective-required; post-merge freeze and local/durable preflight receipt recording the exact selected SHA; creation of exactly one local annotated `v1.0.0-rc.2` tag on that frozen commit; clean local-candidate runtime preparation and `aether update` preview/activation; supported immediate interruption/restart of Aether-owned services (no drain); later public push of that same tag plus rc.2 prerelease and assets only after recovery canaries/REGATE/final gates; existing-site Pages and other objective-required Aether deployments; rollback/reactivation; issue/residue cleanup and closeout, all through provisioned access. Morfeo owns intent, this contract and final acceptance; Supervisor independently reviews receipt, decides shared execution decomposition, integrates and closes execution; Implementers choose equivalent reversible unit-local methods and produce evidence. Authority does not create scope. No force/history rewrite, check bypass, secret exposure, credential acquisition/widening, protected-edge circumvention, unrelated-service restart, stable publication, PyPI/OIDC or other-project effect is authorized. This contract version itself does not create the tag, activate a runtime, publish, push, open a PR or merge.

## Deliverables

D1 Reviewed merged maintained-fork fixes #450 and #461 at exact fork `aed6591a…`, with historical Aether reconciliation PR #462 retained as pin evidence (not as the recovery tip). D2 Finalized `@v4` contract and required canonical reconciliations landed by normal reviewed green merge; local/durable preflight receipt naming the exact post-merge frozen SHA; exactly one local annotated tag `v1.0.0-rc.2` on that commit. D3 One clean versioned immutable local recovery runtime produced and activated through the supported update path from that tagged Aether commit and fork `aed6591…`, replacing the temporary bootstrap. D4 Runtime identity/doctor/service receipts and real #450/#461 positive and negative canaries, followed by a fresh valid independent REGATE review; run 173 remains explicitly non-accepting history. D5 Exact frozen tagged Aether release tip and fork pin, with every release-facing source, HLP/reconciliation artifact and lock coherent. D6 Rc.1 tag/assets/rejection warning unchanged; rc.2 local tag never moved, deleted or retargeted. D7 Rc.2 version/changelog/readme truth, deterministic eight-member bundle, lock/provenance/checksums, privacy/member/normal-resolver evidence and downloaded-asset verification. D8 Normal PR/check/merge for this correction, then public push of the same annotated tag and GitHub prerelease `v1.0.0-rc.2`. D9 Activated published immutable runtime replacing the local recovery candidate, with doctor/state/projection/gateway/ticker/plugins/Projects/Graphify/three-role evidence. D10 Verified rollback and forward reactivation without mutable-state rollback. D11 Updated issue dispositions, evidence-backed capability registry/reference, authorized Pages deployment/live readback, and complete objective-owned residue audit.

## Acceptance Criteria

AC-01 Fork `aed6591a…` has primary-source PR/review/merge identity containing both #450 and #461; no later unreviewed fork commit is substituted. The selected Aether tip is the clean `origin/main` merge of the reviewed `@v4` correction PR, with its exact SHA recorded only in the post-merge preflight receipt (not hardcoded here as a pre-merge pin). AC-02 After that merge, `main` is frozen for the recovery/publish candidate window; exactly one local annotated tag `v1.0.0-rc.2` points at that frozen commit and matches `VERSION`; the tag is never moved, deleted or retargeted. AC-03 Before REGATE acceptance, `aether update --local` preview and explicit activation produce a versioned immutable runtime whose lock, source imports and installed bytes resolve to that tagged Aether commit and fork `aed6591…`; the former temporary bootstrap is not in the selected runtime, mutable state is preserved, Aether-owned service identity is refreshed under supported immediate interruption/checkpoint semantics (no drain), and `aether doctor --json` is ready. AC-04 On that promoted runtime, native canaries prove #450 terminates or holds every terminal non-current predecessor before successor mutation while a long-running current worker survives, and #461 preserves `review` across phase-less routed recovery while an explicit ready recovery remains `ready`. A real v4-board same-card review is claimed from `source_status=review`; no superseded worker remains alive beside it. AC-05 Only after AC-03/04, a distinct Supervisor independently reviews the unchanged REGATE commit `10bce0e7…` and five matching attachments. Review may reuse their measured outputs after change-impact inspection; run 173 is never cited as the accepting verdict, and changed bytes require new evidence. AC-06 README and wheel METADATA identify `1.0.0rc2` truthfully and deny stable, PyPI and WSL claims. AC-07 #446 retains independent review, normal merge, Pages success and live readback; later authorized docs coherence is separately evidenced. AC-08 Rc.1 annotated tag, commit and eight assets remain byte-unchanged with rejection warning. AC-09 Two clean roots produce byte-identical eight-member rc.2 bundles from the frozen tagged Aether tip and fork `aed6591…`, with schema-4 lock, HLP-426/HLP-427 reconciliation, privacy/member/normal-resolver and fork-matrix checks. AC-10 Rc.2 annotated tag used for publication is the same local tag from AC-02; it dereferences to the frozen accepted main commit; release is prerelease/not draft; every downloaded asset matches qualified checksums/digests. AC-11 Supported activation of published bytes replaces the local recovery candidate; mutable state survives activation, rollback and forward reactivation; doctor, projections, gateway/ticker/plugins/Projects/Graphify and three-role behavior are coherent. Issue dispositions and cleanup match evidence; #460 stays open absent its own fix. Stable/PyPI/WSL remain unverified.

## Testing Standard

Use the existing A1 standard without weakening. For the tip/tag prerequisite: verify the `@v4` correction merge identity on `origin/main`, confirm the preflight receipt SHA equals that merge, confirm exactly one annotated `v1.0.0-rc.2` tag points at it and matches `VERSION`, and refuse any moved/retargeted tag. For the recovery prerequisite: verify clean Git identities and source-tree digests; run focused #450/#461 tests plus their maintained-fork affected surface; preview `aether update --local` with the exact tagged Aether commit and fork commit and verify zero mutation; activate only after the preview is clean; verify the release selector, lock, editable/import mappings for every provisioned interpreter, clean `python -I` imports, changed gateway PID/start identity under immediate interruption (no drain), source-import origin, doctor and profile/plugin parity. Run the real native #450 coexistence/current controls and #461 review/ready controls, then exercise a genuine v4-board request-review/claim/complete lifecycle and inspect event/run/PID evidence. Do not strip identity markers, use `PYTHONPATH`, replay patches or edit the board database. For REGATE, compare preserved commit and attachment hashes, re-derive identity/oracle and inspect raw output; do not rerun heavy website suites unless bytes or change-impact invalidate the evidence. At the frozen tagged tip run `scripts/run_tests.py --clean-between-groups`, the configured 78% coverage gate, Ruff check/format, mypy, documentation/public-artifact checks, website `npm ci`/check/test/e2e/Pages build/path checks, HLP/fork matrices, and `git diff --check`. Build in two clean roots; inspect all eight bundle members; run privacy/secret scans; clean-install exact artifacts with the normal resolver and exercise CLI/runtime handshakes. After publication download and rehash every asset. Final activation records pre-state, preview/refusals/update, all runtime/state surfaces, rollback and forward reactivation. Reused evidence must name its producer revision and change-impact basis; tests never substitute for runtime, deployment or publication evidence.

## Stop Conditions

Stop the affected effect on project/repository/branch/tag ambiguity; moved selected refs; dirty required Aether or fork source; unreviewed/red source or checks; inability to freeze the post-`@v4`-merge tip or to create exactly one local annotated `v1.0.0-rc.2` tag on it; any move/delete/retarget of that tag; inability of the supported local-candidate path to produce an immutable runtime from the tagged commit and fork `aed6591…`; any surviving temporary bootstrap bytes in the selected runtime; failed doctor/import/service identity; failed #450/#461 positive or negative canary; a review claim not durably sourced from `review`; a terminal predecessor alive beside a successor; another ambiguous collaboration root; changed REGATE candidate/attachments without matching evidence; rc.1 mutation; nondeterministic bundle; failed downloaded hash; incompatible package resolution; failed Pages build/deploy/live readback; deployment outside Aether or unrelated to a required reviewed merge; inability to preserve mutable state; or need for force/history rewrite/bypass, secret exposure, credential acquisition/widening, new provider spending, PyPI or stable publication. The two earlier runtime-only repair attempts remain exhausted and authorize no third patch. The planned clean `aether update --local` promotion is the canonical replacement path, not another live repair; if it fails, use its supported rollback once, preserve evidence, and stop for a source-level correction under the recovery policy. Never resume or accept REGATE on the temporary bootstrap or by editing board state. Missing WSL2/macOS/Windows remains recorded, never fabricated.

## Canonical References

Owner’s current autonomous stewardship instruction and hard recovery baseline (no runtime-only patch counts; source → review → merge → immutable runtime promotion → real canary → REGATE review); rejected rc.1 contract `oc_3397f9f05d780f8e@v1`; superseded `oc_742f9f4797494bf9@v1`, `@v2` and `@v3`; Aether PRs #448/#449/#452/#455/#456/#457/#458/#462/#468, closed unmerged contract PR #463, the `@v3` materialization lineage, and the future `@v4` correction PR; maintained-fork PRs #12/#13/#14/#15 with exact merge `aed6591a69f453a1867b73628603e7b53ba40ffc`; Issues #261/#239/#304/#439/#440/#445/#446/#450/#460/#461; preserved REGATE candidate `10bce0e715355412a7f5350a4d162deeb6a61b8e` and five attachments as non-accepting evidence; rc.1 annotated tag/assets/rejection warning; inspected supported updater behavior at `src/aether_agents/lifecycle.py` (`local_candidate`, `_release_tag_at`, `service_plan` mode `immediate restart, no drain`) and `docs/guides/policy-and-recovery.md`; root/website `AGENTS.md`, `DESIGN.md`, `ROADMAP.md`, `CONTRIBUTING.md`, `README.md`, `docs/guides/lifecycle.md`, `docs/authority.md`, `specs/001-aether-v1-productization/` (including `tasks-rc2.md` and `evidence/RC2-V3-MATERIALIZE.md`), `HERMES_LOCAL_PATCHES.md`, release/lifecycle source, and canonical contract/recovery/review/release procedures. The public artifact must contain no machine paths, private runtime identifiers, credentials or board database contents.
