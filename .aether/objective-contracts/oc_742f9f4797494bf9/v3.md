---
artifact_type: "aether.objective-contract.v1"
project_id: "12027989-a08f-41cd-a82c-54ff1bfb6b03"
contract_id: "oc_742f9f4797494bf9"
version: 3
status: "final"
title: "Aether 1.0.0-rc.2: truthful prerelease successor, activation and rollback proof (#261)"
created_at_utc: "2026-09-16T11:32:45Z"
created_at_local: "2026-09-16T05:32:45-06:00"
finalized_at_utc: "2026-09-16T11:43:10Z"
finalized_at_local: "2026-09-16T05:43:10-06:00"
author_profile: "morfeo"
created_in_session: "cron_629633877cfe_20260916_045459"
finalized_in_session: "cron_629633877cfe_20260916_045459"
supersedes: "oc_742f9f4797494bf9@v2"
change_reason: "v2 incorrectly treated REGATE run 173 as valid after a temporary live bootstrap. Current owner instruction requires canonical #450/#461 source fixes, independent review, merge, immutable release/runtime promotion, and real board canaries before REGATE review; no runtime-only patch counts. v3 restores that gate while retaining the corrected fork pin aed6591a69f453a1867b73628603e7b53ba40ffc."
observation_trace_id: "ctr_7f1fbd707617ba722583d940df686f29"
---

# Objective Contract: Aether 1.0.0-rc.2: truthful prerelease successor, activation and rollback proof (#261)

## Owner Intent

The owner authorizes full autonomous operational execution inside the approved Aether 1.0.0rc2 objective and provisioned access, including contract correction, reviewed source integration, local immutable runtime promotion, service restart, GitHub prerelease publication, deployment, rollback/reactivation, issue reconciliation and cleanup. The recovery gate is explicit: the temporary live bootstrap and REGATE review run 173 do not count. REGATE may be accepted only after canonical #450 and #461 source fixes are independently reviewed and merged, promoted as an immutable versioned runtime through the supported `aether update` path, and a real board canary proves both review-phase restoration and superseded-worker reap. After that gate, continue REGATE review, bundle, publish and terminal activation. Rc.1 remains immutable, rejected and unactivated. This authority expands executable effects, not scope: do not invent unrelated work, acquire or widen credentials, expose secrets, waive acceptance, bypass a genuine protected edge, force or rewrite history, publish to PyPI, or claim stable or WSL qualification.

## Objective

Deliver one coherent Aether 1.0.0rc2 from corrected, reviewed and merged source. The maintained-fork input is exact merge `aed6591a69f453a1867b73628603e7b53ba40ffc` (tree `2258311f9aa52c58fda9bdbd03183ba50ac85df0`), carrying both #450 and #461; Aether main `e1af59c0d0f2684cc149fd362d6933dadb964c2c` contains the reviewed #462 reconciliation for that pin. First replace the temporary patched runtime with a clean immutable local candidate built from those exact commits through `aether update --local`, verify source identity, doctor/service health and real #450/#461 board behavior, then obtain a fresh valid independent review of the preserved REGATE candidate. Only after that recovery gate may the flow qualify deterministic release bytes, publish annotated prerelease `v1.0.0-rc.2`, activate the downloaded rc.2 release, prove rollback and forward reactivation without reverting mutable state, reconcile public claims and issues, and close objective-owned residue. Rc.2 is a prerelease candidate, not stable 1.0.0.

## Decisions and Assumptions

DECIDED: `oc_742f9f4797494bf9@v1` and `@v2` are superseded; v2 weakened the owner’s hard recovery gate by calling REGATE run 173 valid after a temporary runtime-only bootstrap. Run 173 and its completion are historical evidence, not acceptance. The preserved REGATE implementation commit `10bce0e715355412a7f5350a4d162deeb6a61b8e` and its five attachments may be reused as content evidence if unchanged, but a new independent review claim must occur only after immutable recovery promotion and the real canaries. DECIDED: executable Hermes source is exact clean `DarkArty07/aether-hermes:aether-main` merge `aed6591a69f453a1867b73628603e7b53ba40ffc`; `.patch` files, HLP records and the temporary bootstrap are audit/reconstruction evidence only and MUST NOT be replayed or layered onto the promoted runtime. DECIDED: the pre-REGATE recovery promotion uses the existing supported local-candidate interface, with a non-mutating preview followed by explicit activation from clean Aether `e1af59c0d0f2684cc149fd362d6933dadb964c2c` and clean fork `aed6591…`. It creates a versioned immutable runtime and replaces the temporary bootstrap; it is neither the public rc.2 release nor acceptance of final release bytes. This promotion is a dedicated interruption boundary: checkpoint the preview, backup and exact transition inputs durably, drain all objective workers, leave unrelated services untouched, allow the Aether-owned gateway to restart through the supported updater, and verify the transition from a successor/controller using the persisted receipt rather than claiming that a killed worker continued. DECIDED: after promotion, a real native canary must prove #450 predecessor reap before successor mutation, the long-running-current negative control, #461 phase-less routed recovery preserving `review`, and explicit ready recovery remaining `ready`; then the REGATE candidate receives fresh independent review from `source_status=review`. DECIDED: #460 remains open unless a separately reviewed producer fix and canary warrant closure; another ambiguous collaboration root stops the flow. DECIDED: release conclusions remain `release_impact=major`, `release_action=publish`, `release_channel=prerelease`. ASSUMED: the selected refs remain exact, the local-candidate path still accepts clean explicit commits, and preserved REGATE bytes remain unchanged; ref movement, dirty source, changed REGATE bytes, or an additional unreviewed fork commit invalidates the affected evidence. LOCALLY DELEGATED: equivalent private implementation details, scratch layout and test ordering remain Implementer choices within these identities, gates and preservation rules.

## In Scope

1. Preserve accepted rc.2 source, PR/check, Pages and rc.1 immutability evidence when revision impact supports reuse; do not repeat heavy suites merely for ceremony. 2. Materialize this v3 contract and execute it on its own canonical board; preserve the v1 board as historical evidence and do not mutate its completed REGATE verdict into acceptance. 3. Verify clean Aether `e1af59c0…` and fork `aed6591…`, preview and activate one immutable local recovery candidate through `aether update --local`, replacing the temporary patched runtime without patch replay or mutable-state rollback. 4. Verify installed source/lock identity, clean doctor and service health, then run the #450/#461 positive and negative canaries against the promoted runtime, including a real same-card review lifecycle whose claim records `source_status=review` and a successor boundary with no terminal predecessor alive. 5. Reuse the unchanged REGATE candidate/evidence but perform a fresh independent content review after item 4; rerun the website gate only if revision/change-impact or evidence inspection reveals a material reason. 6. Reconcile any remaining release-facing source to fork `aed6591…`, land by normal reviewed green PR, and run the canonical Aether, website, HLP and release-input gates at the exact merged release tip. 7. Build and qualify two clean deterministic eight-member bundles, including schema-4 lock identity, privacy/member/normal-resolver checks and both fork defect matrices. 8. Publish and download-verify annotated GitHub prerelease `v1.0.0-rc.2` without mutating rc.1. 9. Activate the published rc.2 bytes, prove state/projection, three-role and product surfaces, rollback and forward reactivation, replacing the local recovery candidate. 10. After observed activation, update only capability claims disproven by evidence, land by normal reviewed green merge, verify the authorized automatic existing-site Pages deployment/live content, reconcile Issues #261/#239/#439/#440/#445/#446/#450/#461 truthfully, keep #460 open absent its own fix, and audit objective-owned residue.

## Out of Scope

Stable v1.0.0; PyPI/OIDC/container publication; acquiring or widening credentials/providers; deployment to another machine, project or target; unrelated features or refactors; force/lease/mirror push, history rewrite, bypass/admin or direct-main push; deleting, rewriting or replacing rc.1 tag/release/assets; treating REGATE run 173 or any temporary live patch as acceptance; replaying `.patch` files into a promoted runtime; direct board-database edits or fabricated lifecycle events; manual Pages dispatch or unrelated Pages workflow/environment/settings changes; mutable-state rollback; fabricated WSL2/macOS/Windows evidence; closing #460 without a separately reviewed producer fix and canary. Automatic deployments to the existing Aether Pages site caused by reviewed green merges required by this objective remain in scope.

## Authority

Authorized for this approved Aether objective: autonomous contract correction; source changes; independent review; ordinary branch push, PR, required checks and green merge; GitHub repository/settings/workflow changes when objective-required; clean local-candidate runtime preparation and `aether update` preview/activation; drain-aware restart of Aether-owned services; rc.2 tag, prerelease and assets; existing-site Pages and other objective-required Aether deployments; rollback/reactivation; issue/residue cleanup and closeout, all through provisioned access. Morfeo owns intent, this contract and final acceptance; Supervisor independently reviews receipt, decides shared execution decomposition, integrates and closes execution; Implementers choose equivalent reversible unit-local methods and produce evidence. Authority does not create scope. No force/history rewrite, check bypass, secret exposure, credential acquisition/widening, protected-edge circumvention, unrelated-service restart, stable publication, PyPI/OIDC or other-project effect is authorized.

## Deliverables

D1 Reviewed merged maintained-fork fixes #450 and #461 at exact fork `aed6591a…`, with Aether reconciliation PR #462 at `e1af59c0…`. D2 One clean versioned immutable local recovery runtime produced and activated through the supported update path from those exact commits, replacing the temporary bootstrap. D3 Runtime identity/doctor/service receipts and real #450/#461 positive and negative canaries, followed by a fresh valid independent REGATE review; run 173 remains explicitly non-accepting history. D4 Exact final clean Aether release tip and fork pin, with every release-facing source, HLP/reconciliation artifact and lock coherent. D5 Rc.1 tag/assets/rejection warning unchanged. D6 Rc.2 version/changelog/readme truth, deterministic eight-member bundle, lock/provenance/checksums, privacy/member/normal-resolver evidence and downloaded-asset verification. D7 Normal PR/check/merge and annotated GitHub prerelease `v1.0.0-rc.2`. D8 Activated published immutable runtime replacing the local recovery candidate, with doctor/state/projection/gateway/ticker/plugins/Projects/Graphify/three-role evidence. D9 Verified rollback and forward reactivation without mutable-state rollback. D10 Updated issue dispositions, evidence-backed capability registry/reference, authorized Pages deployment/live readback, and complete objective-owned residue audit.

## Acceptance Criteria

AC-01 Clean Aether `e1af59c0…` and fork `aed6591a…` have primary-source PR/review/merge identity; fork contains both #450 and #461 and no later unreviewed commit is substituted. AC-02 Before REGATE acceptance, `aether update --local` preview and explicit activation produce a versioned immutable runtime whose lock, source imports and installed bytes resolve to those exact clean commits; the former temporary bootstrap is not in the selected runtime, mutable state is preserved, Aether-owned service identity is refreshed, and `aether doctor --json` is ready. AC-03 On that promoted runtime, native canaries prove #450 terminates or holds every terminal non-current predecessor before successor mutation while a long-running current worker survives, and #461 preserves `review` across phase-less routed recovery while an explicit ready recovery remains `ready`. A real v3-board same-card review is claimed from `source_status=review`; no superseded worker remains alive beside it. AC-04 Only after AC-02/03, a distinct Supervisor independently reviews the unchanged REGATE commit `10bce0e7…` and five matching attachments. Review may reuse their measured outputs after change-impact inspection; run 173 is never cited as the accepting verdict, and changed bytes require new evidence. AC-05 README and wheel METADATA identify `1.0.0rc2` truthfully and deny stable, PyPI and WSL claims. AC-06 #446 retains independent review, normal merge, Pages success and live readback; later authorized docs coherence is separately evidenced. AC-07 Rc.1 annotated tag, commit and eight assets remain byte-unchanged with rejection warning. AC-08 Two clean roots produce byte-identical eight-member rc.2 bundles from the final merged Aether tip and fork `aed6591…`, with schema-4 lock, HLP-426/HLP-427 reconciliation, privacy/member/normal-resolver and fork-matrix checks. AC-09 Rc.2 annotated tag dereferences to accepted main; release is prerelease/not draft; every downloaded asset matches qualified checksums/digests. AC-10 Supported activation of published bytes replaces the local recovery candidate; mutable state survives activation, rollback and forward reactivation; doctor, projections, gateway/ticker/plugins/Projects/Graphify and three-role behavior are coherent. Issue dispositions and cleanup match evidence; #460 stays open absent its own fix. Stable/PyPI/WSL remain unverified.

## Testing Standard

Use the existing A1 standard without weakening. For the recovery prerequisite: verify clean Git identities and source-tree digests; run focused #450/#461 tests plus their maintained-fork affected surface; preview `aether update --local` with the exact Aether/fork commits and verify zero mutation; activate only after the preview is clean; verify the release selector, lock, editable/import mappings for every provisioned interpreter, clean `python -I` imports, changed gateway PID/start identity, source-import origin, doctor and profile/plugin parity. Run the real native #450 coexistence/current controls and #461 review/ready controls, then exercise a genuine v3-board request-review/claim/complete lifecycle and inspect event/run/PID evidence. Do not strip identity markers, use `PYTHONPATH`, replay patches or edit the board database. For REGATE, compare preserved commit and attachment hashes, re-derive identity/oracle and inspect raw output; do not rerun heavy website suites unless bytes or change-impact invalidate the evidence. At the final merged release tip run `scripts/run_tests.py --clean-between-groups`, the configured 78% coverage gate, Ruff check/format, mypy, documentation/public-artifact checks, website `npm ci`/check/test/e2e/Pages build/path checks, HLP/fork matrices, and `git diff --check`. Build in two clean roots; inspect all eight bundle members; run privacy/secret scans; clean-install exact artifacts with the normal resolver and exercise CLI/runtime handshakes. After publication download and rehash every asset. Final activation records pre-state, preview/refusals/update, all runtime/state surfaces, rollback and forward reactivation. Reused evidence must name its producer revision and change-impact basis; tests never substitute for runtime, deployment or publication evidence.

## Stop Conditions

Stop the affected effect on project/repository/branch/tag ambiguity; moved selected refs; dirty required Aether or fork source; unreviewed/red source or checks; inability of the supported local-candidate path to produce an immutable runtime from the exact commits; any surviving temporary bootstrap bytes in the selected runtime; failed doctor/import/service identity; failed #450/#461 positive or negative canary; a review claim not durably sourced from `review`; a terminal predecessor alive beside a successor; another ambiguous collaboration root; changed REGATE candidate/attachments without matching evidence; rc.1 mutation; nondeterministic bundle; failed downloaded hash; incompatible package resolution; failed Pages build/deploy/live readback; deployment outside Aether or unrelated to a required reviewed merge; inability to preserve mutable state; or need for force/history rewrite/bypass, secret exposure, credential acquisition/widening, new provider spending, PyPI or stable publication. The two earlier runtime-only repair attempts remain exhausted and authorize no third patch. The planned clean `aether update --local` promotion is the canonical replacement path, not another live repair; if it fails, use its supported rollback once, preserve evidence, and stop for a source-level correction under the recovery policy. Never resume or accept REGATE on the temporary bootstrap or by editing board state. Missing WSL2/macOS/Windows remains recorded, never fabricated.

## Canonical References

Owner’s current autonomous stewardship instruction and hard recovery baseline (no runtime-only patch counts; source → review → merge → immutable runtime promotion → real canary → REGATE review); rejected rc.1 contract `oc_3397f9f05d780f8e@v1`; superseded `oc_742f9f4797494bf9@v1` and `@v2`; Aether PRs #448/#449/#452/#455/#456/#457/#458/#462, closed unmerged contract PR #463, and the future v3 materialization PR; maintained-fork PRs #12/#13/#14/#15 with exact merge `aed6591a69f453a1867b73628603e7b53ba40ffc`; Issues #261/#239/#304/#439/#440/#445/#446/#450/#460/#461; preserved REGATE candidate `10bce0e715355412a7f5350a4d162deeb6a61b8e` and five attachments as non-accepting evidence; rc.1 annotated tag/assets/rejection warning; root/website `AGENTS.md`, `DESIGN.md`, `ROADMAP.md`, `CONTRIBUTING.md`, `README.md`, `docs/guides/lifecycle.md`, `specs/001-aether-v1-productization/`, `HERMES_LOCAL_PATCHES.md`, release/lifecycle source, and canonical contract/recovery/review/release procedures. The public artifact must contain no machine paths, private runtime identifiers, credentials or board database contents.
