# Source authority map

This reference records the reusable evidence order for current Aether identity and runtime questions. Re-run every check against the current tree.

1. Current user instruction.
2. Root `AGENTS.md` and exact local branch/checkout.
3. Active `home/SOUL.md` prompt identity and routing policy.
4. Current product/architecture docs and accepted decisions.
5. `docs/releases/v0.23.0/STATUS.yaml` and operating policy.
6. Executable source, schemas and tests.
7. Read-only installed status or a deterministic probe for runtime claims.
8. Profile role contract, only after checking current participant policy.
9. Decision/release history and user memory, explicitly labeled as such.

## Hefesto example

The current physical profile, architecture document and status file agree that Hefesto is an allowed implementation archetype. That establishes identity and policy, not blanket dispatch authority. A real Dispatch still requires an admitted project, validated Task, provider/model/effect/budget authority and the current production-entry gate.

The reproducible formulation is:

> Hefesto is a present and allowed implementation profile; model-backed production use remains gated by the current release contract.

## Investigation recipe

1. Verify `/home/darkarty/Desktop/agentes/aether` and local `main`.
2. Read `AGENTS.md`.
3. Search the exact term across current files.
4. Classify each match as current authority, executable/runtime proof, role definition or history.
5. Build identity/lifecycle/policy/qualification/availability/effect-authority separately.
6. Cite decisive paths and label unknowns.

Stop when every factual claim has a classified source and no historical mechanism is presented as executable.
