# Olympus v3 project-local configuration template.
# Copy to AETHER_HOME/olympus_v3.yaml only when an isolated runtime needs it.

coordination:
  # Harmonia kernel remains default-off. Configuration does not authorize activation.
  enabled: false
  mode: legacy
  allowed_modes:
    - legacy
  project_allowlist: []
  max_active_runs: 0
