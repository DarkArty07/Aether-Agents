# Contributing

Keep changes limited to the versioned profile policy and template. Do not commit local configuration, credentials, databases, sessions, memories, or generated runtime state.

Before proposing a change, compare the tracked files with the canonical manifest and validate the workflow YAML. Publication is a separate protected action.
